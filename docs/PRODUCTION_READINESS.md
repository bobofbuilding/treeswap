# Production readiness

Status: Gate 0 is complete. Gates 1 through 6 are in progress. No funded testnet or mainnet gate is complete.

“Production” means a capped mainnet bridge operated against published limits after testnet evidence and independent review. It does not mean public pooled liquidity, yield, or unlimited permissionless exposure.

## Gate 0 — freeze the safety boundary — complete

- [x] Full-fill, two-direction intent format and immutable escrows.
- [x] Exact units, fees, beneficiaries, hashes, deadlines, nonces, and replay domains.
- [x] No public LP deposits, rewards, partial fills, email delivery, or hidden upgrade path.
- [x] Local unit, integration, adversarial, and stateful invariant campaigns.
- [x] Threat ledger, launch checklist, incident sequence, and CI.

## Gate 1 — prove the live EVM boundary — in progress

- [x] Implement a credential-safe observer for one finalized BIT snapshot.
- [x] Pin the EIP-1967 implementation slot, proxy and implementation code hashes, pause state, decimals, symbol, finalized block hash, and source commit in its output.
- [x] Fail on the wrong chain, missing bytecode, an empty implementation slot, malformed finality, or unsafe token state.
- [x] Pin one exact block behind each provider's finalized head, bind every state read to its canonical block hash with EIP-1898, and compare all safety-critical fields.
- [x] Reproduce the proxy from an exact Sourcify match and the implementation from Etherscan standard JSON, including the implementation-address immutables.
- [x] Fork the recorded block and pass live BIT snapshot, transfer-delta, both-direction open/claim/refund, pause/unpause, implementation-slot, false-return upgrade with locked-liability recovery, and cross-direction hash-reuse campaigns. The standalone runner now rejects the wrong chain or canonical pinned-block hash before Forge and reproduced all seven cases through two public archive RPC URLs; those URLs do not establish provider authentication or independence.
- [ ] Capture the observation through two independently operated Ethereum RPC providers and compare every field.
  - [x] The BIT/WBTC market-reference verifier now treats matching RPC observations as one pool venue, never multiple prices. It requires exact finalized-head agreement, the policy-pinned BIT proxy runtime plus EIP-1967 implementation slot/address/runtime and `BIT` / `18` / `false` state, the canonical WBTC runtime and `WBTC` / `8` / `false` state, the remaining source/runtime identities, pool maturity, active and wide-range liquidity, a request-sized directional probe, and a pinned WBTC/BTC peg feed before producing a non-authorizing signal. All nested inputs are snapshotted from exact plain data before validation, signature recovery, and hashing; accessor/coercion execution, hidden or inherited authority, sparse sets, and post-review mutation fail closed. The general risk gate hard-caps the complete price-candidate set at 64, collapses exact repeats, quarantines every otherwise-eligible observation involved in a source/venue/control/operator identity collision, and canonically orders the remaining independent set; caller order cannot choose the market median. Tests prove missing pool evidence and one pool alone cannot enable quotes. BIT upgrades and either token's runtime, pause, symbol, or decimal drift fail closed. The pool does not exist and no real provider observation has been supplied. See [BIT/WBTC market reference](./BIT_WBTC_MARKET_REFERENCE.md).
  - [x] Risk-attestation v3 now binds the exact direction, Lightning sats, net BIT wei, complete inventory snapshot, independent source set and observation windows, market median/spread, implied quote price, exact quote/market deviation, fee, reserves, earliest safe expiry, and policy from the original same-process evaluation. Both fixed-reference and live-market ceilings use exact cross-multiplication, so opposite-edge quotes and one-unit rounding bypasses reject in both directions. A BIT/WBTC signal must retain original provenance for that exact request and its probe BIT amount must equal the request; favorable exact-output market notional remains truthful rather than being rejected as shallow. The authenticated blind-RFQ path rejects each offer without original exact market evidence covering its complete signed lifetime or when the evidence policy differs from the reviewed release policy. The user's received-set commitment includes the risk digest, policy digest, and expiry; coordinator schema v9 persists them, execution-policy v3 binds them, and active execution rechecks them against the same-process release context after restart.
  - [x] Current published commit `37fa0044554451b392447ac79c83021c5f41ea02` captured a fresh authenticated Alchemy observation at canonical finalized block `25807484` and independently revalidated the exact block, proxy and implementation hashes, implementation slot/address, `BIT` / `18` / `false` token state, source provenance, mode-`0600` file safety, and privacy exclusions through Alchemy CLI `0.22.0`. The ignored file digest is `sha256:4948f7b8dcf8e05120fc6d506783081f78cdc0d6a01ebec872c2939de34bfeda`. Both passes use the same provider, so independent-provider comparison, review, promotion, and funding authorization remain open.
  - [x] Observation v3 now requires the canonical TreeSwap origin, an exact clean commit fetched from `origin/main`, a nonzero provider-identity commitment, a credential-free label, exact canonical fields, a closing chain/finality/target-block handshake, and a fresh one-hour comparison window. Comparison v2 binds both distinct identities and exact observation digests, uses bounded non-symlink inputs and mode-`0600` non-overwriting output, and states that organizational independence remains externally unverified and funding authorization is false. Historical v2 observations cannot enter this path.
  - [x] Exact published v3 source `fa331ff41bb0a49eab55d968f250959518649ffc` captured one authenticated Alchemy observation at canonical finalized block `25807580` and a separate Alchemy CLI `0.22.0` pass revalidated the block, finality, canonical EIP-1898 state, source, identity commitment, both runtime hashes, implementation, and `BIT` / `18` / `false` state. The private 1,446-byte mode-`0600` file digest is `sha256:448bbd98d68f9b7cb16430fe6a17ce99cffc73fc4ab08872d632eaf7f59d6913`. Both passes use the same provider account and operator, so the two-provider item remains open and no review, promotion, or funding authority exists.
  - [x] A short-lived provider-evidence ceremony now requires the two provider operators to sign the same exact observations, comparison, finalized block, published source, and policy through EIP-712. Provider signers, organization commitments, retained identity evidence, and retained service-control evidence must be distinct. The verified summary remains non-authorizing and explicitly says organizational independence requires external verification. See [Signed BIT provider evidence](./BIT_PROVIDER_EVIDENCE.md).
  - [x] A deterministic static [BIT ceremony preflight](./BIT_CEREMONY_PREFLIGHT.md) now validates both provider and both reviewer policies, all seven artifacts, safe findings, global control separation, matching published source, and a configured effective review ceiling of at least five minutes before live evidence starts expiring. The actual remaining lifetime is still enforced during live candidate preparation. Its digest-only summary contains no observation, signature, external-independence finding, manifest provenance, or funding authority.
  - [x] Clean published ceremony-preflight checkpoint `83f669d6c25b1814554cbdf17b640b704843bcff` passed 422 application/security tests with no skips, both production web builds, 68 contract tests, all 41 sealed local campaigns from `2026-08-22T10:39:12.817Z` through `2026-08-22T11:48:39.941Z`, and [hosted main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32568051678). Exact production deployment [`dpl_FtJvBKWtWjTHqA5ZSrbVonpzYVSh`](https://treeswap-pze2nd89b-bittrees-tech.vercel.app) is ready and serves the HTTP-200 [canonical alias](https://treeswap.vercel.app). The ignored mode-`0600` evidence independently reconstructs to `sha256:54b8bfbb76603f0e847a421f97a9c7d5fc7ff3b8972e4cc2334607b2356415cc`; all 133 configuration hashes, 41 unique passed campaigns, three pinned images, privacy exclusions, exact remote-`main` binding, and empty lab teardown reproduce. This remains local-only evidence with simulated EVM reservation and no independent provider, reviewer, public-testnet deployment, production infrastructure, inventory, or funding authority.
- [ ] Obtain independent review of both matched source bundles, compiler inputs, roles, storage, and upgrade behavior.
  - [x] A dedicated short-lived BIT review boundary now re-verifies the complete two-provider candidate and both provider signatures in-process, then requires distinct contract-security and provider-independence reviewers to sign the same source-, block-, comparison-, provider-, artifact-, findings-, and policy-bound EIP-712 record. Reviewer signers, organizations, identities, and retained identity evidence cannot overlap one another or either provider; critical, high, or open findings fail closed. The resulting summary remains non-authorizing and treats organizational facts and report truth as retained external evidence. See [BIT independent review evidence](./BIT_INDEPENDENT_REVIEW.md).
- [ ] Promote real reviewed observations into the BIT and TreeSwap deployment manifests and bind both into the release; never promote an `unreviewed-live-observation` automatically.
  - [x] The BIT-specific reviewed-manifest boundary now re-verifies both provider signatures and both reviewer signatures in one process, requires exact cross-layer source/block/proxy/comparison/provider links, and derives the proxy, implementation, token state, provider heads, review artifacts, findings, reviewers, and canonical digest only from module-private provenance. Saved summaries and cloned objects cannot enter the boundary; the short-lived output is private, non-overwriting, and explicitly grants no funding authority. This closes the repository derivation gap only; real providers, real review evidence, and the separate TreeSwap deployment remain required. See [Reviewed BIT deployment manifest](./BIT_REVIEWED_MANIFEST.md).
  - [x] Clean published reviewed-BIT-manifest checkpoint `a0e8291f6c20816a8ee66387a814f794300efe57` passed 418 application/security tests, both production web builds, 68 contract tests, all 40 sealed local campaigns from `2026-08-22T09:05:57.146Z` through `2026-08-22T10:15:06.747Z`, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32563882756). The exact [production deployment](https://treeswap-pru2plff2-bittrees-tech.vercel.app) is ready. The ignored mode-`0600` evidence independently reconstructs to `sha256:6274811e83196f98c2bd3543bab930fdc2e325e97bd3a81585ef2456b2fc0da9`; all 132 configuration hashes, 40 unique campaigns, three pinned images, privacy exclusions, and lab teardown reproduce. This is local-only evidence with a simulated EVM reservation; it supplies no real provider, reviewer, TreeSwap deployment, public-testnet finality, production infrastructure, or funding authorization.
  - [x] An exact promotion verifier now revalidates the complete deployment policy and matching fresh canonical observations, binds the finalized block, source, manifest, provider observation, review-bundle, and findings digests, and requires distinct EIP-712 approvals from every provider plus contract and operations reviewers. Its provenance-bound output has no funding authority. See [Signed deployment-manifest promotion](./DEPLOYMENT_PROMOTION.md).
  - [x] Clean published source checkpoint `bcbf2b03e7064be136cb54a8c567f905abec8516` passed 271 application/security tests, 91 direct pinned-runtime tests plus bounded-filesystem rollback, 68 contract tests, both builds, and all 33 sealed local campaigns from `2026-08-20T18:12:50.167Z` through `2026-08-20T19:20:55.699Z`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32401732721) passed the same source. Its ignored mode-`0600` evidence independently reconstructs to `sha256:cbb4f5b62033429e8db734a8fd98f29db6b4c444ccdf5ec18949f91059f90152`. It explicitly excludes real public-testnet operators, independent provider/reviewer evidence, production infrastructure, and funding authorization.
  - [x] The public-testnet evidence policy binds the exact reviewed deployment-manifest, source, admission, risk, and fee digests into every participant attestation. It cannot create or promote the manifest, and a changed digest invalidates every signature.
- [ ] Bind the exact signed preflight and finalized execution receipts into the release boundary.
  - [x] The postflight observer reconstructs four exact creation transactions and three ordered standard Safe calls, their successful Safe/registry events, canonical receipt blocks, isolated deployer nonce, and finalized zero-balance manifest through each provider. The retained preflight providers and operations reviewer plus a distinct contract reviewer sign the exact package. See [Signed closed-deployment postflight](./CLOSED_TESTNET_DEPLOYMENT_POSTFLIGHT.md).
  - [x] Promotion v2 requires module-private verification of the complete signed postflight, retains the exact provider/reviewer set and deployment facts, and signs the exact postflight record and policy digests. Release authorization v2 and runtime funding snapshots require the exact postflight and promotion digests.
  - [ ] Capture the preflight, receipts, postflight, promotion, and release chain on Sepolia through independently operated providers and hardware-backed signers.
- [ ] Run controlled execution-client reorgs before and after escrow authorization/claim and attach finality-rollback evidence.
  - [x] A deterministic Anvil campaign replaces actual vault and user-escrow blocks before authorization, after authorization, and after claim; both directions reject stale authorization/dispatch, roll back orphaned receipts and balances, and recover through one canonical beneficiary-bound claim.
  - [x] Published commit `1908b539e2bcb6fa48ce9c2883a0770979b82b01` repeats all six block-replacement boundaries on an Anvil fork of exact Ethereum block `25788856` using the pinned live BIT proxy and implementation. The clean-published, secret-free result has digest `0x1475c60668bf57ded78659302e1e03382f17a26c1d8479835f8a8a2436176507`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32366965287) passed 240 application/security tests, 89 pinned-runtime tests plus disk-full rollback, 68 contract tests, both builds, and every noncredentialed EVM campaign.
  - [ ] Repeat on public testnet with genuine finalized/unfinalized transitions, two independently operated authenticated providers, deployed test escrows, and retained operator evidence.

Run the observer only with an authenticated mainnet endpoint:

```sh
export TREESWAP_EVIDENCE_DIR=/absolute/path/to/an/existing/mode-0700-directory
ETHEREUM_RPC_URL=<secret> \
ETHEREUM_RPC_PROVIDER_LABEL=<provider> \
ETHEREUM_RPC_PROVIDER_IDENTITY=<nonzero-bytes32-identity-commitment> \
npm run observe:bit -- --out "$TREESWAP_EVIDENCE_DIR/bit-observation.json"
```

For the second provider, pass the first observation's `finalizedBlock.number` so both providers inspect the identical state:

```sh
ETHEREUM_RPC_URL=<second-secret> \
ETHEREUM_RPC_PROVIDER_LABEL=<independent-provider> \
ETHEREUM_RPC_PROVIDER_IDENTITY=<different-nonzero-bytes32-identity-commitment> \
npm run observe:bit -- --block <first-finalized-block> --out "$TREESWAP_EVIDENCE_DIR/bit-observation-2.json"
npm run compare:bit -- "$TREESWAP_EVIDENCE_DIR/bit-observation.json" "$TREESWAP_EVIDENCE_DIR/bit-observation-2.json" --out "$TREESWAP_EVIDENCE_DIR/bit-comparison.json"
```

Run each capture from a separate clean checkout after fetching `origin/main`. A provider identity is a stable commitment used to bind later signatures; different labels, identity commitments, accounts, URLs, or keys do not by themselves prove organizational independence. Retain the operators' public identities and commercial/control evidence outside the secret-free observation.

Only an eligible comparison from independently controlled operators may enter review. The comparison remains explicitly unreviewed until source verification, organizational-independence evidence, and reviewer signatures are attached.

After comparison, use the [signed BIT provider-evidence ceremony](./BIT_PROVIDER_EVIDENCE.md) to make both operators accountable for the exact bytes. Provider signatures prevent an unsigned coordinator fabrication from becoming a review input; they do not prove that the signers or backends are organizationally independent.

The current reproducibility and fork evidence is recorded in [BIT mainnet boundary evidence](./BIT_MAINNET_EVIDENCE.md) and [controlled EVM reorg evidence](./EVM_REORG_EVIDENCE.md).

The command refuses to overwrite an existing file and never records the RPC URL.

## Gate 2 — prove the Lightning boundary

- [x] Build a pinned, internal two-node Bitcoin/LND regtest with balanced private-channel liquidity.
- [x] Bake distinct exact-URI invoice, payer, and observer credentials and prove representative forbidden RPCs fail.
- [x] Complete a real 10,000-sat hold-invoice create, decode, pay, accept, settle, and success smoke across the nodes.
- [x] Connect the repository policy to separate real invoice and payer adapter processes on the internal regtest network.
- [x] Require an exact, short-lived Ed25519 coordinator authorization before every action; the adapters hold only the public key.
- [x] Authorization payload, verification call, envelope, and method-specific operation fields are exact plain own-enumerable data snapshotted once before signing or verification. Getters, symbols, hidden or inherited fields, prototype keys, custom prototypes, object coercion, unsupported methods or values, and post-sign mutation fail closed before adapter policy or LND contact.
- [x] Pin and verify LND TLS, mount one exact role credential per process, recheck node sync/active directional liquidity, and expose no host port.
- [x] Persist request IDs before dispatch and prove a completed payment remains replay-blocked after the payer process restarts.
- [x] Prove a payer authorization is rejected by the invoice process and representative forbidden macaroon RPCs fail.
- [x] Prove each credential's exact grant manifest against the pinned LND permission registry, require representative forbidden capability categories to fail specifically for authorization, and demonstrate live timeout enforcement.
- [x] Test standard invoices and hold invoices through create, accept, settle, cancel, expire, late-settle, and replay paths.
  - [x] Hold create/accept/settle, cancel, expiry, wrong and late preimage, exact signed-action replay, and restart while accepted pass through the isolated adapters.
  - [x] Standard invoices above the signed fee cap or per-payment cap are rejected before dispatch, and read-only tracking proves `NOT_FOUND`.
  - [x] Standard success, terminal no-route failure, exact-request replay, same-hash duplicate rejection, and payer- and invoice-side lost-response recovery pass with one-dispatch evidence.
- [x] Inject delayed and fast Bitcoin blocks, LND restart, lost responses, idempotent retry, force close, unsynced state, TLS identity change, credential rotation, and credential revocation. Accepted-state LND restart, terminal no-route failure, exact and same-hash duplicate rejection, one-dispatch payer and invoice recovery, overlap credential rotation and old-root revocation, real TLS certificate/pin rotation with rollback, live in-flight saturation, channel-offline recovery, genuine unsynced-node catch-up, full force-close recovery, and compressed-threshold stale-header rejection pass.
  - [x] Rapid blocks reach the live HTLC boundary; the adapter rejects settlement at a 24-block reserve, six blocks before the auto-cancel boundary observed in pinned LND.
  - [x] A real no-block interval holds LND's observed height/header past a compressed local-progress limit; the adapter independently requires chain sync, wallet sync, bounded past age and future skew, and fresh progress, rejects before dispatch, and read-only tracking proves no payment exists even when the synthetic header timestamp is ahead of wall time.
  - [x] Pausing the payer node across a real 500-block backlog forces a genuine unsynced catch-up state; exact payment authorization rejects, read-only tracking proves zero dispatch, and both sync flags plus the active channel recover.
  - [x] Force-closing the only channel blocks new exposure; the real commitment confirms, its node-reported CSV maturity and sweep complete, pending-close exposure clears, and a fresh balanced channel restores service with zero prior dispatch.
  - [x] The observer role now has the separate read-only WalletKit `PendingSweeps` grant. A strict aggregate evaluator marks every close, HTLC, and non-anchor sweep unsafe; permits only matured, non-immediate 330-sat anchors under count, total-value, age, retry, budget, and deadline caps; emits no Lightning identifiers; and drives the existing `lightning-node` halt reason. Each v2 collector signs a timestamp-independent digest of the exact aggregate block/state, and the two-collector quorum rejects state disagreement or observation-window skew even when both reports individually say healthy. The live force-close campaign proves unsafe-before-dispatch and bounded recovery. See [Lightning close and sweep monitor](./LIGHTNING_CLOSE_MONITOR.md).
  - [x] Draining one live channel side below a 100,000-sat exposure makes both outbound payment and inbound hold-invoice creation fail before dispatch; rebalancing restores the exact operations, with one payment and a canceled hold invoice.
  - [x] Separate live payer and invoice adapters saturate a 10,000-sat daily cap, restart against their durable journals, and reject the next exposure before dispatch; exact-boundary tests prove UTC value rollover never rolls permanent replay protection.
  - [x] Prove an exact replacement credential works concurrently, remains live after deterministic old-root revocation, and permits baseline recovery without leaving the temporary root or credential behind.
  - [x] Prove a real LND certificate/key rotation invalidates the old pin, preserves the node/channel, dispatches no payment, and recovers only after explicit peer reconnection and new-pin rollout, with rollback on failure.
  - [x] Prove fresh adapter state and restart cannot authorize exposure until a higher real block and new hash are durably observed; clock/height/hash/header conflicts remain latched until later progress.
  - [x] The uncompressed 3,600-second block-delay threshold passed again on published daemon-recovery commit `e860eabbfb188d3597df25ee8dfa14001126026e`: 3,603 monotonic seconds, 119 continuous observations, restart persistence after 1,813 seconds, deterministic no-progress rejection, and zero target-payment dispatch.
- [ ] Prove that the computed Lightning cutoff always precedes the EVM refund boundary by the published margin. Pure ordering, the live Lightning-height boundary, the integrated local EVM/LND campaign, and the credentialed pinned-live-BIT fork pass; public-testnet finality through independently operated providers remains.
  - [x] The integrated local campaign binds both live invoice directions to actual TreeSwap escrows, waits for twelve simulated EVM confirmations, claims BIT only from the matching paid-invoice proof, drives the accepted hold HTLC to its exact observed 24-block boundary, proves the vault is not refundable there, and proves claim/refund mutual exclusion at the exact EVM refund timestamp. Its privacy-safe output has no funding authority; mock BIT and simulated EVM finality remain explicit limitations.
  - [x] Clean published source `ccae7f05b4dbe8b082cc7880924717b781b20b6f` passed 279 application/security tests, 91 direct pinned-runtime tests plus bounded-filesystem rollback, 68 contract tests, both builds, and all 34 sealed local campaigns from `2026-08-20T19:45:37.092Z` through `2026-08-20T20:54:09.927Z`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32410170977) passed the same source. Its ignored mode-`0600` evidence independently reconstructs to `sha256:7bc2988a39081d511a46abb7e27e2160e5dceabe23354e6faeeab14c0381f9ec`. This remains local-only evidence with simulated EVM finality and grants no funding authority.
  - [x] A credentialed combined runner now requires clean published `main`, forks the exact pinned live BIT block, re-verifies the proxy, implementation, both runtime hashes, implementation slot, symbol, decimals, and pause state, then runs the same live-LND two-direction deadline sequence. Its distinct evidence verifier rejects every provenance mutation and cannot accept mock evidence. See [Pinned live-BIT cross-chain deadline evidence](./LIVE_BIT_CROSS_CHAIN_EVIDENCE.md).
  - [x] Clean published `main` checkpoint `814c1bc44bdb40a3985131442bdcbf95ff36a0a7` passed the credentialed combined runner through an authorized archive RPC against the exact pinned BIT proxy and implementation plus live LND in both directions. The secret-free result digest is `0x48ba2c72a3d1b27def1080824c3347eccd21e5d558d82c7c86c845c63a58cbcd`; no endpoint, invoice, hash, preimage, public-testnet finality, independent provider, production infrastructure, or funding authority is included.
  - [x] Current clean published `main` checkpoint `9ba7d89a8b555f749c2a0f9a450effff340d6833` repeated the complete credentialed sequence and retained it through the fail-closed mode-`0600` writer. The outer digest is `0xb9711ca69dcdad37f41de2ca2bd321534f0404bebd8d6facf89ad2f9f8cad70c`; the nested deadline digest is `0xe2c2efc01ae1f8e04f9765e21e1069ff9f071e87b5fea3e313e3d5816bd10f8e`. Exact provenance, timing, limitations, digests, and privacy exclusions independently revalidate.
- [ ] Produce a secret-free evidence bundle from the final published release commit containing versions, configuration hashes, test results, and timestamps—never macaroons, invoices, or preimages.
  - [x] A fail-closed generator requires clean published `main`, destroys only the named disposable regtest volumes before and after the campaign matrix, reruns every local qualification campaign, records no command output or environment data, rejects secret-bearing fields, and writes a non-overwriting mode-`0600` artifact under ignored `outputs/`. Initial or final destruction failure aborts evidence; an interrupted run cannot silently seed the next checkpoint.
  - [x] Qualification schema v2 binds the one-hour Lightning guard's validated wall and monotonic duration, continuous observation count and maximum gap, midpoint restart, deterministic no-progress rejection, zero observed block advances during the guard, and zero target-payment dispatch directly into the sealed digest. Its private companion is verified against the exact published source and removed before the final artifact is written; the qualifier also configuration-hashes both evidence builders and the guarded writer.
  - [x] A standalone exact-main verifier now reads only one bounded private regular artifact, reconstructs the complete release evidence, recomputes the current versioned configuration manifest including CI and its own verifier boundary, compares it with a fresh canonical `origin/main`, repeats that source check after verification, and emits a digest-only receipt with every authority explicitly false. Artifact, source, configuration, symlink, permission, and mutation faults fail closed.
  - [x] Qualification-review v1 reconstructs the exact final artifact bytes, requires the complete mandatory campaign plan and exact shared versioned configuration manifest including its own release boundary, unique immutable images, matching source and production-duration evidence, and one independent reviewer's short-lived EIP-712 signature over both SHA-256 commitments, report, findings disposition, reviewer identity, organization, date, and expiry. Candidate v6 requires that live uncopyable provenance and exact signed upstream agreement; candidates v4/v5 are rejected. A real final artifact and external reviewer remain required before this gate can close. See [Qualification review evidence](./QUALIFICATION_REVIEW_EVIDENCE.md).
  - [x] Clean published qualification-review checkpoint [`83165bb3216f9fec5ba857c4bbe46569161bd9bf`](https://github.com/bobofbuilding/treeswap/commit/83165bb3216f9fec5ba857c4bbe46569161bd9bf), merged through [PR #59](https://github.com/bobofbuilding/treeswap/pull/59) and corrective [PR #60](https://github.com/bobofbuilding/treeswap/pull/60), passed 491 application/security tests with one intentional deployment-only skip, both production web builds, 68 contract tests, 111 pinned-runtime coordinator tests, both dependency audits, and [exact-main CI](https://github.com/bobofbuilding/treeswap/actions/runs/32636650332). Production deployment record `6047448027` binds the successful generated target [treeswap-lycde9agr-bittrees-tech.vercel.app](https://treeswap-lycde9agr-bittrees-tech.vercel.app) to that exact source, and both official aliases ([Vercel](https://treeswap.vercel.app), [custom](https://treeswap.bittrees.org)) return HTTP 200. Its fresh sealed qualification ran from `2026-08-23T11:31:35.838Z` through `2026-08-23T12:42:30.150Z`; the ignored regular mode-`0600` file digest is `sha256:7732e07df1f5e8e5e82dd99bea884d999213abe5f60fb848cae3d94c6393a782`, while the canonical evidence digest is `sha256:7197f9c04bb1ee3ad15269f48b9da324822dda40107ee37c754cc1e8e486c93f`. Strict reconstruction passed all 41 ordered campaigns, all 160 current configuration hashes with zero mismatches, three distinct immutable image pins, source/privacy checks, and empty disposable-lab teardown. Nested duration digest `sha256:047c3d698701609e5d929feb28996b05bad246bd499ba4bd0d2666e5b9ca6116` proves 3,604 monotonic seconds, 119 observations, a 32-second maximum gap, restart persistence at 1,814 seconds, zero guard-interval block advances, and zero dispatch. This remains local-only evidence with a simulated EVM reservation and no public testnet, independent provider, solver or reviewer, production infrastructure, multisig, inventory, external review, or funding authority.
  - [x] Clean published schema-v2 checkpoint [`69ef5991de0bc37cd8fbabd4dcdd2cb41fc9c498`](https://github.com/bobofbuilding/treeswap/commit/69ef5991de0bc37cd8fbabd4dcdd2cb41fc9c498), merged through [PR #47](https://github.com/bobofbuilding/treeswap/pull/47), passed 472 application/security tests with no skips, both production web builds, 68 contract tests, all 41 sealed local campaigns from `2026-08-23T01:32:29.378Z` through `2026-08-23T02:43:17.386Z`, and [main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32610593250), including all 100 pinned-runtime coordinator checks. GitHub deployment record `6043231932` binds exact READY production deployment [`dpl_2NvLKu4JcbTjj1EJ3B5D1FQvG8Ko`](https://treeswap-mpvyk82gr-bittrees-tech.vercel.app), whose generated URL is authentication-protected, to that source; [treeswap.vercel.app](https://treeswap.vercel.app) and [treeswap.bittrees.org](https://treeswap.bittrees.org) serve it publicly with HTTP 200. The ignored mode-`0600` schema-v2 artifact independently reconstructs to `sha256:3deb006f01bb8e0afed88c682202719a3baec1847470279646b06035b3c36ae7`; all 148 configuration hashes, 41 unique passed campaigns, three immutable image pins, exact clean published-`main` binding, privacy exclusions, and empty lab teardown reproduce. Its nested companion digest `sha256:715c20d849b2ae6159fbc5c3ffab3f1b9f3552ac435351bec9c50460651fdec7` binds 3,604 wall seconds, 3,603 monotonic seconds, 119 observations, a 32-second maximum gap, restart persistence at 1,815 seconds, deterministic rejection, zero guard-interval block advances, and zero target-payment dispatch; the temporary companion was removed. This is local-only evidence with simulated EVM reservation and no deployed Lightning monitoring, public testnet, independent providers or solvers, production coordinator, multisig, inventory, independent review, or funding authority.
  - [x] Current hermetic-regtest checkpoint [`f74b86c802cd0f1b510f9d47637c130f25dbb546`](https://github.com/bobofbuilding/treeswap/commit/f74b86c802cd0f1b510f9d47637c130f25dbb546), merged through [PR #37](https://github.com/bobofbuilding/treeswap/pull/37), passed 447 application/security tests with one intentional deployment-only skip, both production builds, 68 contract tests, 99 pinned-runtime tests plus bounded-filesystem rollback, all 41 sealed campaigns, and [main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32587271070). Exact Vercel deployment [`dpl_8BiEcyY2FqdDTN48mrLtb5b3fDNu`](https://treeswap-6da8hwy7m-bittrees-tech.vercel.app) is `READY`, and both official aliases ([Vercel](https://treeswap.vercel.app), [custom](https://treeswap.bittrees.org)) return HTTPS 200. The qualification ran from `2026-08-22T17:17:17.951Z` through `2026-08-22T18:26:28.077Z`; its ignored regular mode-`0600` evidence independently reconstructs to `sha256:5d86008863e611b3f58a4a97ff4ecabbc193fb2413ca1923d5a716ad9179d724`. All 138 configuration hashes, 41 unique pass states, three immutable image pins, exact clean remote-`main` binding, privacy exclusions, and destruction of the lab both before and after the matrix reproduce. The artifact remains local-only, uses a simulated EVM reservation, and grants no funding authority.
  - [x] The shared source-provenance boundary now requires the exact canonical HTTPS TreeSwap origin and compares `HEAD` with a freshly queried remote `refs/heads/main`, not a potentially stale local remote-tracking ref, before qualification, live BIT fork evidence, deployment-artifact rebuilding, BIT observation, or owner-only account evidence. Every retained capture rechecks the same source afterward; invalid long-running BIT results are withheld or removed, and offline contract rebuilding rechecks before retaining artifacts.
  - [x] Published checkpoint `794b9f3bedb21dd6fe39dae0d7a10a5e94289899` passed all 25 local campaigns and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32312491429); its independently rebuilt local-only digest is `sha256:985eb9ff7eb610982edfa27088a731b6eabd277add22bfe0a7dabf6d61aba825`.
  - [x] Authenticated-endpoint checkpoint `f474e9c577f9c4e70183275f693ce89216e24032` passed all 25 local campaigns and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32318279120); its independently rebuilt local-only digest is `sha256:55170d90bb3509ca5c0f133327290863d158f2cfa6e0401178a7b8ce3d523b3a`.
  - [x] Capacity-reader checkpoint `67655f859ec70c191501d073e75cba808ce06def` passed all 26 local campaigns from `2026-08-20T02:15:55.025Z` through `2026-08-20T03:23:02.372Z` and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32323948108); its independently rebuilt local-only digest is `sha256:0c20cb3ea69ca7eb56ed5f79b215ad317523908ee09436ac4203966c90ac3d58`.
  - [x] Daemon-recovery checkpoint `e860eabbfb188d3597df25ee8dfa14001126026e` passed all 26 local campaigns from `2026-08-20T03:38:59.074Z` through `2026-08-20T04:45:56.472Z` and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32328893965); its independently rebuilt local-only digest is `sha256:65a86a92f7f93407df0cc8254e61cf3740fda2aaf9f76d663c3d9d49e8895419`. The mode-`0600` ignored record explicitly excludes production infrastructure, public testnet, independent review, and funding authorization.
  - [x] Deployment-observer checkpoint `44d929e708768d8bbe53087b415eda0f4ac75f43` passed 239 application/security tests, 89 direct pinned-runtime tests plus the bounded-filesystem rollback campaign, 68 contract tests, both builds, all 29 sealed local campaigns from `2026-08-20T10:44:40.005Z` through `2026-08-20T11:52:49.730Z`, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32360223386). Its independently rebuilt mode-`0600` local-only digest is `sha256:9a0bb29bc90d603327b56606603489247f2b3cab5f3be5ecad18d2cd8417d5e9`; the record explicitly excludes independent providers, production multisigs, public testnet, production infrastructure, review, and funding authorization.
  - [x] Live-BIT-reorg qualification checkpoint `c67d385cc00a6506ce7b8766d208c7c1ceb2b11b` passed 240 application/security tests, 89 direct pinned-runtime tests plus the bounded-filesystem rollback campaign, 68 contract tests, both builds, and all 29 sealed local campaigns from `2026-08-20T12:08:42.039Z` through `2026-08-20T13:16:50.096Z`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32367298446) passed the same published source. The ignored mode-`0600` record independently reconstructs to `sha256:20c43996d638a6d95b4d51a3c388701d5e245096dd9dc3eb9b1c5b77f986afef`. It configuration-hashes the credentialed live-fork runner; the separate clean-published fork result is `0x1475c60668bf57ded78659302e1e03382f17a26c1d8479835f8a8a2436176507`. Neither record includes public testnet, independent providers, production infrastructure, independent review, or funding authorization.
  - [x] Cross-chain-deadline qualification checkpoint `ccae7f05b4dbe8b082cc7880924717b781b20b6f` passed all 34 local campaigns, including the uncompressed 3,604-second/119-observation chain-delay campaign and the integrated two-direction EVM/LND deadline campaign. The clean-source artifact independently reconstructs to `sha256:7bc2988a39081d511a46abb7e27e2160e5dceabe23354e6faeeab14c0381f9ec`; its limitations exclude public testnet, production infrastructure, independent review, and funding authorization.
  - [x] Prior clean published checkpoint `9ba7d89a8b555f749c2a0f9a450effff340d6833` passed 381 application/security tests, both production builds, 68 contract tests, all 37 sealed local campaigns from `2026-08-21T21:15:10.042Z` through `2026-08-21T22:24:14.633Z`, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32520096089). Its ignored mode-`0600` evidence independently reconstructs to `sha256:164dfdf522a9dec83c391d62b1f283aafee41fdd4012d5be50f6cb8b460051dd`; all 113 configuration hashes reproduce, and the record excludes public testnet, production infrastructure, independent review, and funding authorization.
  - [x] Prior service-isolation checkpoint `0bd76995350011fdaf4cdd2f1e22c3db47a2e4f8` passed 388 application/security tests, the production Vercel build, 68 contract tests, and all 37 sealed local campaigns from `2026-08-21T22:58:49.911Z` through `2026-08-22T00:07:59.522Z`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32534947589) passed the same exact source. Its ignored mode-`0600` evidence independently reconstructs to `sha256:49fe2e48498ac10d251f975663bcdca17c1fd8a68ecb3c12e3502a67b75cc3a7`; all 116 configuration hashes, the exact campaign order, file safety, privacy exclusions, and lab teardown independently reproduce. The artifact is local-only, uses simulated EVM reservation, and includes no public testnet, production infrastructure, independent review, or funding authorization.
  - [x] Prior adoption-policy checkpoint `3bf4b17d94236c3ab3bf1ee3114c39ef6a01ee77` passed 393 application/security tests, the production Vercel build, 68 contract tests, and all 37 sealed local campaigns from `2026-08-22T00:41:36.305Z` through `2026-08-22T01:50:27.616Z`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32540977722) passed the same exact source. Its ignored mode-`0600` evidence independently reconstructs to `sha256:281f8210ca426239225f7bd344b699de840e51eb205b4ec02e2d88ee90d65e31`; all 117 configuration hashes, the exact 37-campaign order, three pinned images, source provenance, file and directory safety, privacy exclusions, and lab teardown independently reproduce. The artifact is local-only, uses a simulated EVM reservation, and includes no public testnet, production infrastructure, independent review, or funding authorization.
  - [x] Prior source-provenance checkpoint `0351d640c1f675bde186681c20e557e72a0663bd` passed 400 application/security tests, both production web builds, 68 contract tests, and all 37 sealed local campaigns from `2026-08-22T02:49:58.409Z` through `2026-08-22T03:59:20.853Z`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32547181431) passed the same exact source. Its ignored mode-`0600` evidence independently reconstructs to `sha256:c99c4cc32bacb5a54fad5f619d5ce185ba3eafa1c15adb79943fdf29c44cb22c`; all 121 configuration hashes, the exact 37-campaign order, three pinned images, file safety, privacy exclusions, lab teardown, and fresh remote-`main` equality reproduce. The 3,603-second Lightning delay included 119 observations and a midpoint restart. The artifact is local-only, uses a simulated EVM reservation, and includes no public testnet, production infrastructure, independent review, or funding authorization.
  - [x] Prior Gate 1 handoff checkpoint `2e29d4114c25e2561d367969c7ee84fd0910dc53` requires provider comparison to revalidate clean canonical published `main` before reading and before writing, rejects observations from a superseded source, and bounds authenticated JSON-RPC responses. It passed 400 application/security tests, both production web builds, 68 contract tests, all 37 sealed local campaigns from `2026-08-22T04:19:23.391Z` through `2026-08-22T05:28:28.757Z`, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32551249387). Its ignored regular mode-`0600` evidence independently reconstructs to `sha256:4749d29610c9199f1194d6853eb51f5a3f35cd3ee65ee9c69a79b30ff0dc8ef5`; all 121 configuration hashes, 37 campaigns, three pinned images, privacy exclusions, clean remote-`main` equality, and empty lab teardown reproduce. The 3,603-second Lightning delay included 119 observations and a midpoint restart after 1,812 seconds. The artifact remains local-only, uses a simulated EVM reservation, and contains no second independent provider, public testnet, production infrastructure, independent review, or funding authorization.
  - [x] Current signed-provider checkpoint `65bbd9603771d089ba70050e5121be564895eba5` adds the short-lived two-operator EIP-712 handoff and a closing chain/finality/target-block handshake. It passed 408 application/security tests with one intentional deployment-only skip, both production web builds, 68 contract tests, all 38 sealed local campaigns from `2026-08-22T05:57:03.649Z` through `2026-08-22T07:06:31.717Z`, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32555547734); the exact production deployment is [ready and promoted](https://treeswap-ln6sm26py-bittrees-tech.vercel.app). Its ignored mode-`0600` evidence independently reconstructs to `sha256:89d8613a766508b9049601a78a5cece6544d0456f105845d19895fd48045eee5`; all 126 configuration hashes, 38 unique passed campaigns, three pinned images, canonical source binding, privacy exclusions, and empty lab teardown reproduce. The artifact remains local-only, uses a simulated EVM reservation, and contains no real second provider, organizational-independence finding, public testnet, production infrastructure, independent review, or funding authorization.
  - [x] Current independent-review-handoff checkpoint `e893b65038cea566ecbd7a2c63795d6ab99abe7f` requires two distinct reviewers to bind the exact still-live signed provider evidence, seven exact review artifacts, reconciled findings, source, compiler, storage, upgrade behavior, and provider-independence conclusions. It passed 414 application/security tests with one intentional deployment-only skip, both production web builds, 68 contract tests, all 39 sealed local campaigns from `2026-08-22T07:31:35.891Z` through `2026-08-22T08:40:43.586Z`, and [hosted main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32559681865); the exact [production deployment](https://treeswap-o24m03oo6-bittrees-tech.vercel.app) is ready and serves both official aliases. Its ignored regular mode-`0600` evidence independently reconstructs to `sha256:ffb53940aa999e2cb4556d4cf5037a157697b31fb9a3c46744395f8ce9ca4b00`; all 130 configuration hashes, 39 unique passed campaigns, three pinned images, canonical clean remote-`main` binding, privacy exclusions, and empty lab teardown reproduce. The 3,603-second Lightning delay included 119 observations and a midpoint restart after 1,813 seconds. The artifact remains local-only, uses a simulated EVM reservation, and contains no real second provider, external reviewer or reports, organizational-independence finding, public testnet, production infrastructure, or funding authorization.
  - [ ] Attach the final release artifact digest, reviewer, and date to the signed deployment manifest.

The current adapter boundary, live lab, and remaining fault matrix are documented in [Lightning adapter](./LIGHTNING_ADAPTER.md) and [Lightning regtest lab](./LIGHTNING_REGTEST.md).

## Gate 3 — build the durable automatic coordinator

- [x] Private-packet Lightning operations now enter action planning and dispatch through one exact method-specific plain own-enumerable data snapshot with canonical, bounded scalar types. Accessors are not invoked; object coercion, hidden/symbol/inherited/prototype fields, custom prototypes, and later caller mutation reject or become inert before the durable commitment, side-effect claim, authorization signature, or adapter contact. This does not authenticate deployed packet, coordinator, signer, or adapter infrastructure.
- [x] Active and retained-recovery job preparation now freeze one bounded exact-data daemon-evidence-policy snapshot before digesting or authority comparison. Getters are not invoked; mutation after snapshot is inert; hidden, symbol, inherited, prototype-key, custom-prototype, unsafe, executable, cyclic or oversized, sparse, and decorated inputs reject. This does not prove deployed policy truth or independence.
- [x] Persist intent nonce, payment hash, quote receipt, selected set, capacity epoch, reservation, Lightning action, and mutually exclusive terminal state through atomic transitions.
- [ ] Make every value-moving action idempotent and recoverable after a process crash or ambiguous response.
  - [x] Both official operator runtimes now use one module-owned EVM JSON-RPC HTTPS transport for the claim broadcast route and both reconciliation origins. Production configuration has no RPC callback, requires exact port-443 HTTPS URLs, rejects disabled certificate verification, and opens fresh connections without global `fetch` or a shared agent. Each request uses an unpredictable matching JSON-RPC ID, an exact six-method allowlist, bounded plain-data params, and a bounded complete HTTP 200 JSON result. Plaintext, nonstandard ports, URL credentials/fragments, caller transport substitution, wrong IDs, extra response fields, wrong content type/encoding, malformed or inconsistent lengths, oversized responses, and stalled bodies fail closed. Loopback HTTP injection remains explicit in local Anvil evidence only. This proves repository transport control, not deployed endpoint identity, credential custody, independent upstream providers, or public-testnet truth.
  - [x] Lightning actions use a durable one-dispatch outbox; process restart, transport loss, replay conflict, and malformed success enter `UNKNOWN` and block retries.
  - [x] Adapter success and reconciliation responses now require an exact empty-reason `allowed` audit for the same role, method, credential hash, request ID, intent, payment hash, invoice digest, amount, capacity epoch, and signed authorization window. Generic/cross-action/expired audits, unknown states, malformed or insufficient invoice/HTLC proofs, and non-exact `NOT_FOUND` errors cannot confirm or fail an action. They leave the one-dispatch action `UNKNOWN`. This is the exact semantic layer inside the separately authenticated response envelope below.
  - [x] Every parsed action and reconciliation outcome, including rejection, is now authenticated by a bounded domain-separated Ed25519 response envelope under one reviewed active-runtime-pinned adapter public key and key ID. Rejections bind the exact request ID and complete canonical authorization-envelope digest. Missing, modified, wrong-key, retired-key, unsigned, request- or authorization-replayed, noncanonical, accessor-bearing, sparse, or oversized envelopes stay `UNKNOWN` and cannot enter durable reconciliation evidence; even an authenticated rejection or exact `NOT_FOUND` remains non-authorizing and cannot permit retry. Eligible proof commitments bind the response digest, public-key digest, key ID, and authorization. Adapter startup rejects response/capacity key reuse, and active configuration rejects response/request key reuse. Real key custody, rotation, retained old-release recovery, encrypted transport, and compromised-signer drills remain deployment evidence.
  - [x] A live regtest payment recovers from a deliberately lost success response through a fresh read-only tracking request with dispatch count one.
  - [x] A live accepted hold invoice recovers from a deliberately lost settlement response through a preimage-free lookup with dispatch count one; SQLite, WAL, and shared-memory scans contain no preimage.
  - [x] Bind EVM claims to one signed transaction hash before broadcast; persist no raw preimage; permit only byte-identical rebroadcasts; require a canonical finalized successful receipt and exact `Claimed` event.
  - [x] A local Anvil campaign proves a real claim remains `UNKNOWN` after RPC acceptance, records its inclusion, and halts after the included transaction is removed by a snapshot reorg.
  - [x] The unattended daemon requires exactly two read-only EVM provider observations to agree before one durable transition; outage or disagreement leaves the action `UNKNOWN` without recording an inclusion.
  - [x] A second controlled Anvil campaign proves genuine local finalized success, higher-fee same-nonce replacement without claim execution, byte-identical rebroadcast only, provider disagreement before durable mutation, relayer substitution rejection, newly bound relayer rotation, and no persisted preimage. The two agreeing origins share one local backend and do not satisfy independent-provider evidence.
  - [ ] Repeat finalized success, provider outage/disagreement, dropped/replaced transaction and nonce contention, relayer-key rotation, and reorgs before and after authorization/claim on the pinned live BIT fork and public testnet using two independently operated authenticated providers.
- [ ] Run at least two independent RFQ relays plus direct solver endpoints; a relay may deliver but never rewrite or select a quote.
  - [x] The local multipath client sends only an unlinkable blind pricing request under a fresh challenge to the complete configured plan, authenticates each bounded blind-offer batch, supplies receipt time locally, requires two relay paths plus two distinct capability-bound direct solvers to each contribute valid retained offers, and commits safe failures and every response digest before user selection. The configured path/batch product is hard-capped at 128 candidates; candidates are verified before the smaller retained-competition cap, so invalid relay junk cannot consume quote slots, while excess valid offers are deterministically reduced to the best price/time/ID set with one offer per solver. Only the selected solver then receives private fields and must return a matching executable quote. Empty paths, invalid-offer cap poisoning, private-field leakage, flat executable lists, duplicate identities, repricing, solver substitution, and relay mutation fail closed.
  - [x] The blind competition boundary accepts only exact plain-data call, pricing, policy, and envelope schemas plus dense undecorated capability and market-attestation arrays. Getters, scalar coercion, hidden/symbol/inherited fields, custom prototypes, sparse entries, and array decorations fail closed. Every retained offer requires its original exact Lightning/BIT market-risk attestation through expiry. A future BIT/WBTC signal may inform that attestation as one venue but never becomes a bridge route or solver-selection authority.
  - [x] Public solver-capability and multipath RFQ clients now share the strict complete-response reader used by the operator plane. They require identity JSON and `no-store`; reject compression, unsupported charset, malformed, truncated, overlong, or BOM-prefixed UTF-8, noncanonical or conflicting framing, declared/actual length mismatch, invalid chunks, interruption, and declared or received oversize before parsing; cancel redirected, rejected, or malformed bodies without awaiting responder-controlled teardown; and keep the hard deadline active through the complete RFQ body. The decoder is fixed to the Node runtime module rather than the replaceable process global, and the fixed public Node HTTPS path streams through its own byte ceiling while retaining DNS/TLS pinning. This closes repository framing, encoding-normalization, truncation, cache, stalled-body, and rejected-body resource seams, but does not prove deployed endpoint behavior, relay/solver independence, availability, or global quote completeness.
  - [x] The RFQ delivery API snapshots exact plain own-enumerable caller, policy, pricing, path, request, response, and blind-offer records once through property descriptors, and admits only dense undecorated path/envelope arrays plus recursively bounded JSON. Accessors and scalar coercion are never invoked; hidden, symbol, inherited, prototype-named, custom-prototype, sparse, decorated, unsafe, non-JSON, cyclic, oversized, and post-call mutation authority data rejects or remains inert before transport. This is local repository evidence and does not attest remote paths, quote completeness, availability, or control independence.
  - [x] The production multipath RFQ factory validates and freezes the exact roster, policy, timeout, request lifetime, and response ceiling, then uses only the module-owned pinned public Node HTTPS transport, system time, and cryptographic challenges. The fixed one-shot production calls reject transport, clock, or entropy injection; those dependencies exist only in explicit test entry points and a separately branded test client. Every production call requires an `AbortSignal`; shutdown aborts every path, and cancellation never returns a partial selectable collection. Copied/extracted/closed clients reject. Wiring that client to a deployed service lifecycle and retaining shutdown/failover evidence remain open.
  - [x] The public-payload privacy assertion uses an exact nine-field plain-data allowlist with canonical primitive types and direction-matched output units. Unknown, symbol, prototype-named, accessor, nested, non-canonical, and disguised private fields reject without reading property getters. Privacy-safe audit redaction separately folds Unicode, case, and separators before matching sensitive key names.
  - [x] Clean published source [`28a3542d0f70027aef028e9f52d828958c846964`](https://github.com/bobofbuilding/treeswap/commit/28a3542d0f70027aef028e9f52d828958c846964) passed 438 application/security tests with no skips, both production web build paths, 68 contract tests, and all 41 sealed local campaigns from `2026-08-22T12:35:19.483Z` through `2026-08-22T13:44:47.684Z`; [hosted main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32573377971) passed the same source. Production prototype deployment [`dpl_2zxDPfgrrmrCzQdnj3P14NY8wT9C`](https://treeswap-60re0ks7w-bittrees-tech.vercel.app) is `READY` and serves [treeswap.vercel.app](https://treeswap.vercel.app) and [treeswap.bittrees.org](https://treeswap.bittrees.org). Its ignored mode-`0600` evidence independently reconstructs to `sha256:61fdb565cb75c8cf7e2f89f90a32d87a52e50f8c56ebe2b8e253c663e8323d3a`; all 135 configuration hashes, 41 unique passed campaigns, three immutable image pins, source and privacy boundaries, and lab teardown reproduce. This remains local-only proof with simulated EVM reservation and no independently operated paths, encrypted disclosure deployment, public-testnet solvers, production persistence, external review, or funding authority.
  - [x] Clean published blind-reservation source [`b4f10c1604ed2d4c384d05cc1971afc050536533`](https://github.com/bobofbuilding/treeswap/commit/b4f10c1604ed2d4c384d05cc1971afc050536533) passed 443 application/security tests with one intentional deployment-only skip, both production web builds, 68 contract tests, the 97-test pinned schema-v5 coordinator matrix plus bounded-filesystem rollback, and all 41 sealed campaigns from `2026-08-22T14:22:19.243Z` through `2026-08-22T15:31:47.379Z`; [hosted main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32578495706) and Vercel production deployment passed, while both official aliases return HTTP 200. The ignored regular mode-`0600` evidence independently reconstructs to `sha256:f6c7d23181fcc17989ef6f122ff8f08b52f4eae3b90d72157c1168dfd8397272`; all 137 configuration hashes, 41 campaign statuses, three immutable image pins, source equality, privacy exclusions, and lab teardown reproduce. This remains local-only proof with a simulated EVM reservation and no independently operated paths, encrypted disclosure deployment, shared production persistence, public testnet, external review, inventory, or funding authority.
  - [ ] Deploy the four-or-more paths under independently reviewed organizational, hosting, and key control; retain rotation and availability evidence; then measure suppression, duplication, latency, expiry, path outage, malformed responses, and failover through the signed public-testnet campaign. Local keys and distinct commitments do not prove independence.
- [ ] Operate a solver daemon that quotes, reserves, waits for finality, performs the exact Lightning action, relays the preimage, reconciles, and halts on any mismatch.
  - [x] The production coordinator image now starts a persistent closed supervisor rather than a smoke script. It permits only separated absolute durable/runtime paths, recovers interrupted actions to `UNKNOWN`, verifies the database, holds a stale-safe single-owner lease, emits only private aggregate health, and rejects any funding-enabled mode. It deliberately has no network or dispatch path; same-process release activation and value-moving execution remain deployment gates.
  - [x] Clean published supervisor checkpoint [`22fcc266cca06efff883f2c4a860f8cb257cd672`](https://github.com/bobofbuilding/treeswap/commit/22fcc266cca06efff883f2c4a860f8cb257cd672), merged through [PR #49](https://github.com/bobofbuilding/treeswap/pull/49) and corrective [PR #50](https://github.com/bobofbuilding/treeswap/pull/50), passed 478 application/security tests with no skips, both production web builds, 68 contract tests, 105 pinned-runtime checks plus bounded-filesystem rollback and default-container health, and all 41 sealed campaigns from `2026-08-23T04:26:46.982Z` through `2026-08-23T05:37:37.309Z`; [exact-main CI](https://github.com/bobofbuilding/treeswap/actions/runs/32617694145) passed. Production prototype deployment [`dpl_CTg2Pnka8CB1irhuR8rLp1wUu6WM`](https://treeswap-1thu9n1es-bittrees-tech.vercel.app), deployment record `6044332294`, is `READY`, and both official aliases return HTTP 200. Its ignored mode-`0600` evidence independently reconstructs to `sha256:24e68be32de7e88c479e6d6125907be9dce593ed2cb14785248ba502ae7a9f29`; all 151 configuration hashes, 41 unique campaign results, three immutable image pins, source/privacy boundaries, and teardown reproduce. The nested `sha256:0285b6d5a47519f9e908f6fbc29464de6b25a6f2947ee371ff1389e5ee855de8` guard covered 3,604 wall and monotonic seconds, 119 observations, maximum 32-second gap, restart at 1,813 seconds, zero block advances, and zero dispatch. This remains local-only proof with simulated EVM reservation and no funded activation service, independently operated paths, production persistence, public testnet, external review, inventory, or funding authority.
  - [x] Current schema v9 atomically persists opaque identity quotas, rolling cancellation/request events, permanent cancellation sequences, verified capability digests and expiries, monotonic solver capacity epochs, separate BIT and Lightning firm commitments, routing headroom, one-use executable bindings, reviewed market-risk policy authority, fill/failure reliability, and suspension. A quote cannot outlive its capability or market evidence; any legacy database with an active offer or nonterminal settlement that lacks a current binding refuses migration. Independent local connections cannot oversubscribe an identity, either capacity dimension, or executable binding, and fills close the RFQ plus release competitors in one transaction.
  - [x] Published recovery checkpoint `dbc9f1daa205549a0af559bc024c40b347ca8ecd` passes 18 persistence tests directly inside the immutable Node `22.22.0-alpine` coordinator image plus a real bounded-filesystem `SQLITE_FULL` campaign. Verified non-overwriting backup/fresh-path restore, startup integrity and unknown-schema refusal, SIGKILL/WAL recovery, v2 migration, and rollback without partial state pass locally and in [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32311057995).
  - [x] The local capability verifier feeds only fully verified, independently observed capacity plus its signed expiry into the ledger; malformed proofs, cross-direction replay, capacity overstatement, stale observations, epoch rebinding, and expired capability/quote combinations fail closed.
  - [x] A local authenticated endpoint protocol binds each fresh request challenge, exact solver, direction, origin, response window, capability envelope, and Ed25519 response signature. Its default client refuses redirects and private, reserved, mapped, or mixed DNS; pins the public address through TLS; preserves SNI/hostname verification; bounds response work; and enforces a hard deadline.
  - [x] Concrete local readers independently compare finalized, canonical EIP-1898 BIT-vault state across two providers and obtain direction-bound, reserve/budget-capped Lightning aggregates through a separately signed short-lived request/observation protocol. The Lightning observation contains no channel identifiers and authenticates private node state; it is not a trustless balance proof.
  - [x] A deterministic durable-state planner prioritizes restart recovery and ambiguity, enforces direction-specific action order, forbids a BIT claim before confirmed Lightning payment, and halts paid-but-unclaimable paths. Confirmed payment preimages can be re-read after restart through an exact signed request but are never persisted.
  - [x] Published checkpoint `e860eabbfb188d3597df25ee8dfa14001126026e` passed 210 application/security tests, 52 direct pinned-runtime coordinator tests plus the bounded-filesystem rollback campaign, both web builds, 68 contract tests, every live Lightning campaign, and hosted CI. Its real payer lost-response campaign restarts twice, re-reads the exact hash-bound success proof without redispatch, and finds no invoice or preimage in SQLite, WAL, or shared memory.
  - [x] A local signed one-use private-packet protocol binds the exact settlement, reservation, action, quote, selected offer, capacity epoch, payment hash, invoice digest, deadlines, direction, purpose, provider, and fresh caller challenge. A valid response cannot be signed or returned unless an atomic replay consumer first accepts its exact requester key, request ID, request digest, service time, and expiry; missing, failed, and duplicate consumption reject. A concrete local handler now authenticates and claims before the private read, uses an uncopyable packet-specific wrapper over the strict SQLite replay engine, derives response expiry, consumes before signing, persists a clock high-water mark, bounds request work under one hard timeout, and returns generic errors. Only the original module-private object-identity verification may reach execution; plain, spread-copied, and serialized results reject. Both reviewed operator runtimes require the production factory's module-owned fixed Node HTTPS client transport, system clock, cryptographic request-ID source, and separate requester/provider keys; transport, clock, and entropy injection is isolated in an explicitly test-only factory and rejects before composition. The fixed path opens a fresh port-443 connection with certificate verification and no global `fetch`, shared agent, or process-wide Undici dispatcher. Replay, mutation, wrong action/provider/purpose, deadline inflation, plaintext/public/credential-bearing/path-bearing/query-bearing/nonstandard-port origins, globally disabled certificate verification, cacheable or ambiguous responses, complete-body stalls, and an EVM packet containing a preimage fail closed. The expanded local matrix passes 447 application/security tests with one intentional deployment-only skip, both web builds, 68 contract tests, and 99 pinned-runtime tests plus bounded-filesystem rollback; the exact clean-published-source qualification is recorded in Gate 1. Deployment on a separate persistent volume, certificate identity, reviewed reader/secret scope, trust-root control, network policy, private routing, backup/restore, drills, and live encryption evidence remain external. See [Durable private-packet provider](./DURABLE_PRIVATE_PACKET_PROVIDER.md).
  - [x] A bounded local runtime executes one planner step per call and accepts only original module-private verification of a dual-signed, short-lived EIP-712 record before reservation, Lightning dispatch, EVM-claim dispatch, or terminal reconciliation. The record binds release/policy, chain, solver, direction, escrow/runtime, settlement, reservation transaction/block, intent, purpose-specific packet/action/deadlines, and proof digest; nominal booleans, copies, signer substitution, mutation, cross-purpose reuse, stale/future evidence, unsafe deadlines, and exact expiry halt. One-shot ambiguity handling and planned-but-unbound EVM transaction recovery remain enforced, and a nominal both-assets callback cannot create completed history. Deployed evidence producers, release-bound configuration, and organizational independence remain external.
  - [x] Clean published daemon-evidence checkpoint [`3f1e3a9be37a6a417d4b876f8d018e6259f38b2b`](https://github.com/bobofbuilding/treeswap/commit/3f1e3a9be37a6a417d4b876f8d018e6259f38b2b), merged through [PR #55](https://github.com/bobofbuilding/treeswap/pull/55), passed 484 application/security tests plus one intentional deployment-only skip, both web builds, 68 contract tests, 111 direct pinned-runtime tests plus bounded-filesystem rollback, and all 41 sealed local campaigns from `2026-08-23T07:48:03.500Z` through `2026-08-23T08:58:51.936Z`; [exact-main CI](https://github.com/bobofbuilding/treeswap/actions/runs/32626394096) and production deployment record `6045711512` passed. Its private mode-`0600` evidence independently reconstructs to `sha256:16b4b35e11ab39cd93f1e4fc0f0a9e83f5d982cd2ce2cfc688278c9eb4df5ebf`, including 152 matching configuration hashes, 41 unique passed campaigns, three immutable image pins, exact clean source, privacy exclusions, and empty teardown. The nested `sha256:cb5d66d3ba5de9e051275313e8f5cb66ede3d7798458b687a7a90b2edbde2ce3` duration record passed 3,604 seconds, 119 observations, restart persistence, zero block advances, and zero dispatch. It grants no funding authority and does not supply the remaining deployed independent infrastructure, public-testnet, multisig, inventory, or review evidence.
  - [x] The production daemon wrapper derives its evidence-policy digest from an uncopyable same-process release/solver context rather than caller input. Active and recovery context creation now accepts only exact plain own-enumerable outer records plus an exact primitive-only evidence policy and approver record. Symbols, hidden fields, accessors, prototype-named fields, custom prototypes, and object-coerced scalar values reject without invoking getters or coercion methods, so policy hashing and release/solver comparison cannot observe different values. Context creation binds the active release, live deployment, original solver proof, direction-specific escrow runtime, both evidence signers, and freshness limits. Execution additionally binds the durable selected offer's solver, direction, capability epoch, exact historical capability digest, RFQ notional, executable quote, second user authorization, reviewed release, and evidence policy; every Lightning dispatch rechecks active offer/RFQ state and funding authorization. Expired activation closes new exposure while fresh dual-signed evidence can still authorize recovery-only work in the original process.
  - [x] Schema v9 and a separate recovery-only activation close the local restart/cross-release gap. Every funded settlement must bind once, before reservation or action, to a unique selected offer, its historical solver capability, reviewed release and market-risk policy, and exact daemon-evidence policy through the original active context, live service lease, and unmodified store. After restart, raw evidence plus the historical five-role bundle and current provider quorum may create only a short-lived recovery context. It tolerates closed/emergency/paused/below-reserve incident state but authorizes no funding, new exposure, or Lightning dispatch. A fresh rotated capability for the same solver can reconcile the old settlement without changing its historical binding. Cross-release/policy/capability substitution, duplicate selected offers, late binding, copied contexts, method replacement, provider disagreement, implementation/ownership/accounting drift, and migration of any legacy nonterminal settlement fail closed.
  - [x] The packaged coordinator now continuously verifies that recovery-only activation in a distinct non-default mode. Release and recovery manifests are mutually exclusive, live outside runtime/database state, and never enter health output. Every refresh deactivates the old in-memory recovery authority first; failed, malformed, expired, externally revoked, clock-regressed, stopped, or restarted processes remain unhealthy and authority-free. Aggregate health may expose only reconciled incident flags and exact digests. The default service creates no solver context and invokes no action path.
  - [x] A separate packaged recovery-action loop can advance a fixed startup set of already-bound settlements by one step per cycle. It reacquires the original lease, current same-process recovery activation, fresh solver proof, historical execution-policy binding, and a cancellable same-process fence; closes both Lightning action planning and dispatch; and emits only aggregate outcomes. Local tests cover copied fences, forbidden Lightning authority, inactive verification, restart-only waiting/closure, and stop-during-external-read behavior.
  - [x] Recovery work intake is now database-derived instead of transported. A one-use module-private job-set proof requires the exact restored store used by retained-release readiness, every and only nonterminal settlement for that release, a complete field-level startup-set commitment, every retained evidence-policy group, the original fresh solver proofs, unchanged liability commitments, recursively snapshotted permitted runtime adapters, and a 64-settlement ceiling. An uncopyable lease permits only one active loop per restored release and survives stop until any in-flight cycle exits. Raw jobs, copies, reuse, concurrent activation, omissions, additions, another store, method replacement, post-verification mutation, stale authority, or any settlement/database progress before loop activation fail closed. The default service still does not instantiate the loop; deployed retained-custody restoration, runtime/provider wiring, persistent infrastructure, public-testnet recovery actions, and incident drills remain external gates.
  - [x] A packaged recovery-execution supervisor now owns verification refresh, aggregate health publication, background failure shutdown, and drain-before-release lifecycle for that fixed loop. Its status distinguishes bounded EVM claim recovery for existing liabilities from Lightning dispatch, new exposure, and funding, which remain false. Shutdown revokes verifier/fence authority before awaiting the in-flight cycle and exclusive job-set lease. The default entrypoint explicitly refuses execution mode unless a deployment-specific wrapper reconstructs retained custody, fresh solver proofs, runtime adapters, and the one-use set in the same process; no environment flag or network job queue can activate it.
  - [x] A packaged active-execution service boundary now owns bounded solver-policy preparation, complete database-derived cycles, release refresh, periodic integrity checks, aggregate health publication, background-failure shutdown, and cancellation-fenced lease removal. Active configuration requires the release manifest, bounded intervals and work limits, exact `single-host` mode, and exactly one expected replica; the lease records that mode. Preparation and any running/degraded cycle are unhealthy, and the active health schema reports funding, Lightning dispatch, and new exposure true only after one complete current-release cycle. The default entrypoint refuses active mode and accepts no environment-built solver or runtime policy.
  - [x] The reusable active service now durably journals each run and its aggregate status digest before health publication. Clean stops do not count as failures; startup/background failures and a prior unclosed run do. A bounded, policy-stable failure window opens a crash-loop breaker before release verification or policy preparation, and a read-only supervision command is non-healthy for preparation, degradation, stop, failure, corruption, or breaker state. This is local database evidence, not proof of a deployed persistent volume, orchestrator restart behavior, or delivered alert.
  - [x] A concrete dual-route evidence client now supplies exactly the daemon's reservation, Lightning-dispatch, EVM-claim-dispatch, and both-assets controls. One fresh signed request goes to distinct private origins for the release's Lightning operator and security reviewer; both must return the same exact record, their assigned valid EIP-712 approval, and a non-cacheable bounded response before the existing uncopyable verifier is created. Provider response construction requires a durable atomic request replay consumer. All authority-bearing configuration, call, request, policy, record, and approval inputs require exact own-enumerable plain data and are boundedly snapshotted once without invoking getters; validation, replay consumption, signing, and response construction use the same frozen values, so mutation during consumption cannot change the response. Hidden, symbol, accessor, sparse/decorated-array, unsupported nested, prototype-named, inherited, and unknown inputs reject. The reviewed active and recovery runtimes require controls built by the production factory with the module-owned fixed Node HTTPS implementation, system clock, and cryptographic request-ID source; transport, clock, and entropy injection is isolated in explicit test factories and rejected before operator composition. The fixed path opens a fresh port-443 connection with certificate verification and does not use global `fetch`, a shared agent, or the process-wide Undici dispatcher. Shared routes, one-route success, disagreement, copying, replay, signer/role/transport substitution, redirect, timeout, caching, mutation, and expiry fail closed. This does not prove deployed routes, certificate/trust-root identity, durable storage, operator independence, or truthful observations.
  - [x] The provider-side replay seam now has one concrete handler and strict SQLite ledger. It verifies and policy-matches the exact request, atomically claims before the independent read, derives every authority field from the request, signs only the assigned role, and consumes durably before responding. One-time initialization is explicit; ordinary startup refuses missing, empty, permissive, symlinked, corrupted, or altered state. A durable clock high-water mark rejects backward time before validation and across every authority boundary, including after expiry pruning and restart. Concurrent replay, reader/signing failure, storage outage, expiry during signing, copied provenance, reservation substitution, authority injection, live schema tampering, and prototype-named request or reader fields fail closed. Snapshotting preserves an own `__proto__` field for exact-schema rejection rather than silently converting it into an object prototype. The database excludes evidence bodies and transaction/proof/signature commitments. See [Durable solver-evidence provider](./DURABLE_EVIDENCE_PROVIDER.md). This is repository evidence only; it does not prove deployed TLS, persistent volumes, independent operators/readers, truthful observations, host-clock monitoring, or witnessed volume/clock-rollback drills.
  - [x] Private Lightning-capacity, packet, and dual-evidence clients now share one exact DNS-pinning boundary. Resolution requests every answer verbatim and rejects an empty set, any public result, or any declared/actual family mismatch before opening a socket. One validated private IP is pinned while the reviewed hostname remains the TLS SNI and HTTP Host. Private literals skip DNS but retain certificate verification. The shared module and adversarial tests are included in the release qualification configuration. This is repository evidence only; deployed DNS control, routing, egress, certificate, trust-root, and operator independence remain external.
  - [x] Private-packet and dual-evidence clients now share one strict complete-response boundary. Rejected or redirected bodies are canceled. Identity JSON and `no-store` are required; compression, malformed, truncated, overlong, or BOM-prefixed UTF-8, noncanonical or simultaneous content-length/transfer framing, declared/actual length mismatch, invalid chunks, interruption, and declared or received oversize reject before parsing. The fixed Node decoder cannot be replaced through the process global. The shared module and adversarial suite are included in release qualification and the coordinator image. This is repository evidence only; deployed proxy framing, TLS termination, and endpoint behavior remain external.
  - [x] A reviewed operator composition boundary now refreshes every solver with the concrete finalized BIT and authenticated Lightning readers, accepts only original same-process packet/evidence/Lightning/EVM runtime objects, requires the evidence controls' exact policy digest, and matches the EVM action chain/contract/code to the fresh capability before handing one uncopyable policy set to the active lifecycle. Its official launcher rejects arbitrary preparation callbacks; preparation is one-use, bounded, and aborts endpoint I/O on shutdown. Both launcher composition and the lower-level policy-set handoff accept only exact own enumerable data fields and undecorated dense arrays; symbol, hidden, accessor, sparse/decorated-array, unsupported nested, and prototype-key smuggling rejects without invoking getters. This is code-path evidence only and does not establish real endpoints, credentials, provider or operator independence, or deployment.
  - [ ] Deploy a thin operator-owned entrypoint through that reviewed launcher with live solver node-signature verification, both concrete evidence-provider handlers on separate persistent replay-ledger volumes, independent evidence readers, private-packet providers, Lightning/EVM adapters, secret handles, and alert routes. Enforce one replica outside the process and run real shutdown/timeout/provider/evidence-route/replay-store loss and rollback/adapter/process/host-restart drills. The local declaration, function identity, and filesystem lease are not organizational-independence evidence, orchestrator evidence, or distributed fencing.
  - [x] A reusable recovery-execution bootstrap now owns the outer same-process lease, store, verifier, custody/job-set preparation, handoff, signal, timeout, and teardown sequence. Preparation publishes a distinct schema that health always rejects and that exposes neither executable work nor any Lightning, exposure, or funding authority. Verification and lease ownership are checked before and after preparation; cancellation revokes the verifier before waiting for the deployment callback and action loop to drain, then the store closes and the lease releases. This packages the lifecycle but cannot establish real custody, restored-host identity, solver-key possession, provider independence, adapters, or incident-drill truth.
  - [x] A reviewed recovery operator composition now supplies that bootstrap callback. One immutable one-use preparer fixes the absolute private custody path, release, restored host/process, authenticated capability clients, recovery-only evidence controls, packet provider, Lightning reconciliation adapter, EVM signer, and reconciliation providers. It repeatedly proves lease ownership, inspects custody, refreshes every solver possession proof, matches solver/direction/chain/escrow/code/policy, derives readiness from the current same-process activation, and constructs the exact restored-database job set. Recovery evidence controls omit Lightning authorization; active controls, copies, accessors, extra or prototype-named fields, nested runtime prototype keys, substitution, duplicate policies, cancellation, lost lease, and reuse reject without prototype mutation. See [Recovery operator entrypoint](./RECOVERY_OPERATOR_ENTRYPOINT.md). Deployment and drill evidence remain external.
  - [x] Clean published recovery-only checkpoint [`337a1dc5cb1e1279b4a9e441958d529da4312933`](https://github.com/bobofbuilding/treeswap/commit/337a1dc5cb1e1279b4a9e441958d529da4312933), merged through [PR #68](https://github.com/bobofbuilding/treeswap/pull/68), passed 509 application/security tests with 508 passes, zero failures, and one intentional published-main-only skip; 126 exact-image coordinator checks plus the disk-full fail-closed probe; 68 contract/fuzz tests; lint; both production web builds; and both production dependency audits with zero known vulnerabilities. [Exact-main CI](https://github.com/bobofbuilding/treeswap/actions/runs/32725580011) passed all four jobs. Production deployment record `6062374046` binds the source to [treeswap-kd7p3omv5-bittrees-tech.vercel.app](https://treeswap-kd7p3omv5-bittrees-tech.vercel.app), and both official aliases ([Vercel](https://treeswap.vercel.app), [custom](https://treeswap.bittrees.org)) return HTTP 200. All 41 sealed local campaigns passed from `2026-08-24T12:09:43.081Z` through `2026-08-24T13:20:51.818Z`. The ignored mode-`0600` file digest is `sha256:e7f1f8753d5e4b535f138ca7f1a227cd00fb8ec5489f0d68d05b9eff252f630d`, and its independently reconstructed canonical evidence digest is `sha256:46c723602181a05032a746e4d6c0d086e5b75c5410118d3c31986db5ac2c9e12`; all 167 configuration hashes, 41 unique ordered campaigns, three immutable image pins, exact clean source, privacy exclusions, and empty disposable-lab teardown reproduce. Nested duration digest `sha256:e66afa1d551bdc7f4da094e7578dc6c0cce4cb41affb8e5901eb2148a9ef987f` proves 3,604 wall and monotonic seconds, 119 observations, a 32-second maximum gap, restart persistence at 1,813 seconds, zero guard-interval block advances, and zero dispatch. This remains local-only evidence with a simulated EVM reservation and supplies no deployed public-testnet contracts or multisigs, live independent providers/operators/signatures, retained old-release recovery infrastructure, seven-day solver campaign, inventory, independent review, or funding authority.
  - [ ] Deploy and drill release-retention custody: keep every raw release input, approval bundle, exact provider configuration/identities, evidence policy, compatible runtime, and solver recovery key available until its nonterminal-settlement count is zero. Exercise recovery before owner/provider/key/schema rotation and demonstrate old-release recovery after a process and host restart without enabling Lightning dispatch.
    - [x] A fail-closed local custody and rotation boundary now commits directly to the coordinator's complete nonterminal liability set, reconstructs all raw release evidence, verifies private immutable file bytes plus the schema-v9 backup, requires exact policy/runtime/provider and recovery-authority coverage, and rejects unbound settlements or firm offers. A restored process must prove a distinct host/process, fresh same-process recovery activation, and fresh three-possession solver capabilities; witnessed action records require distinct organization commitments and zero Lightning dispatch/new exposure/funding. Rotation additionally rechecks the original live database and, with liabilities, requires distinct old/new custody catalogs over the identical snapshot plus fresh closed-gate drills for every release. This repository evidence does not establish real custody, independence, host replacement, retained keys/providers/images, or truthful recovery actions. See [Retained-release custody and rotation](./RETAINED_RELEASE_CUSTODY.md).
  - [x] The funded wrapper now also requires the exact original live coordinator lease. Its raw token remains private, copies and stale owners reject, and every pre-request or evidence-derived durable mutation rechecks ownership and a fresh internal clock after external reads. Private packets, dual-signed action evidence, live funding for new exposure, and the Lightning adapter envelope must still be current at actual use. Both Lightning and EVM paths check before outbox claim and immediately before network dispatch. Leadership or validity loss after a claim starts no request and is forced through conservative interrupted-action recovery; an already-started request may record only its result or ambiguity. This closes the local split-brain and slow-step expiry gaps; funded multi-replica operation remains unsupported.
  - [x] Active work discovery and lifecycle containment are now implemented locally. One uncopyable preparation binds a bounded immutable solver/runtime set to the original current release supervisor, service lease, and store. Cycles accept no caller-selected jobs, derive the complete nonterminal set from the original database, preflight every settlement against its selected offer and exact policy before executing any, rotate bounded work, and publish no identifiers. Unmatched or already-halted liabilities degrade with zero attempts; a newly reached halt or gate closure ends the cycle immediately; the v2 cycle commitment binds the discovered, blocked, attempted, and outcome sets; and none of those states can advertise funding, Lightning-dispatch, or new-exposure authority. Provider-refresh failure, changed release, copied proofs, forged supervisors, modified discovery methods, runtime mutation, and shutdown likewise close all advertised authority. The default image still refuses funded mode; the remaining repository/deployment handoff is a deployment-specific active entrypoint with persistent health/alerts and real-adapter crash drills, followed by a separately reviewed shared fence before any second replica.
  - [x] Clean published leadership/expiry source [`b804c20ff55f32a45ae7171554aca5cf8c7e36df`](https://github.com/bobofbuilding/treeswap/commit/b804c20ff55f32a45ae7171554aca5cf8c7e36df), merged through [PR #66](https://github.com/bobofbuilding/treeswap/pull/66), passed 506 application/security tests with 505 passes, zero failures, and one intentional deployment-only skip; 124 exact-image coordinator tests plus the bounded-filesystem fail-closed probe; 68 contract/fuzz tests; lint; both production web builds; and both production dependency audits with zero known vulnerabilities. [Exact-main CI](https://github.com/bobofbuilding/treeswap/actions/runs/32721978252) passed all four jobs. Production deployment record `6061734414` binds that source to the successful target [treeswap-2bmjb1r5v-bittrees-tech.vercel.app](https://treeswap-2bmjb1r5v-bittrees-tech.vercel.app), while [treeswap.vercel.app](https://treeswap.vercel.app) and [treeswap.bittrees.org](https://treeswap.bittrees.org) both return HTTP 200. This checkpoint contains no sealed qualification rerun, deployed public-testnet contracts or multisigs, live provider/operator/signature evidence, independent review, solver campaign, inventory, or funding authority; those remain release blockers.
  - [x] Clean published active-daemon checkpoint [`c62177cce5eeaa3b7bb1fddfe7251ca70ff5f2af`](https://github.com/bobofbuilding/treeswap/commit/c62177cce5eeaa3b7bb1fddfe7251ca70ff5f2af), merged through [PR #57](https://github.com/bobofbuilding/treeswap/pull/57), passed all 41 sealed local campaigns from `2026-08-23T09:30:23.039Z` through `2026-08-23T10:41:26.439Z`; [exact-main CI](https://github.com/bobofbuilding/treeswap/actions/runs/32631046049), production deployment record `6046492157`, and both public aliases passed. Its private mode-`0600` evidence independently reconstructs to `sha256:5e266b9eebe30d93d3b449212124a7e00fc8bc6cf345ebd65938236112bbc262`, including 153 matching configuration hashes, 41 unique passed campaigns, three immutable image pins, exact clean source, privacy exclusions, and empty teardown. The nested `sha256:a36c96a45c4b9c1fecf78db7279bba1f8c1204219d6dd2ff33b799ca20675268` duration record passed 3,604 seconds, 119 observations, a maximum 32-second gap, restart persistence at 1,814 seconds, zero block advances, and zero dispatch. It grants no funding authority and does not supply the remaining deployed independent infrastructure, public testnet, multisig, inventory, or review evidence.
  - [x] Published authenticated-runtime checkpoint `ac08946d962d062845d585a716c829c0ad73a4a0` passed the full matrix, all 26 sealed local qualification campaigns, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32334233367). The qualification ran from `2026-08-20T05:05:56.878Z` through `2026-08-20T06:12:48.897Z` and independently reconstructs to `sha256:dc6f60bacfa32332eb90bba0b75a879e35022f1cb473cba17241f225feb5450d`. It explicitly grants no funding authorization.
  - [x] Published EVM-outbox quorum checkpoint `cd2a81c0f5ecc7ab1902a1fa576d71f4c7520509` passed 226 application/security tests, 74 direct pinned-runtime tests plus the bounded-filesystem rollback campaign, 68 contract tests, both build paths, all 27 sealed local campaigns, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32339918956). The qualification ran from `2026-08-20T06:32:18.096Z` through `2026-08-20T07:39:19.663Z` and independently reconstructs to `sha256:f0e362260e813c3880b4328800a074c8f57df1459ec6b37a045369a718d17e5a`. It explicitly records no independent provider, public testnet, production infrastructure, review, or funding authorization.
  - [ ] Deploy the endpoint, private-packet, and reader protocols with independently operated RPC/Lightning observers; protect provider confidentiality with reviewed network identity and encrypted transport; deploy finality authorizers, the both-assets verifier, bounded daemons, and reviewed persistence; then run deployed-volume recovery plus real multi-instance drills.
- [ ] Keep browser, web server, relay, coordinator, and Lightning credentials in separate trust domains.
  - [x] Repository containers separate the coordinator signing key/database from the adapters' public key and role macaroons; the public web database contains neither.
  - [x] A strict [service-isolation evidence](./SERVICE_ISOLATION_EVIDENCE.md) boundary now requires twelve exact service roles, unique trust-domain/network/deployment commitments, exact public ingress, encrypted transport, least-privilege credential classes, unique credential sets, bounded rotation windows, and distinct infrastructure, Lightning, and security signatures. Operational-readiness v4 maps the two exact confirmer routes to the isolated asset-verifier and EVM-finality-authorizer services, and both release-candidate paths re-verify and bind that live provenance; copied verification or an opaque replacement digest fails closed.
  - [ ] Reproduce that boundary with deployed service identities, networks, secret scopes, and encrypted independently retained backups; measure restore objectives on the deployed volume.
- [ ] Add structured metrics and alerts without logging invoices, preimages, wallet links, email, or unrestricted addresses.
  - [x] The store exposes aggregate settlement, RFQ, firm-offer, capacity-conflict, and suspension counters plus a secret-free event view; live campaigns prove neither the raw invoice nor EVM claim preimage is persisted, and storage tests find no raw RFQ wallet identity. Both bounded audit serializers keep the trusted event label immutable, ignore accessors without invocation, accept only primitive events and values without object coercion, and serialize prototype-named fields as own data without prototype mutation; the text serializer also prevents normalized field collisions from overwriting an earlier field.
  - [x] A bounded local monitor accepts only exact fresh EIP-712 observations from a complete release- and gate-bound v4 policy with two globally distinct collector identities/signers and two distinct operator commitments per safety domain. The same policy pins one quote-closure route, exactly two guardian broadcaster routes, exactly two finalized gate-confirmation routes, and exactly two alert routes under globally distinct route identities; the four broadcaster/confirmer operator commitments are also globally distinct. One missing collector, disagreement, unsafe status, unsigned/copy input, expiry, wrong policy, malformed data, an extra field, copied action plan, or non-canonical callback result fails closed. The monitor emits fixed reason codes, closes quote issuance, attempts both guardian routes independently, requires both read-only confirmation providers to agree on the exact accepted transaction, successful receipt, `Halted` event, finalized block, alert digest, gate address, fully closed state, zero active risk, and no pending reopen, and attempts both alert routes only after confirmation completes. One broadcaster or alert-route failure is tolerated but recorded as degraded; one confirmer failure or disagreement leaves the halt incomplete while alerts still run. A healthy cycle has no reopen authority. Operator commitments, local callbacks, and two reads of one local backend do not prove real organizational independence or delivery.
  - [x] Published safety-monitor checkpoint `4b40a3ca682b63f8d1fec11fa1900448d33676f5` passed 233 application/security tests, 81 direct pinned-runtime tests plus the bounded-filesystem rollback campaign, 68 contract tests, both builds, all 28 sealed local campaigns, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32345936040). The qualification ran from `2026-08-20T07:52:32.228Z` through `2026-08-20T08:59:56.235Z` and independently reconstructs to `sha256:2644372eebed253ae9649c625a770953ed8c90b9d8da7876cb69ec514e265450`. It explicitly records no public testnet, production infrastructure, independent review, or funding authorization.
  - [x] A five-role signed operational-readiness v4 boundary now requires the exact incident set, at least two alert channels, backup/restore, monitoring, reconciliation, privacy retention, service isolation, solver operations, loss allocation, support, and qualification artifacts. It also binds one immutable snapshot of the exact v4 safety-monitor policy, isolated confirmer-service assignments, and explicit finalized-gate controls for the provider-disagreement, provider-outage, and gate-halt drills. Release candidates require that monitor policy to point to the exact signed bootstrap/campaign record, expose that commitment, derive the otherwise-identical final-release monitor policy without a circular hash, reconcile all other commitments against the signed operator roster or campaign, and reject opaque manually supplied support/loss/isolation hashes. See [Operational readiness evidence](./OPERATIONAL_READINESS_EVIDENCE.md).
  - [x] Adoption-policy v1 replaces opaque support/loss/privacy commitments with exact public fees, caps, reserves, solver-liveness rules, retention, loss allocation, public support paths, owners, and immutable-upgrade responses. Every operational signer explicitly signs its digest, and release-candidate v6 requires exact limit and upstream-policy agreement. This validates terms, not real staffing, service availability, retention enforcement, or acceptance by operators. See [Adoption policy boundary](./ADOPTION_POLICY.md).
  - [x] The local action runtime now exercises two policy-bound guardian broadcasters, two policy-bound finalized gate confirmers, and two policy-bound alert routes with independent bounded attempts, failover, explicit degradation counters, canonical secret-free results, and copied-plan rejection. The actual-gate campaign proves closure precedes broadcast, both confirmers reproduce the exact finalized halt before closure is credited, alerts follow confirmation even when confirmation fails, false broadcaster claims cannot establish closure, one failed broadcaster or alert route does not suppress the surviving route, and no monitor path can reopen the gate.
  - [ ] Deploy continuous redundant collectors and scheduler plus genuinely independent guardian broadcasters, finalized-state confirmation providers, and alert providers; prove transaction delivery, finality agreement, paging, escalation, and outage handling against the deployed gate, and retain secret-free incident evidence.

The coordinator state, crash semantics, live evidence, runtime qualification risk, and remaining work are documented in [Durable coordinator boundary](./COORDINATOR.md).

## Gate 4 — permissionless solver testnet

- [ ] Deploy and demonstrate open solver competition on public testnet through multiple independent operators and delivery paths.
  - [x] The closed-deployment workflow deterministically derives an unsigned Sepolia plan from exact reviewed inputs after a clean published-source offline rebuild. It fixes all constructor calldata, CREATE addresses, risk limits, role commitments, and registry calls, and grants no signing, broadcast, gate-opening, or funding authority. The local Anvil rehearsal executes the same generated topology. See [Closed public-testnet deployment plan](./CLOSED_TESTNET_DEPLOYMENT.md).
  - [x] A live preflight observer and EIP-712 quorum boundary now bind two-to-five matching canonical provider snapshots plus an operations reviewer to the exact unsigned plan, deployer nonce, role-wallet state, BIT state, source, input, review digest, and block for no more than fifteen minutes. Anchored/pending nonce movement, provider disagreement, mutation, replay, or expiry fails; the result cannot authorize signing, broadcast, opening, or funding.
  - [x] Finalized deployment observation v2 and the signed promotion boundary now require the vault's available, locked, accounted, and raw BIT balances plus the user escrow's liabilities and raw BIT balance at the same canonical block. All six must reconcile and equal zero; pre-funding, hidden liabilities, malformed values, omission, or provider disagreement fails. `npm run observe:deployment-manifest` captures mode-`0600` evidence without recording provider credentials.
  - [x] An exact candidate-evidence verifier now requires at least two separately identified and signing EVM providers, Lightning observers, monitors, relays, solvers, and alert channels; a seven-day bidirectional campaign; twenty-four mandatory adversarial scenarios; per-solver reliability/halt metrics; a final closed gate; and zero unreconciled liabilities. Its derived output has no funding authority. See [Public-testnet campaign evidence](./PUBLIC_TESTNET_EVIDENCE.md).
  - [x] A guarded operator workflow now initializes an exact policy/participant-bound campaign, adds one scenario or solver metric per immutable mode-`0600` snapshot, verifies hash-linked single-entry transitions, emits independently comparable checkpoints, derives safe test-only features and counts, and finalizes through the same strict record validator. It refuses overwrites, symlinks, secrets/endpoints, evidence substitution, incomplete samples, unsafe metrics, short duration, open gates, and unreconciled liabilities; it neither signs nor proves any external evidence.
  - [x] Two evidence-derived public-testnet release paths remove the bootstrap/campaign circularity. `operator-testnet-bootstrap` permits only operator-owned test inventory under hard 500-sat-per-swap and 10,000-sat-per-day ceilings; `operator-testnet` requires the complete signed seven-day campaign. Preparation re-verifies the full deployment chain, derives all shared fields, binds monitor counts and every external review/operations digest, and emits only non-authorizing mode-`0600` approval material. See [Public-testnet release candidates](./PUBLIC_TESTNET_RELEASE_CANDIDATES.md).
  - [x] Release-candidate preparation now reads both public calls and their templates only as exact plain own-enumerable data records, then retains bounded exact-data snapshots of the record/policy templates, deployment manifest, and operational safety-monitor policy before candidate hashing and runtime binding. Accessors are not invoked; coercion, later mutation, hidden/symbol/inherited/prototype-named fields, custom prototypes, unsafe or executable values, cycles, oversized structures, and sparse or decorated arrays reject. This prevents a review-to-hash/runtime substitution but does not attest deployed state, evidence truth, custody, or independence.
  - [x] Bootstrap release preparation now rejects public permissionless execution. Permissionless solver execution is reachable only through the campaign-qualified mode, whose exact adoption policy must match all release caps and signed admission/risk/fee commitments.
  - [x] Tiny-limit bootstrap counts are no longer accepted from an unsigned evidence file. A short-lived v2 roster requires every EVM provider, Lightning observer, monitor, relay, and solver to sign the same source-, deployment-, artifact-, feature-, and time-bound record; derives counts from globally distinct operator identities and signers; requires the EVM providers to match the signed deployment promotion exactly; and binds the record, policy, participant set, and attestation set into release evidence. It proves signatures, not organizational independence or artifact truth. See [Bootstrap operator evidence](./PUBLIC_TESTNET_BOOTSTRAP_EVIDENCE.md).
  - [x] A guarded release ceremony derives the exact policy-pinned payload for each of five roles, rejects a controller/guardian signer-kind downgrade, binds one exact approval bundle to the candidate, rechecks both contract-wallet signatures and runtime hashes through the candidate's exact canonical finalized provider quorum, and writes a private receipt without signatures, URLs, or activation provenance. Candidate, approval-bundle, and provider-configuration data are boundedly snapshotted once through exact own-enumerable data schemas before validation; accessors, symbols, hidden/inherited/prototype-named fields, custom prototypes, object coercion, and sparse/decorated arrays reject without execution. Consistency review and role payload preparation use the same frozen candidate, so caller mutation cannot change the reviewed signing artifact. See [Public-testnet release approvals](./PUBLIC_TESTNET_RELEASE_APPROVALS.md).
  - [x] The same-process activation path rejects a signed release plus caller-manufactured runtime JSON. It requires live candidate and provider provenance, rechecks the five-role bundle, verifies fresh Lightning-operator/security-reviewer reconciliation signatures, and obtains exact provider agreement on the current release-bound gate, registry, escrow, BIT implementation, accounting, reserves, and caps. Capability and snapshot copies cannot authorize. See [Public-testnet release activation](./PUBLIC_TESTNET_RELEASE_ACTIVATION.md).
  - [x] The activation preflight now consumes one exact manifest of distinct raw upstream files for either the tiny bootstrap or campaign-qualified release, rejects mixed evidence shapes, reconstructs the candidate and every module-private proof in one process, rebuilds the candidate-bound provider set from secret environment variables, and calls the live activation boundary. It writes only a private, non-overwriting, secret-free digest summary with signing, broadcast, gate-opening, dispatch, and funding authorization all false; it accepts no solver input and destroys the transient capability on exit. Malformed manifests, serialized-candidate substitution, duplicate paths, copied runtime state, stale signatures, provider disagreement, and unsafe live balances fail closed. This does not deploy or replace the persistent production coordinator.
  - [x] The packaged coordinator now has a non-default persistent release-verification mode that repeatedly reconstructs those raw inputs and retains only the original in-memory activation. Every refresh revokes its predecessor first; failure, expiry, clock rollback, shutdown, restart, or replacement immediately closes activation-derived new exposure. Health remains secret-free and explicitly non-authorizing, becomes unhealthy while verification is inactive, and cannot restore provenance from status. This mode has no listener, solver input, context creation, signing, broadcast, dispatch, gate-opening, or funding path; the full funded daemon integration and deployed infrastructure remain open.
  - [x] Clean published persistent-verifier source [`634e1c232728c90c0916cf00605e76d5ea4ad5f1`](https://github.com/bobofbuilding/treeswap/commit/634e1c232728c90c0916cf00605e76d5ea4ad5f1), merged through [PR #64](https://github.com/bobofbuilding/treeswap/pull/64), passed 500 application/security tests with 499 passes, zero failures, and one intentional published-main-only skip; 118 exact-image coordinator security tests plus disk-full and default-closed probes; 68 contract/fuzz tests; both production web builds; lint; and production dependency audits. [Exact-main CI](https://github.com/bobofbuilding/treeswap/actions/runs/32719312065) passed all four jobs. Production deployment record `6061272922` binds that exact commit to a successful Vercel deployment, and [treeswap.vercel.app](https://treeswap.vercel.app) and [treeswap.bittrees.org](https://treeswap.bittrees.org) both return HTTP 200. This checkpoint contains no sealed qualification rerun, deployed testnet contracts or multisigs, live provider/operator/signature evidence, independent review, solver campaign, inventory, or funding authority; those remain release blockers.
  - [x] Clean published activation-preflight source [`f13828af9dc4547d5d7fc5cf16b1b01c420e677d`](https://github.com/bobofbuilding/treeswap/commit/f13828af9dc4547d5d7fc5cf16b1b01c420e677d), merged through [PR #62](https://github.com/bobofbuilding/treeswap/pull/62), passed 493 application/security tests with one intentional deployment-only skip, both production web builds, 68 contract tests, the pinned coordinator fault/recovery and deployment-rehearsal jobs, and [exact-main CI](https://github.com/bobofbuilding/treeswap/actions/runs/32714104657). GitHub production deployment record `6060389743` binds the exact commit to a successful Vercel production deployment; [treeswap.vercel.app](https://treeswap.vercel.app) and [treeswap.bittrees.org](https://treeswap.bittrees.org) both return HTTP 200. This checkpoint contains no sealed qualification rerun, public-testnet deployment, real provider or signer evidence, persistent funded coordinator, independent review, inventory, or funding authorization; those remain release blockers.
  - [x] The final solver-funding predicate now rejects caller-supplied authentication/capability booleans and requires the original locally verified solver proof. It binds that proof to the active release chain and direction-specific escrow address/runtime hash, requires independently observed capacity to remain fresh, and fails at the exact capability expiry; copied, cross-chain, cross-escrow, stale, and expired proofs reject. The packaged coordinator remains closed and supplies no funding authority.
  - [x] Clean published solver-capability checkpoint [`a70cf867f5ca5a71b3056b8828a89c5fff57706e`](https://github.com/bobofbuilding/treeswap/commit/a70cf867f5ca5a71b3056b8828a89c5fff57706e), merged through [PR #52](https://github.com/bobofbuilding/treeswap/pull/52), passed 479 application/security tests with no skips, both production web builds, 68 contract tests, 105 pinned-runtime checks plus bounded-filesystem rollback, and all 41 sealed local campaigns from `2026-08-23T06:01:33.567Z` through `2026-08-23T07:12:20.217Z`; [exact-main CI](https://github.com/bobofbuilding/treeswap/actions/runs/32621702921) passed. GitHub production deployment record `6044951328` binds the exact ready Vercel deployment [`dpl_9YxgaX6PfFmAXmoXupWEWWLtadbX`](https://treeswap-ac6f9zrtg-bittrees-tech.vercel.app), and both official aliases return HTTP 200. The ignored regular mode-`0600` evidence independently reconstructs to `sha256:e5251543e191c68d45805a3ebd96a74b83b6ffe3fb7825de577aadc51024026e`; all 151 configuration hashes, 41 unique passed campaigns, three immutable image pins, exact clean published-`main` binding, privacy exclusions, and empty lab teardown reproduce. Its 3,603-second Lightning guard recorded 119 observations, a 32-second maximum gap, midpoint restart after 1,813 seconds, deterministic stale-chain rejection, zero block advances, and zero dispatch. This is local-only closed-coordinator evidence with a simulated EVM reservation; it is not public-testnet or production infrastructure, independent operators/providers/review, deployed multisigs, inventory, or funding authorization.
  - [x] Clean published evidence-boundary checkpoint `e662885346791b80153835c3123f0f77b63ad9f5` passed 263 application/security tests, 91 direct pinned-runtime tests plus bounded-filesystem rollback, 68 contract tests, both builds, and all 32 sealed local campaigns from `2026-08-20T16:49:58.056Z` through `2026-08-20T17:57:51.589Z`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32394191747) passed the same source. The ignored mode-`0600` record independently reconstructs to `sha256:de647c4a2b35e411bed48a2062e2964c008702f6f17715072b5d9c9388575e6d` and explicitly excludes real public-testnet operators, production infrastructure, independent review, and funding authorization.
  - [x] Repository admission has no solver allowlist: any solver may publish an indicative offer, while only a fully verified EVM, endpoint, LND-node, escrow-runtime, and independently observed capacity declaration can produce an executable offer.
  - [x] An executable offer binds the exact capability and capacity-snapshot digests, observed inventory, per-solver capacity epoch, endpoint key, quote expiry, and direction-specific settlement runtime code hash. An indicative book cannot authorize invoice payment or settlement.
  - [x] Lightning → BIT is admitted only against exact, independently verified, already deposited solver-owned BIT inventory plus sufficient inbound Lightning capacity.
  - [x] Unknown BIT → Lightning solvers receive the configured first-fill cap, while an atomic global in-flight ceiling spans all identities to contain Sybil multiplication. The higher established cap requires objective completed BIT → Lightning history backed by an exact selected-offer and both-assets terminal proof; Lightning → BIT fills cannot promote, improve reliability, or reset failures in the riskier direction, and there is no administrator promotion flag.
  - [x] The first accepted RFQ permanently binds the normalized admission-policy digest in coordinator metadata; every later admission, reservation, and outcome fails closed if caps, quotas, or promotion/failure thresholds drift.
  - [x] Clients can verify multiple blind solver signatures locally, retain one offer per solver, commit the complete received set, privately finalize only the selected solver's matching executable quote, and require new disclosure and authorization for fallback.
  - [x] Before disclosure, the exact authenticated selection, original verified capability, reviewed market-risk policy, and first exact EIP-712 user authorization must atomically reserve the active schema-v9 firm record. Lightning → BIT commits gross BIT plus inbound Lightning; BIT → Lightning commits output plus maximum routing headroom. Active RFQ, capacity, authorization, risk evidence, and firm records are revalidated at disclosure and finalization, then one exact private-request/executable-quote pair is durably bound. A second user signature over the complete executable quote and exact invoice/hash is persisted before that result can become executable. Copies, wrong wallets, cancellation, expiry, stale or mutated records, fake stores, method replacement, second invoices, and competing connections fail closed.
  - [x] Direction-specific invoice privacy is enforced: the BIT → Lightning user invoice and the Lightning → BIT selected-solver hold invoice enter only the selected private finalization, never public quote delivery.
  - [x] A short-lived direction-specific declaration binds the EVM solver, escrow domain and runtime code, LND node public key, canonical HTTPS origin, Ed25519 endpoint key, exact capacities, monotonic epoch, and expiry. Local verification requires all three possession proofs plus fresh independent capacity observations. The official operator client fixes the public Node HTTPS transport, system clock, cryptographic challenge source, and exact LND default-double-hash zbase32 compact-signature recovery. Its required production Lightning-capacity reader independently fixes the private port-443 Node HTTPS `/v1/capacity` transport, system clock, cryptographic entropy, bounded non-cacheable complete response, and separate requester/observer keys. Test-injected endpoint or capacity transport, clock, entropy, or verifier clients cannot enter active or recovery composition. The live regtest campaign cross-checks four exact recovered node keys against a separately credentialed LND verifier and rejects mutation/cross-role use. This proves possession, not deployed node activity or liquidity.
  - [x] The local request/response transport authenticates the bound endpoint on a fresh challenge and fails closed on response mutation, stale authority, origin/identity mismatch, redirect, malformed input, timeout, capacity overstatement, and SSRF targets.
  - [x] The local capacity-reader campaign proves direction binding, dual-provider finalized vault agreement, exact runtime/proxy/implementation hashes, vault solvency, reserves and budgets, response/request replay rejection, separate requester/observer/endpoint keys, aggregate-only Lightning output, and cross-role denial. Production capacity transport additionally rejects callback/clock/entropy injection, plaintext/public/credential-bearing/path-bearing/query-bearing/nonstandard-port origins, redirects, caching, compression, ambiguous lengths, oversized or incomplete bodies, and disabled TLS verification. The Compose live-LND smoke remains explicitly test-only HTTP inside the disposable network.
  - [x] Clean published permissionless-admission checkpoint `430a829bed99e88596d27326aecb6e3aca21a35a` passed 246 application/security tests, 91 direct pinned-runtime tests plus bounded-filesystem rollback, 68 contract tests, both builds, and all 30 sealed local campaigns from `2026-08-20T13:46:25.789Z` through `2026-08-20T14:54:24.378Z`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32375971751) passed the same source. The ignored mode-`0600` evidence independently reconstructs to `sha256:acd75e9bd650883329ffac589e802c6c429d6afecab96e63d80bd220ec9e64c3`. It records local-only scope, a simulated EVM reservation, no public testnet, independent providers, production multisigs or infrastructure, independent review, or funding authorization.
  - [ ] Deploy the authenticated endpoint and reader protocols, then prove all repository bindings and risk caps across at least two independently operated testnet solvers, observers, and relays.
- [ ] Run two or more independently operated solvers through adversarial churn, withholding, relay censorship, restarts, and insolvency simulations.
  - [x] The repository workflow makes every required scenario non-removable, requires two known observers per scenario, and binds each retained evidence digest into a hash-linked campaign history. The real seven-day execution and observer independence remain external.
- [ ] Publish fill rate, timeout rate, median completion, capacity freshness, and halt history per solver without claiming a globally best price.
  - [x] The verified adoption summary derives those rates from reconciled integer outcomes, publishes only solver digests and aggregate metrics, and omits participant signers, endpoints, invoices, payment hashes, and preimages. No real campaign summary exists yet.

## Gate 5 — deploy governance and operations

- [ ] Deploy distinct 2-of-3-or-stronger controller, guardian, and fee-recipient contract wallets with hardware-backed owners and tested recovery.
  - [x] Repository authorization now requires five distinct policy-pinned approval identities over one exact EIP-712 release record and policy digest. Controller and guardian ERC-1271 approvals must match reviewed runtime hashes at the record's exact canonical finalized block through its exact provider-set digest; number, hash, timestamp, and provider count must agree. The Lightning operator, security reviewer, and incident commander sign the same record directly.
  - [x] Release v2 requires independent monitor counts, loss allocation, support policy, finding disposition, and five report digests derived from a fresh five-reviewer signed evidence package for either funded testnet mode. Only the explicitly tiny bootstrap mode may omit the not-yet-created campaign commitment; the ordinary operator-testnet mode cannot.
  - [x] Prepared release-candidate v6 carries the exact adoption-policy digest, independently reviewed qualification artifact, monitor upstream-record digest, safety-monitor-policy digest, and isolated gate-confirmer-binding digest into the five-role approval boundary; derives the final-release monitor policy from same-process provenance; and rejects legacy candidates v4/v5. The policy fixes the directionally higher BIT → Lightning base fee, no-bond objective-history admission, no last-look/partial fills, published retention/linkage, explicit no-insurance loss allocation, named support/incident owners, and fail-closed immutable upgrades.
  - [x] The operator ceremony now prepares each role's exact signing payload without accessing a key and verifies the collected five-role bundle against the exact release candidate and live finalized provider set. Every data input is snapshotted once before review, and public argument records are exact; getter execution, hidden/symbol/prototype fields, object coercion, post-review mutation, signer substitution, replay, provider disagreement, stale authorization, output overwrite, and receipt reuse for activation fail closed.
  - [x] Only a module-proven verified release can derive an operator-funding capability, and only the capability/runtime-snapshot pair created by the same uninterrupted live activation can authorize it. Arbitrary feature flags, nominal audit/test booleans, copied verification or runtime objects, wrong manifests, stale observations, expired reconciliation, closed gates, provider disagreement, implementation changes, or unreconciled balances fail closed. Release v2 rejects mainnet entirely; a later schema must require prior qualified public-testnet evidence, an equivalent mainnet deployment ceremony, every review and operations digest, exact caps, reserves, counts, and finding disposition.
  - [x] Clean published signed-release checkpoint `2e2917389b6191a4c62bdbe56f6bab9904141406` passed 252 application/security tests, 91 direct pinned-runtime tests plus bounded-filesystem rollback, 68 contract tests, both builds, and all 31 sealed local campaigns from `2026-08-20T15:15:57.828Z` through `2026-08-20T16:34:26.514Z`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32384877772) passed the same source. The ignored mode-`0600` evidence independently reconstructs to `sha256:22011a8ab9c608dbbac34cc88b09074d9f4a562626666d33e208ae22922a6fa3`. It records a simulated EVM reservation and no public testnet, production infrastructure, independent review, or funding authorization.
  - [ ] Have all five real roles independently reproduce the candidate from original evidence, compare both digests out of band, sign with deployed Safe contracts and hardware-backed identities, and retain the original signatures plus the private verification receipt. Deploy independent Lightning-operator and security-reviewer reconciliation signers plus the persistent production coordinator; re-run candidate, approval, reconciliation, and live-state verification without restarting before activation. Provider labels, distinct URLs, repository signatures, or the receipt alone do not establish upstream provenance or operational independence.
- [ ] Deploy the reviewed versioned contracts closed, seal the payment-hash registry, and reproduce every immutable and runtime code hash.
  - [x] Exact unsigned Sepolia deployment preparation and independent reconstruction are implemented. Any source, build, input, transaction, action-order, or postcondition mutation fails; only a clean commit already published on `origin/main` can be used.
  - [x] Exact live preflight collection and signed quorum verification are implemented. The repository can verify separately signed agreement, but current provider labels and local signatures are not evidence of real provider independence, production Safes, or hardware custody.
  - [x] A local finalized RPC rehearsal deploys the actual gate, registry, and both escrows closed; reconstructs exact role owners/thresholds, runtime hashes, BIT implementation slot, escrow bindings/limits, and registry set through two distinct local identities; and rejects a captured quorum plus production promotion. Its deterministic digest is `0xcab23fa2503054e2bc95c25238ac153f83f44f4f38b17cb316359972a4deef2a`.
  - [x] The rehearsal is included in the 29-campaign sealed checkpoint bound to clean published commit `44d929e708768d8bbe53087b415eda0f4ac75f43`; the local evidence independently reconstructs to `sha256:9a0bb29bc90d603327b56606603489247f2b3cab5f3be5ecad18d2cd8417d5e9`.
  - [ ] Repeat with production Safe contracts and hardware-backed owners on public testnet, two independently operated providers, the reviewed BIT deployment, and an independently signed release manifest.
- [ ] Monitor the external BIT proxy implementation slot, BIT pause/decimals, EVM finality, price sources, solver capacity, LND health, and asset reconciliation continuously.
  - [x] The repository monitor requires two authenticated observations under distinct operator commitments for all eight domains on every bounded cycle. One missing collector, disagreement, stale, expired, duplicate, malformed, future, unsigned, wrong-policy, or unsafe observation takes the halt path. Signatures authenticate the configured claim but do not prove collector honesty, upstream correctness, or organizational independence.
  - [x] The repository action boundary binds the exact release/gate policy to two guardian broadcaster routes, two finalized gate-confirmation routes, and two alert routes; attempts every route independently; accepts one broadcaster or alert outage with explicit degradation; rejects copied or substituted action plans and broadcaster-only closure claims; and treats any confirmation outage, disagreement, or non-closed state as an incomplete halt while alerts still run.
  - [ ] Deploy independently sourced, continuously scheduled, supervised redundant monitor instances and prove monitor-outage escalation.
- [ ] Prove an alert blocks new exposure while every existing claim, refund, and withdrawal remains available.
  - [x] Local combined evidence opens the actual gate, closes quotes, attempts two guardian routes with one intentional outage, observes the surviving guardian halt close the gate, then attempts two alert routes with one intentional outage. It retains the contract-suite guarantees that halts are absent from claim, refund, and withdrawal paths.
  - [ ] Repeat through production-like multisigs, external paging, redundant transaction delivery, deployed escrows, and outstanding testnet liabilities.
- [ ] Run every incident in the incident runbook and attach the evidence to the release record.
  - [x] The repository now requires every exact drill, bounded times, distinct evidence, one primary operator, two retained observers, five operational-role signatures, and exact campaign evidence agreement before candidate preparation. This validates structure and signatures only; the deployed drills and retained external artifacts are still required.
  - [x] The public-testnet v2 release schema has mandatory incident-drill, monitoring, backup/restore, provider, solver, loss-allocation, support, finding-disposition, postflight, and promotion digest slots. It rejects every mainnet environment and funding mode until a later schema can require an equivalent mainnet deployment ceremony and reviews.

## Gate 6 — independent review

- [ ] Contract review: both escrows, registry, gate, signatures, accounting, boundaries, and deployment reproducibility.
- [ ] Lightning review: invoice validation, HTLC timing, adapter permissions, idempotency, restarts, and force-close behavior.
- [ ] Coordinator review: persistence, concurrency, quote fairness, replay resistance, and recovery.
- [ ] Identity/privacy review: SIWE, optional wallet linking, retention, logs, and cross-network correlation.
- [ ] Operational review: multisigs, secrets, monitoring, reconciliation, incident command, and loss allocation.
- [ ] Pin every report digest and close or explicitly accept every finding before opening the release gate.
  - [x] The local [independent-review verifier](./INDEPENDENT_REVIEW_EVIDENCE.md) now requires distinct contract, Lightning, coordinator, identity/privacy, and operations reviewers to sign one exact source-, protocol-, deployment-, report-, finding-, and time-bound package. Finding counts must reconcile, open findings and accepted critical/high risks fail closed, review signers cannot overlap deployment wallets/owners, promotion attesters, bootstrap/campaign operators, or release approvers, and both release-candidate modes derive review hashes only from live verifier provenance. Candidate v1 and release-record template v1 are rejected. No real reviewer, report, disposition, or signature has yet been supplied.

## Gate 7 — capped mainnet beta

- [ ] Publish exact contract addresses, source commit, review digests, fee schedule, price band, per-swap and per-epoch caps, daily Lightning cap, in-flight cap, reserve floor, and support path.
- [ ] Start with operator-owned solver inventory only, at substantially smaller limits than the tested maximum.
- [ ] Require explicit user confirmation for the exact quote and, when paying Lightning, the exact invoice.
  - [x] The repository now enforces two short-lived EIP-712 confirmations: selection authorization before capacity reservation/private disclosure, then execution authorization after the solver's full quote binds the exact payment hash and invoice digest. Both digests and signed expiries persist atomically in coordinator schema v9, and use fails at the exact expiry boundary; copied objects, wrong signers, changed recipients, invoices, hashes, amounts, fees, routing caps, solvers, received sets, requests, or expiries fail closed.
  - [x] The browser-safe wallet ceremony uses those canonical fields and independently expected digest, renders both effects and exact terms, then signs only the original verified prompt. It refuses implicit connection or chain switching, wrong/changing chain or account, mutation, prompt copies, concurrent use, malformed/wrong signatures, incomplete invoice commitments, and expiry before or after the wallet returns. The prototype previews both steps while explicitly performing no signature or value-moving action.
  - [x] Clean published checkpoint [`2a39f11b9e4f7b157bb72cc15946cb8f1f91757a`](https://github.com/bobofbuilding/treeswap/commit/2a39f11b9e4f7b157bb72cc15946cb8f1f91757a), merged through [PR #45](https://github.com/bobofbuilding/treeswap/pull/45), passed 467 application/security tests plus one intentional deployment-only skip, both production web builds, 68 contract tests, 100 pinned-runtime coordinator checks, and 41 sealed local campaigns; [hosted main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32606507409) and exact commit-bound production deployment [`dpl_HAcE5xanE1vJwSmaDPceHXmjixSK`](https://treeswap-e4h9ogys3-bittrees-tech.vercel.app) passed. The generated deployment URL is authentication-protected, while both official aliases serve the commit with HTTP 200. The ignored mode-`0600` evidence independently reconstructs to `sha256:1e2e0d70005646472a92660e8f6fbcf64c4b7785f6e59527624f749f32f78976`; all 145 configuration hashes, 41 unique campaigns, three immutable image pins, source binding, privacy exclusions, the enforced one-hour Lightning guard with midpoint restart, and empty teardown reproduce. This is local-only evidence with a simulated EVM reservation and no deployed testnet signing journey, independent operator, public testnet, production infrastructure, multisig, inventory, external review, or funding authority.
  - [x] Clean published checkpoint [`3d9f75a13d27796a465c4cd2ca38a48d9e8bafa9`](https://github.com/bobofbuilding/treeswap/commit/3d9f75a13d27796a465c4cd2ca38a48d9e8bafa9), merged through [PR #43](https://github.com/bobofbuilding/treeswap/pull/43), passed 462 application/security tests plus one intentional deployment-only skip, both production web builds, 68 contract tests, 100 pinned-runtime coordinator checks including bounded-filesystem rollback, and all 41 sealed local campaigns; [hosted main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32601849688) and exact commit-verified production deployment [`dpl_2bERrLK9Gjc3HhUaH1xbevnExR4N`](https://treeswap-5qvo5qhig-bittrees-tech.vercel.app) passed. The ignored mode-`0600` evidence independently reconstructs to `sha256:560694e844a073d9c391f750a73e95646a43d0796f921ea0af68df794c662a9a`; all 145 configuration hashes, 41 unique campaign results, three immutable image pins, source binding, privacy exclusions, one-hour Lightning guard, and teardown reproduce. This is local-only evidence with a simulated EVM reservation and no deployed wallet ceremony, independent operator, public testnet, production infrastructure, multisig, inventory, external review, or funding authority.
  - [ ] Integrate the two readable signing prompts into the deployed testnet wallet flow and retain user-journey evidence from both directions; repository verification alone is not deployed confirmation evidence.
- [ ] Reconcile continuously and close automatically on stale data, disagreement, unexpected code, role loss, or any inventory mismatch.
- [ ] Increase caps only through a new signed release record supported by observed reliability and incident-free operation.

## Separate future gate — public liquidity

Third-party deposits, shares, withdrawal queues, yield, and rewards are a new custody and economic protocol. They require new contracts and independent insolvency, accounting, adverse-selection, governance, and legal review. They are not required for a permissionless solver market.
