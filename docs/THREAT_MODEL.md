# TreeSwap adversarial review

Status: the repository hardening pass covers every listed finding, but funded operation remains blocked by the external evidence in the launch checklist. This is not a smart-contract audit, Lightning implementation audit, or assurance that the system is safe for real funds.

Reviewed surfaces:

- signed intents and solver quotes;
- short-lived RFQs and selected signed solver quotes;
- BIT escrow and Ethereum finality;
- BOLT 11 and Lightning hold-invoice settlement;
- fee and decimal accounting;
- segregated solver-owned BIT and Lightning inventory;
- coordinators, governance, keys, and operational recovery.

## Resolution ledger

“Repository resolved” means the control and local evidence exist. “External gate” means the repository fails closed but funded operation still needs deployed infrastructure, fork/regtest evidence, live values, or independent review.

| Finding | Repository disposition | Remaining external gate |
| --- | --- | --- |
| TS-C01 | Risk, price, inventory, cap, fee, attestation policy, and local actual-gate monitor halt implemented | Live pinned BIT state, independent executable-price inputs, continuous monitor/alerts, review |
| TS-C02 | Both beneficiary-bound escrows implemented | Independent review |
| TS-C03 | Ordered-deadline derivation, exact boundaries, integrated local EVM/LND execution, and credentialed pinned-live-BIT combined fork implemented | Public testnet, independent finality, congestion drills |
| TS-C04 | Multi-solver signed selection plus authenticated two-relay/two-direct-path collection implemented | Independent relay/solver deployment and suppression telemetry; global-best availability is explicitly not claimed |
| TS-H01 | Chain/contract/version/direction/nonce replay protection and ERC-1271 implemented | EOA-only SIWE remains an explicit account limitation |
| TS-H02 | Dual-signed user exercise plus local atomic admission, capability expiry, authenticated endpoint response, capacity, and last-look accounting implemented | Deployed shared enforcement/readers, live reliability telemetry, objective bond decision |
| TS-H03 | Closed expiring gate, token runtime checks, local guardian-halt monitor, finalized closed-deployment observer, signed reviewed-manifest promotion, and short-lived signed deployment preflight implemented | Live continuous proxy monitor, genuinely independent observations/reviews, deployed multisigs, public-testnet campaign |
| TS-H04 | Integer units, rounding, overflow, dust, and conservation implemented | None |
| TS-H05 | BIT-only signed capped fee accounting implemented | Pin deployed collector and caps |
| TS-H06 | Public pool/shares/yield/rewards/partial fills disabled | Live solver reconciliation and failure drills |
| TS-H07 | Least-privilege isolated regtest adapters, signed actions, fixed private-HTTPS coordinator transport, solver-node possession proof, durable replay and chain-progress state, stateless fail-closed initialization, production-duration timing, credential and real TLS rotation/rollback, lost-response coordinator recovery, and secret-free audit implemented | Deployed TLS identity/trust root, isolation/monitoring, independent review |
| TS-H08 | Full-fill invoice policy, sealed shared hash registry, and live standard/hold LND regtest integration implemented | Deployed adapter isolation and independent review |
| TS-H09 | Finality authorization, dispatch-time revalidation, exact EVM claim outbox, mandatory two-provider read-only agreement, local finality/reorg/replacement/rotation campaigns, and the credentialed pinned-live-BIT combined fork implemented | Public-testnet repetition through independent providers |
| TS-M01 | Maker rewards excluded from v1 | None |
| TS-M02 | Fill/reward incentives excluded from v1 | None |
| TS-M03 | RFQ quotas, cancellation, work bounds, and local atomic capacity admission implemented | Deployed distributed enforcement |
| TS-M04 | Signed routing cap, short-lived three-key solver binding, authenticated endpoint challenge/response, fixed private-HTTPS capacity reader, and fresh independently observed directional capacity boundary implemented | Deployed endpoint/readers with reviewed service identity, reconciliation, and routing telemetry |
| TS-M05 | Preimage relays cannot redirect either payout | Independent review |
| TS-M06 | Independent bounded raw BOLT 11 decode, field/digest/payee validation, and isolated live standard/hold LND integration implemented | Deployed composition and independent review |
| TS-M07 | Canonical identifiers, text-only rendering, safe logs, and response headers implemented | Production header/log verification |
| TS-M08 | Blind pricing, selected-solver disclosure, redaction, and deletion policy implemented | Storage-layer deletion evidence |
| TS-M09 | Immutable escrows, constrained gate, sealed registry, strict deployment policy, finalized topology observer, provider/reviewer-signed manifest promotion, three-role signed service-isolation evidence, five-role signed release authorization, same-process two-party-reconciled live activation, provenance-bound restart recovery, liability-bound old-release custody/rotation verification, and policy-bound two-route guardian/finalized-confirmation/alert failover implemented | Deployed role-separated services and multisigs, scoped credentials and hardware owners, independently operated providers/reviewers/runtime attesters/guardian broadcasters/gate confirmers/alert providers, real retained packages and old/new host drills, persistent activation/recovery coordinator, continuous event monitoring, and verified external transaction, finality, and alert delivery |
| TS-M10 | Strict EOA SIWE and short rotated sessions implemented | Durable atomic production account storage, or disable accounts |
| TS-M11 | Email delivery hard-disabled; pending data expires in 24 hours | None for swaps; a new reviewed release is required to send mail |
| TS-M12 | Frozen one-shot wallet dispatch and unknown-outcome handling implemented | Trusted wallet confirmation remains the user security boundary |

The quote-selection handoff no longer ends in an unclaimable route-local object. One factory-created, lifecycle-matched reservation service receives the original verified selection and exact original solver capability without serializing either. The public route returns only a fresh bearer token. Preparing signable terms is stateless, so a stolen token cannot lock a malicious beneficiary or request into the ceremony; only the exact wallet's EIP-712 signature can record capacity, admit the RFQ, and reserve the selected firm offer. Separate private-ceremony routes take one-use ownership of preparation/confirmation and selected-solver finalization/authorization, block retained lifecycle and method copies, accept tokens only in strict bounded JSON bodies, forbid header credentials, and return generic non-cacheable responses with identifier-free status. The token can still reveal selected signable terms or invoke its already selected solver if stolen, so private TLS, memory-only browser custody, log exclusion, short expiry, and deployed leakage tests remain mandatory. Exact retry rechecks the active durable binding. One hashed in-memory pending index prevents concurrent calls for the same token; a route timeout returns generic `425` without canceling or duplicating the owned solver attempt, and a later retry receives its verified result. Deployment shutdown revokes the finalization lease before a delayed response can bind. Copied provenance, accessor/coercion input, method replacement, request mutation, wrong signers, expiry, stop, and restart fail closed. Restart never reconstructs module-private authority or lets an old token reach the solver; an already-written firm offer remains a bounded capacity liability until reviewed recovery or expiry. The routes still have no network listeners, wallet signer, Lightning credential, funding key, or settlement dispatcher, so funded operation remains closed.

The authoritative phase gates are in [Launch checklist](./LAUNCH_CHECKLIST.md). Today only the public-prototype phase passes.

Funding cannot be enabled by a configuration boolean. The local release boundary binds the deployment, policies, evidence, reviews, independent-operator counts including monitors, limits, reserves, excluded features, and one exact public adoption-policy digest into one expiring record. Controller and guardian ERC-1271 approvals are checked at the same canonical finalized block through a configured provider quorum and against reviewed wallet code hashes; three separate operating/review roles sign the same EIP-712 digest. The production funding path must then rebuild the provenance-bound candidate and provider set in one uninterrupted process, re-verify all five approvals, verify a fresh reconciliation signed by the Lightning operator and security reviewer, and obtain exact provider agreement on the current canonical gate, registry, escrow, BIT implementation, pause state, accounting, reserves, and caps. The resulting capability and runtime snapshot are a module-private pair; copies, manufactured JSON, restart, reconciliation expiry, or provider disagreement cannot authorize. The final solver decision also requires the original module-private capability verifier result—not caller-supplied authentication or verification flags—and binds its fresh capacity, expiry, chain, escrow, direction, and runtime code to that active release. A separately named bootstrap mode is hard-capped at 500 sats per swap and 10,000 sats per day, uses operator inventory, and cannot enable public permissionless execution; ordinary operator-testnet funding requires the signed seven-day campaign. Both require exact directional fees, liveness limits, loss allocation, privacy retention, support paths/owners, immutable upgrade response, findings, operating evidence, and five report digests. This prevents self-asserted release activation but does not prove provider or runtime-attester independence, hardware custody, evidence quality, service availability, or reviewer independence; those remain external gates. See [Adoption policy boundary](./ADOPTION_POLICY.md) and [Public-testnet release activation](./PUBLIC_TESTNET_RELEASE_ACTIVATION.md).

Bootstrap operator counts are no longer self-asserted inputs. The v2 bootstrap-evidence boundary derives them from a canonical roster with globally distinct operator IDs, signers, and retained-evidence commitments; requires every provider, Lightning observer, monitor, relay, and solver to sign the complete source-, deployment-, feature-, artifact-, and time-bound record; and requires the EVM providers to match the signed deployment promotion exactly. Copied verification, missing participants, shared identities, provider substitution, unsafe features, stale evidence, and signature replay fail closed. Multiple keys still do not prove multiple organizations or truthful artifacts, so organizational independence and evidence inspection remain external gates. See [Public-testnet bootstrap operator evidence](./PUBLIC_TESTNET_BOOTSTRAP_EVIDENCE.md).

The deployment-promotion boundary separately prevents an unreviewed RPC observation from being relabeled as a manifest. It rechecks the strict deployment policy and every matching canonical finalized observation, binds the review bundle and provider evidence, and requires all provider operators plus separate contract and operations reviewers to approve the same EIP-712 record and policy. Its provenance-only mapper returns candidate evidence and cannot authorize funding. Signatures still cannot establish organizational independence or artifact truth. See [Signed deployment-manifest promotion](./DEPLOYMENT_PROMOTION.md).

The earlier live-BIT handoff now has its own short-lived provider accountability boundary. A deterministic preflight first checks both provider/reviewer policies, artifact and finding sets, global control separation, source binding, and a configured effective review ceiling of at least five minutes before any live evidence expires; live preparation separately enforces the actual time remaining, and the preflight's digest-only output has no provenance or authority. Two distinct provider signers then approve the exact raw observations, eligible comparison, canonical finalized block, published source, and policy; the policy also requires distinct organization, retained identity-evidence, and retained service-control commitments. A second in-process boundary refuses a copied provider summary and requires separate contract-security and provider-independence reviewers to approve the same signed provider record plus exact proxy/implementation source, compiler input, roles/storage, upgrade, organizational-control, findings, and disposition commitments. Reviewer and provider control may not overlap, and critical, high, or open findings fail closed. A third provenance-only boundary links both live verifications and derives the exact reviewed BIT token-deployment manifest; serialized or cloned verification objects cannot be promoted. Mutation, cross-bundle substitution, replay, expiry, copied signers, stale source, endpoint/signature material, and output overwrite fail closed. Every layer reports `fundingAuthorization: false`, and the manifest preserves `providerIndependenceStatus: reviewer-attested-requires-retained-evidence-audit`. Multiple keys and commitments do not prove multiple organizations, independent backends, reviewer competence, or artifact truth. The BIT manifest is not a TreeSwap deployment or release capability. See [BIT review ceremony preflight](./BIT_CEREMONY_PREFLIGHT.md), [Signed BIT provider evidence](./BIT_PROVIDER_EVIDENCE.md), [BIT independent review evidence](./BIT_INDEPENDENT_REVIEW.md), and [Reviewed BIT deployment manifest](./BIT_REVIEWED_MANIFEST.md).

Clean published ceremony-preflight source [`83f669d6c25b1814554cbdf17b640b704843bcff`](https://github.com/bobofbuilding/treeswap/commit/83f669d6c25b1814554cbdf17b640b704843bcff) passed 422 application/security tests with no skips, both production builds, 68 contract tests, and all 41 sealed local campaigns. [Hosted main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32568051678) passed the same source. Its independently reconstructed mode-`0600` evidence digest is `sha256:54b8bfbb76603f0e847a421f97a9c7d5fc7ff3b8972e4cc2334607b2356415cc`; all 133 file hashes, unique campaign results, immutable image pins, privacy exclusions, source binding, and lab teardown reproduce. This validates only the local threat controls and leaves every provider, reviewer, deployment, infrastructure, and funding fact external.

Release evidence no longer treats equality with a local `origin/main` reference as proof of publication. The shared provenance verifier requires the canonical HTTPS TreeSwap origin, queries the current remote `refs/heads/main`, and requires that exact commit, branch `main`, and a clean worktree before expensive qualification, live BIT fork evidence, deployment-artifact rebuilding, BIT observation, or owner-only account evidence. Each retained capture rechecks afterward; invalid long-running BIT results are withheld or removed, and deployment rebuilding repeats the check after the offline build. This prevents a stale or substituted local remote-tracking ref from being presented as the official published release; it does not authenticate GitHub beyond the operator's Git/TLS trust boundary or replace signed release review.

Local qualification also no longer trusts pre-existing Docker state. A failed or interrupted force-close run can leave valid but uneconomic 330-sat LND anchor sweeps or incomplete pending-close state in persistent volumes, causing a later run either to fail for the wrong history or to inherit evidence it did not create. The sealed runner destroys only the named disposable `treeswap-regtest` volumes before the first campaign and again on every exit; either destruction failure invalidates the run. A separate aggregate-only evaluator reads exact `PendingChannels` and WalletKit `PendingSweeps`, marks every close/HTLC/non-anchor unsafe, and permits only matured, non-immediate anchors under count, total-value, 1,008-block age, 1,008-attempt, budget, and 144-block deadline-margin caps. Two non-listening read-only collectors now reduce raw LND state locally and sign short-lived aggregate attestations under distinct Ed25519 keys. V2 binds a timestamp-independent digest of the exact aggregate block/state, and the exact quorum fails closed on missing, stale, invalid, unsafe, wrong-node, key-reused, state-disagreeing, or time-skewed reports; two independently healthy labels can no longer conceal inconsistent underlying snapshots. The force-close campaign exercises both exact three-URI macaroons and signing identities through actual pinned LND REST before, during, and after bounded recovery without emitting identifiers. This makes the local protocol deployable and the exception explicit, but two containers in one lab do not prove production independence or node longevity; separate supervised hosts, alerts, review, and actual operator-node recovery evidence remain required.

The candidate public-testnet campaign boundary now requires every listed provider, observer, monitor, relay, and solver operator to sign one exact seven-day bidirectional record and policy. A guarded workspace removes the need to hand-edit that record: every scenario or solver metric extends a mode-`0600`, non-overwriting snapshot by one hash-linked revision; pairwise verification detects removal or substitution; checkpoints expose missing evidence; and finalization derives counts and a test-only feature set through the same strict validator. It rejects omitted fault scenarios, one-direction samples, weak reliability, stale reconciliation, open final state, unexplained liabilities, and signature replay; its privacy-safe summary derives rather than trusts reported rates. Release-candidate assembly then re-verifies both this campaign and a fresh deployment promotion, derives every shared field, commits to record and policy digests, and hashes both provider/finding inputs so neither can overwrite the other. Both public candidate calls and templates must be exact plain own-enumerable data records, and candidate preparation retains only bounded exact-data snapshots of the templates, deployment manifest, and operational safety-monitor policy before hashing and runtime binding. Accessors are not invoked; coercion and later mutation are inert; hidden, symbol, inherited, prototype-named, custom-prototype, unsafe, executable, cyclic, oversized, sparse, and decorated inputs fail closed. A guarded five-role ceremony independently rebuilds each role's exact payload and checks controller/guardian ERC-1271 signatures through the exact finalized provider set, but writes only a non-activating receipt with no raw signatures or URLs. Candidate, approval-bundle, and provider-configuration data are snapshotted once through bounded exact own-enumerable schemas before review; getters, caller coercion, symbols, hidden/inherited/prototype-named fields, custom prototypes, sparse/decorated arrays, and later caller mutation cannot produce a different signing payload. Snapshot hashes and that receipt do not prove elapsed time, upstream provenance, artifact truth, deployed manifest truth, a unique history, signer custody, or operator independence, so independently retained checkpoints, external validation, and the live campaign remain external gates. See [Public-testnet campaign evidence](./PUBLIC_TESTNET_EVIDENCE.md), [public-testnet release candidates](./PUBLIC_TESTNET_RELEASE_CANDIDATES.md), and [public-testnet release approvals](./PUBLIC_TESTNET_RELEASE_APPROVALS.md).

Operational release inputs no longer enter through opaque operator-supplied support, loss, privacy, service-isolation, or final-qualification hashes. A separate three-role service-isolation verifier requires twelve exact service roles, unique trust domains, exact ingress and encrypted transport, least-privilege credential classes, unique credential sets, bounded rotation windows, and committed network/deployment evidence. Adoption-policy v1 defines exact public limits, directional fees, solver liveness, privacy, loss allocation, support, and upgrade behavior. A separate five-role operational-readiness v4 verifier requires that policy plus exact backup, alert, incident, monitoring, provider, reconciliation, service-isolation, solver, and qualification artifacts; the complete immutable incident-drill set; at least two alert channels; one primary plus two retained observers per drill; bounded timestamps; and five distinct EIP-712 signers across at least two organization commitments. It revalidates one immutable snapshot of the complete safety-monitor v4 policy, maps both confirmer route/operator commitments to the isolated asset-verifier and EVM-finality-authorizer services, and requires exact signed finalized-gate control claims for provider disagreement, provider outage, and gate-halt-with-exits drills. Every signer explicitly signs the adoption-policy, safety-monitor-policy, and gate-confirmer-binding digests. Qualification-review v1 reconstructs the exact raw sealed artifact, enforces the mandatory campaign and shared versioned configuration manifests, binds both SHA-256 commitments, report and findings disposition, and verifies a separate short-lived reviewer signature. Release-candidate v6 requires that uncopyable provenance to match both upstream and operational signatures, requires the monitor policy's record digest to equal the exact signed bootstrap/campaign record, exposes that digest, and deterministically rebinds the otherwise-identical policy to the final candidate record. It also excludes reviewer authority and organization overlap, binds isolation, adoption, monitor, and confirmer evidence plus participant-set digests, requires exact Lightning/security role continuity, keeps the infrastructure signer separate from operational/deployment/review/release authorities, matches alerts, artifacts, liabilities, and drill evidence, and rejects a cap or upstream-policy mismatch. The signatures and commitments still do not prove deployed separation, reviewer competence, scoped secrets, drills, alerts, backups, support, retention, or organizational independence; retained external evidence, direct infrastructure inspection, and witnessed drills remain launch gates. See [Qualification review evidence](./QUALIFICATION_REVIEW_EVIDENCE.md), [Adoption policy boundary](./ADOPTION_POLICY.md), [Service isolation evidence](./SERVICE_ISOLATION_EVIDENCE.md), and [Operational readiness evidence](./OPERATIONAL_READINESS_EVIDENCE.md).

## Executive conclusion

The v1 mechanism is a bilateral RFQ followed by one full-fill hash-locked settlement. It deliberately excludes a public order book, public LP deposits, rewards, and partial fills. It is not safe merely because both sides use a hash: the beneficiary, amount, hash, timeouts, fee caps, and replay domain all have to be bound before a Lightning payment becomes irreversible.

The prototype is appropriate for product exploration. Real funds should remain disabled until every critical and high item below is implemented, tested in adversarial regtest/fork environments, and independently reviewed.

The official active and retained-recovery runtimes now also remove
deployment-supplied request functions from the EVM claim path. Broadcast and
two-provider reconciliation use a module-owned port-443 Node HTTPS client with
certificate verification, fresh connections, unpredictable request IDs, a
fixed method allowlist, bounded exact params, and bounded exact responses.
Plaintext, URL credentials, nonstandard ports, globally disabled verification,
envelope mismatch, oversized or stalled bodies, and caller transport
substitution fail closed. This prevents an injected client from bypassing the
pre-broadcast chain/code checks or manufacturing reconciliation agreement; it
does not prove the truth or organizational independence of deployed providers,
their TLS/trust-root identity, or custody of credentials embedded in an RPC
path or query.

## Release-blocking findings

### TS-C01 — Fixed-par inventory drain

**Severity:** Critical  
**Status:** Repository controls implemented; live monitor, pinned deployment values, and independent executable-price sources remain a testnet gate

The 100-sat par value is not enforced by the BIT token contract. If BIT's executable market value or BNote redemption value moves below 100 sats, an attacker can acquire discounted BIT and repeatedly drain the Lightning side at TreeSwap's stale par. If BIT moves above par, the BIT side can be drained instead.

Safeguards:

- treat 100 sats as a reference, not an unconditional redemption promise;
- enforce separate per-direction and per-epoch notional caps;
- add inventory-band fees that increase as one side becomes scarce;
- stop new intents when backing, redemption, contract implementation, or external executable-price signals breach configured limits;
- require multisig recovery to resume after a circuit breaker;
- display that par is a project rule, not an onchain guarantee;
- start with solver-owned inventory so no public LP absorbs an undefined peg risk.

Implemented now: the vault enforces an immutable reference-price band, maximum BIT per swap, maximum BIT per solver epoch, and maximum fee. The fail-closed policy in `lib/risk-policy.mjs` additionally requires a fresh ERC-1967 implementation and code-hash match, an unpaused 18-decimal token, healthy Ethereum finality, three independent fresh executable price sources, a bounded source spread, a market/reference band, an exact implied-quote/market band, per-direction inventory reserves, and scarcity fees. It hard-caps the candidate set at 64 before price-source work, reduces exact repeats, quarantines every conflicting eligible observation sharing a source, venue, control domain, or operator organization, and orders the surviving set canonically. A coordinator cannot select a favorable still-valid signed price by reordering duplicate identities or pair opposite extremes of the contract reference band and live market band. Risk-attestation v3 binds the exact direction, Lightning sats, net BIT wei, full inventory snapshot, median, quote price, exact deviations, fee, reserves, source observation windows, earliest safe expiry, sources, and policy; request or snapshot substitution fails. Only its original same-process object can satisfy a current downstream check, and it cannot cover a quote window beyond the first snapshot/source expiry. The blind-RFQ path also requires the attestation's policy digest to equal the reviewed release risk policy, so a coordinator cannot substitute a genuine proof produced under weaker limits. The user-signed received set commits that policy binding, and coordinator schema v9 plus execution-policy v3 preserve and recheck it against the same-process release context after restart. Every deviation decision uses cross-multiplication, so a one-unit overrun cannot vanish through basis-point rounding. The bounded monitor requires two short-lived EIP-712 health observations under distinct policy-pinned operator commitments in each of eight domains. One collector outage, disagreement, or unsafe report closes quotes and asks two guardian routes to submit the policy-bound alert digest. It does not trust either broadcaster's success claim: two policy-bound read-only confirmation providers must agree on the exact accepted transaction, successful receipt and `Halted` event, finalized block, and fully closed gate state before the monitor credits onchain closure. One confirmation failure or disagreement leaves the halt incomplete while fixed secret-free alerts still run. Unsigned objects and copied verified results fail closed. Collector signatures authenticate who made the claim; operator commitments do not prove the claim's truth or real independence. `docs/RISK_POLICY.md` and `docs/MONITORING.md` define halt and recovery behavior. These controls are executable and tested, but production quotes remain disabled until live inputs, pinned values, continuously supervised independent collectors, confirmation providers, and monitor instances, external alerts, deployed multisigs, and the recovery process are independently reviewed.

The evaluator snapshots the exact policy, request, state, and bounded candidate array once before making any decision. This prevents a hostile getter, proxy, later mutation, or custom array iterator from using loose limits during evaluation and strict values when the reviewed policy digest is committed.

A planned BIT/WBTC pool does not remove this threat. The [BIT/WBTC market-reference boundary](./BIT_WBTC_MARKET_REFERENCE.md) treats it as one venue only and requires the exact policy-pinned BIT proxy runtime, EIP-1967 implementation slot/address/runtime, safe `BIT` / `18` / `false` token state, the canonical WBTC runtime with safe `WBTC` / `8` / `false` state, a mature finalized TWAP, active and wide-range liquidity, a request-sized directional probe, exact pool/quoter/feed runtime identities, two-provider agreement, and WBTC/BTC peg validation. It snapshots exact plain-data inputs before validation and hashing, rejecting accessors, coercion hooks, hidden/symbolic/inherited fields, sparse or decorated arrays, and post-review mutation. Any BIT upgrade, token runtime drift, or unsafe BIT/WBTC state invalidates the allowlisted policy. The original same-process verified signal and its allowlisted policy digest are required. Every other venue must likewise supply a short-lived policy-pinned EIP-712 observation through the executable-venue verifier. Copied objects, invented JSON sources, two labels for one venue/control domain, two RPCs observing one pool, or the conversion feed itself cannot manufacture source independence. Tests prove no observations, a placeholder pool, and one pool signal all remain non-authorizing. No pool exists yet, so this source remains unavailable and funded public execution remains closed.

### TS-C02 — Unbound preimage claim

**Severity:** Critical  
**Status:** Implemented in both direction-specific escrow prototypes

The preimage becomes public in a claim transaction and is known to a Lightning invoice creator. An escrow that pays `msg.sender` or allows the recipient to be chosen when the preimage is revealed can be stolen or front-run.

Safeguards:

- bind one exact Ethereum beneficiary during an onchain `RESERVED` transition before the Lightning payment starts;
- require `sha256(preimage) == paymentHash` but transfer only to the bound beneficiary;
- include escrow ID, payment hash, maker, beneficiary, amounts, fees, and expiry in the reservation event;
- make beneficiary changes impossible after payment authorization;
- mark every payment hash consumed globally so it cannot be reused.

The solver-funded vault binds the user's beneficiary in the user-signed Lightning → BIT quote before inventory is reserved. The user-funded escrow independently binds the solver beneficiary in a direction-specific, solver-signed BIT → Lightning quote before BIT is deposited. In both contracts anyone may relay the preimage, but the transfer target cannot change; payment hashes and direction-specific nonces are single-use, and the complete amount, fee, invoice digest, and deadlines are signed before Lightning authorization.

### TS-C03 — Timeout and finality race

**Severity:** Critical  
**Status:** Deterministic derivation, boundary harness, integrated local EVM/LND campaign, and credentialed pinned-live-BIT combined fork implemented; public testnet, independent finality, and congestion remain launch gates

Lightning HTLCs expire in Bitcoin block-height terms while BOLT 11 invoices and Ethereum reservations use wall-clock or Ethereum timestamps. A refund that opens too early can race a valid Lightning settlement; a claim window that is too short can let one party receive one leg while the other leg refunds.

Safeguards:

- define one monotonic settlement policy derived from current Bitcoin height, invoice expiry, final CLTV delta, Ethereum finality target, and a claim buffer;
- require the Ethereum refund deadline to outlive the last safe Lightning settlement time plus Ethereum confirmation and congestion margins;
- wait for sufficient Ethereum confirmations before authorizing a Lightning payment;
- make `CLAIMED` and `REFUNDED` mutually exclusive terminal states;
- test reorgs, delayed blocks, mempool congestion, force-close, and boundary timestamps;
- reject invoices whose expiry or final CLTV cannot satisfy the safety policy.

Both escrows enforce `quoteExpiresAt < lastSafeClaimAt`, a minimum settlement window, a minimum claim-to-refund buffer, a maximum lock duration, and a deterministic boundary where claims close exactly when refunds open. `lib/settlement-policy.mjs` now derives those values from the signed invoice timestamp, expiry, final CLTV, observed Bitcoin height, Ethereum finality target, and explicit relay and congestion margins. It requires the conservative larger final CLTV for every Lightning → BIT invoice, checks the actual accepted HTLC height without ever extending the signed deadline, and distinguishes an observed escrow from a canonical, finalized escrow authorized for Lightning action. The integrated local campaign binds both live invoice directions into actual TreeSwap escrows, waits for twelve simulated EVM confirmations, exercises the paid-invoice claim, reaches the actual accepted-HTLC cutoff, and proves refund/claim exclusivity. The credentialed repeat now passes against the exact pinned live BIT fork. `docs/SETTLEMENT_POLICY.md` defines the policy. Public testnet, independent finality, and production-like congestion remain required before funding.

The selected-solver provider's crash window no longer requires a fresh random Lightning → BIT invoice. A separate private core deterministically derives the preimage/payment hash from the exact signed request and a retained versioned secret, then looks up or creates only that hash through a two-URI LND credential. The preimage is never returned or persisted, and a mandatory lookup validates the resulting invoice. Because pinned LND overloads REST status/code pairs, remote error text is reduced inside the pinned transport to a non-sensitive reason enum; only exact missing-invoice classifications permit creation, while permission and unknown failures close. The live regtest campaign proves exact material after service recreation and proves the credential cannot cancel; a different authority performs cleanup. The coordinator now independently decodes the raw returned invoice before the second user authorization. Public-provider composition remains blocked until the private route and decoder are deployed together, the request-to-key-version record and retained-key rotation/recovery are reviewed, and crash/custody evidence exists. See [Selected-solver invoice material](./SELECTED_SOLVER_INVOICE_MATERIAL.md).

### TS-C04 — Relay can suppress or reorder quotes

**Severity:** High for swaps
**Status:** Direction-correct multi-solver validation and authenticated multipath received-set selection implemented; independent deployment remains open and global availability is explicitly not claimed

An offchain relay can hide a better quote or delay one solver. If source labels or receipt times came from the relay, it could also impersonate several paths or fabricate receipt order. The escrow can enforce the exact selected quote but cannot prove that it was globally best.

Safeguards:

- have users sign the exact selected quote and enforce its output and fee caps onchain;
- have the client request quotes concurrently from at least two relays and two direct solver endpoints;
- send only unlinkable blind pricing data, authenticate each complete blind-offer batch under a fresh path-bound challenge, and assign receipt time only after local receipt;
- show “best received quote,” never “global best price”;
- let the user choose the signed quote rather than letting the relay select it;
- privately disclose addresses and invoice/hash data only to the selected solver, then require its executable quote to match the blind solver, price, capability, capacity, endpoint, and runtime;
- require one exact user invoice for BIT → Lightning or one selected-solver invoice for Lightning → BIT only at private finalization; the reference solver separately proves local hold-invoice creation;
- keep public order-book rewards outside v1.

The escrows verify direction-specific accepted terms onchain. `lib/rfq-delivery.mjs` authenticates configured relay and direct-solver paths, rejects reused identities, carries only the blind pricing payload, verifies fresh signed response batches, and commits the complete attempt set using client receipt times. Public capability and RFQ responses share a strict identity-JSON, `no-store`, canonical-framing, exact-length, byte-bounded reader; compression, cacheability, truncation, conflicting framing, interruption, malformed chunks, malformed or BOM-prefixed UTF-8, replaceable global decoding, and responder-controlled teardown cannot authorize or hold the complete-body deadline open. The local RFQ API also snapshots exact descriptor-backed call, policy, pricing, path, request, response, and blind-offer data before any digest, signature, request, or concurrent collection. Getters and scalar coercion hooks are never invoked; hidden, symbol, inherited, prototype-named, custom-prototype, sparse/decorated-array, unsafe, non-JSON, cyclic, oversized, and post-call mutation authority data cannot change a wire request, diversity decision, or receipt. `lib/blind-rfq.mjs` independently snapshots the exact quote-book call, pricing and selection policy, solver envelopes, and dense undecorated capability and market-attestation sets before it verifies capability-bound blind EIP-712 offers. Accessors, hidden or symbolic authority, inherited/custom-prototype records, sparse or decorated arrays, and object coercion cannot alter the request-digest index, candidate validity, or winning order. It retains one best offer per solver, orders exact-output offers by price and local receipt time, commits delivery and offer receipts, and brands one explicit selection. Each retained offer requires the original same-process market-risk attestation for its exact Lightning/BIT economics through quote expiry; a BIT/WBTC observation can inform that attestation as one independent venue but cannot settle the bridge or choose a winner. Only that solver may receive the private settlement fields; its full quote must pass `lib/rfq.mjs` validation and exactly match the blind economic and capability fields before the invoice can be bound. A flat full-quote list or copied finalization cannot authorize, and every fallback requires fresh private disclosure and user authorization. [`RFQ_POLICY.md`](RFQ_POLICY.md) and [`RFQ_DELIVERY.md`](RFQ_DELIVERY.md) define the claim boundary: this proves the selected terms and reproduces the authenticated received set without exposing private identifiers to relays, but multiple keys do not prove multiple organizations and no client can prove global quote availability. The product therefore says “Best received quote,” requires multiple solver identities and paths, and makes no global-best claim.

The local quote-ingress edge adds quote-only EIP-712 authentication and durable anti-replay before collection. It binds the pricing digest, chain, direction-specific escrow, wallet, nonce, expiries, and browser origin; accepts only canonical EOA signatures and exact credential-free HTTPS/CORS/framing; and forwards neither wallet nor signature to solvers. The private store HMACs identities and bearer session tokens, claims before untrusted work, bounds per-identity and global active/window load, and consumes selection once under a durable monotonic clock. A second random token addresses the same-process user-signing and durable-reservation ceremony without serializing selection authority. Wrong-mode preview or reservation authority is closed, shutdown aborts in-flight work and erases pending ceremonies, and restart burns rather than restores any in-memory selection or reservation authority.

The production reader no longer lets a deployment callback acquire production provenance. It accepts only the original fixed-HTTPS RFQ service, original verified solver capabilities and price signals, exact snapshotted blind/risk/inventory/finality inputs, and one lifecycle. It independently validates delivered offers and derives exact same-process Lightning/BIT risk attestations before the blind book and opaque preview exist. One service binds one reader and one route exclusively leases it; retained handles, copies, accessors, stale evidence, policy mismatch, later mutation, cancellation, shutdown, and duplicate binding fail closed. A future BIT/WBTC signal must retain its original exact-request provenance and is capped at one input; with no pool it contributes nothing. Evidence rotation intentionally requires a controlled reader/route restart, preventing a mutable arbitrary provider callback from silently changing policy mid-session.

The selected-solver consumer now fixes the later disclosure to the capability-bound public HTTPS origin and exact `/v1/finalize` path. A separately scoped Ed25519 requester key signs the exact capability, capacity snapshot, endpoint key, solver, direction, private packet, and short window; the solver must compare that key digest with a deployment allowlist. The capability's already-proven endpoint key signs the response, including the original request ID/digest, invoice, executable quote, and expiry. One prepared attempt retains one stable signed request, so ambiguous transport can retry only byte-identically; concurrent sends reject and the first verified response is cached. The provider durably claims that ID and digest before finalizer work, commits the exact signed response before return, replays identical bytes after restart, rejects conflicting re-signs, and routes an interrupted claim only to a separate idempotent recovery method after lease expiry. Lightning → BIT is composed through a second signed, TLS-pinned private service that binds the semantic request to one retained payment-secret key ID before LND and commits its signed response before return; the public finalizer receives neither the LND credential, key, nor preimage. A real hard-kill restart replays byte-identically with one LND liability. BIT → Lightning bypasses this service. The independent raw decoder now checks both directions before disclosure or second authorization and binds returned solver invoices to the capability's Lightning node. Invoice-digest mismatch, changed user invoice/hash, endpoint/requester/provider substitution, stale authority, clock rollback, repricing, capacity/runtime drift, malformed invoices, or copied provenance fails closed. The original durable reservation then produces the exact second user authorization through the strict private execution route; ambiguity, route timeout, concurrency, shutdown, and lifecycle replacement cannot create another attempt or revive bearer authority. Deployed decoder/browser/coordinator composition, liability recovery, encrypted volume/key custody, accepted-HTLC/node/host drills, and external review remain open. See [`SELECTED_SOLVER_FINALIZATION.md`](SELECTED_SOLVER_FINALIZATION.md).

These controls reduce replay, quota evasion, session theft persistence, request smuggling, cross-origin abuse, production-callback substitution, and mid-lifecycle evidence replacement but do not prove TLS-edge privacy, truthful independent RFQ or price/state providers, deployed volume/key durability, safe evidence-rotation operations, browser compatibility, ERC-1271 quote-request support, availability, or global quote completeness. The handler and reader have no listener or funding/settlement/signing authority. See [`RFQ_QUOTE_INGRESS.md`](RFQ_QUOTE_INGRESS.md).

Clean published multipath-RFQ source [`28a3542d0f70027aef028e9f52d828958c846964`](https://github.com/bobofbuilding/treeswap/commit/28a3542d0f70027aef028e9f52d828958c846964) passed 438 application/security tests with no skips, both production builds, 68 contract tests, all 41 sealed local campaigns, and [hosted main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32573377971). Its independently reconstructed mode-`0600` evidence digest is `sha256:61fdb565cb75c8cf7e2f89f90a32d87a52e50f8c56ebe2b8e253c663e8323d3a`; all 135 file hashes, unique campaign results, immutable image pins, privacy exclusions, clean source binding, and lab teardown reproduce. This validates the local suppression/rewrite/privacy controls only. It supplies no independent relay or solver, encrypted private-disclosure deployment, public-testnet behavior, external review, or funding authority.

## High-severity findings

### TS-H01 — Replay across contracts, chains, or versions

**Status:** Implemented in both direction-specific escrows; account-login SIWE remains EOA-only by design

EIP-712 itself does not supply a nonce policy. A signature can be replayed unless the domain and message bind `chainId`, verifying escrow contract, protocol version, maker nonce, direction, amounts, recipient, payment hash, and expiry. The contract must cancel or consume each nonce exactly once.

Both escrows bind chain, verifying contract, protocol version, direction-specific type, participants, amount, fee, invoice digest, payment hash, nonce, and deadlines. User and solver nonces and payment hashes are consumed once. Canonical low-s EOA signatures and ERC-1271 contract signatures are supported through a shared static signature checker; malformed return data, a wrong magic value, a revert, or the wrong owner fails closed. The account-login SIWE surface remains EOA-only and does not advertise contract-wallet login.

### TS-H02 — Reservation griefing and solver last-look

**Status:** Dual-signed user-exercised BIT reservations and local atomic persistent admission controls implemented; deployed shared enforcement and live reliability telemetry remain deployment gates

An attacker can request many quotes, while a solver can advertise attractive liquidity, wait for market movement, and abandon only losing fills.

V1 has no public order reservation. `TreeSwapBitVault` requires the user and solver to sign the same exact quote. The solver pre-funds its segregated BIT balance, only the named user may exercise the quote, and one active reservation is permitted per user until claim or refund. The solver therefore cannot apply last-look after releasing its signature. `lib/admission-policy.mjs` separates non-reserving RFQs from firm quotes; it enforces authenticated per-identity concurrency and rolling quotas, minimum size, cancellation sequences, short expiries, cryptographically verified capabilities, fresh per-solver capacity epochs, bounded firm commitments, capability expiry, and measured fill reliability without a solver allowlist. An executable offer additionally binds the verifier-issued capability and capacity-snapshot digests, exact observed inventory, endpoint key, chain, escrow address and runtime code, and expiry; an indicative book cannot authorize settlement.

`lib/blind-rfq.mjs` now requires the exact authenticated selection, the same module-private capability proof used during collection, and an exact short-lived EIP-712 signature from the RFQ user before private disclosure. That first authorization binds the private request, beneficiary, nonce, invoice commitments, received set, selected solver, and exact blind economics. Schema v7 persists its digest while atomically reserving both scarce resources: gross BIT plus inbound Lightning for Lightning → BIT, or outbound Lightning plus maximum routing headroom for BIT → Lightning. Every capacity read reconciles the stored counters against the active offer ledger. The coordinator then binds one exact private-request and executable-offer digest pair and exact finalization time, but that solver-finalized candidate remains non-authorizing. A second user signature binds the full executable quote, payment hash, invoice digest, beneficiary, exact amounts, first approval, durable execution binding, and expiry before its digest can be persisted or the invoice bound. Only byte-identical retries succeed. Copied approvals or provenance, wrong wallets, fake or method-replaced stores, stale capacity, cancellation, expiry, request substitution, same-ID or partial-ledger mutation, a second invoice, and connection races fail closed. Older schemas with active commitments refuse migration.

The wallet ceremony now renders those two effects from the canonical typed fields. It signs only when received terms match an independently derived local digest, exposes full identity, amount, fee, routing, invoice/hash, contract, chain, received-set/offer/binding, and expiry commitments, and makes the no-funds first step and no-allowance second step explicit. The signer never requests account access or switches chain during authorization, requires the same active wallet and chain before and after the request, rechecks expiry, verifies the returned signature, and rejects copied prompt objects or concurrent use. This removes a repository/UI ambiguity; deployed testnet integration, wallet diversity, phishing resistance outside the trusted origin, and retained both-direction user evidence remain external gates.

Unknown BIT → Lightning solvers receive the configured first-fill cap including routing headroom, while the store enforces one atomic global in-flight ceiling across all identities to contain Sybil multiplication. A solver reaches the established cap only after exact completed BIT → Lightning settlements with independently verified both-assets proofs. Promotion, reliability, and consecutive failures are calculated from exact ledger outcomes in the offered direction; Lightning → BIT success cannot promote or rehabilitate BIT → Lightning behavior. Shared resource commitments remain global per solver. The first accepted RFQ permanently binds the normalized policy digest, only opaque identity commitments are retained, cancellation sequences never decrease, capacity epochs never go backward, a firm quote cannot outlive its capability, conflicts latch new quotes closed, and a claimed fill cannot increment history unless the same selected offer is already in a proven `COMPLETED` settlement. Two independently opened local connections cannot change policy or exceed identity, either capacity dimension, solver, executable-binding, or global in-flight limits. Repeated solver-attributable failures suspend the solver, while user expiry or abandonment does not. BIT → Lightning remains a refundable liveness risk because Ethereum cannot force a Lightning payment. Bonds are deferred until a non-subjective failure adjudicator and operating data exist. The local endpoint transport proves a fresh bound endpoint response and refuses replay, mutation, timeout, redirect, and SSRF targets; deployment, shared persistence, capacity reconciliation, and live reliability telemetry remain required.

### TS-H03 — BIT proxy upgrade or pause

**Status:** Fail-closed, time-bounded onchain open gate, runtime token checks, pinned mainnet-fork and live-BIT reorg campaigns, signed provider/reviewer handoff schemas, a finalized zero-balance closed-deployment observer, and signed deployment preflight/postflight boundaries implemented; actual independent reports, release binding, live monitor, reviewed manifest, production multisig roles, and public-testnet evidence remain deployment gates

BIT is an ERC-1967 proxy and its current implementation is pausable and upgradeable. The recorded v1 pause blocks mint and redeem but not ordinary ERC-20 transfers. An implementation change could alter transfers, decimals, pause behavior, or trust assumptions.

The risk monitor pins the ERC-1967 implementation slot, proxy and implementation code hashes, decimals, pause state, finality, and executable-price inputs. `buildBitRiskAttestation` commits the exact healthy snapshot. The immutable `TreeSwapOpenGate` deploys closed, requires a delayed controller action to open, expires automatically, and lets a separate guardian halt immediately. Both escrows require that live gate and independently fail closed unless `decimals() == 18` and `paused() == false` at the opening transition. Exact sender and recipient balance deltas are enforced on token movement. The gate is never consulted by withdrawal, claim, or refund, so it cannot trap existing positions. A mainnet fork proves exits remain transferable under the recorded v1 pause; a clean-published Anvil fork of the same pinned live BIT state proves pre-authorization, post-authorization, and post-claim block replacement fails closed in both directions. The local hostile-token suite separately proves state rolls back if a future implementation blocks transfers. Production still requires live pinned values, continuous monitoring, multisig-controlled roles, alerting, and independent review before funding.

### TS-H04 — Rounding and unit mismatch

**Status:** Canonical integer units, explicit rounding order, exact-output minimality, and conservation tests implemented

BIT uses 18 decimals, Lightning routes in millisatoshis, and the product quotes whole satoshis. At 100 sats per BIT, one sat equals `0.01 BIT`. Unsupported dust, inconsistent rounding, or fee order can leak value over many fills.

`lib/units.mjs` is the canonical quote-unit boundary. It uses `bigint` only, accepts Lightning outputs as whole `uint64` sats, converts adapter millisatoshis only when divisible by 1,000, bounds escrow BIT to `uint96` wei, and fixes one reference sat at exactly `10^16` BIT wei. The BIT protocol fee rounds down. BIT → Lightning output rounds down to whole sats after the fee, routing is then subtracted, and the remaining sub-satoshi BIT wei is explicitly recorded on the solver leg rather than treated as protocol revenue. Exact-output quotes compute the smallest sufficient integer input. Conservation is asserted for every direction, boundary values, dust, and 10,000 small fills. UI decimal previews remain informational and cannot supply signed integer fields.

### TS-H05 — Fee denomination ambiguity

**Status:** BIT-only protocol fee, immutable recipient/cap, signed routing cap, and fee-free refund behavior implemented in both directions

TreeSwap charges the protocol fee only in BIT wei in both directions and never attempts to skim Lightning. Both escrows bind the exact BIT fee in the signed quote, reject it above an immutable deployment cap, and send it to an immutable collector only on a valid preimage claim. Refund returns the full locked amount and charges no fee. Solver spread is embedded in the signed exchange amounts; BIT → Lightning also binds a maximum routing fee and exact Lightning output. A changed fee requires a new short-lived quote and fresh authorization. [`FEES.md`](FEES.md) defines the display and accounting rules.

### TS-H06 — Liquidity-pool insolvency and adverse selection

**Status:** Public LP deposits, shares, yield claims, rewards, partial fills, and web funding disabled in v1; solver reconciliation and fault campaigns remain deployment gates

A Lightning pool is not an ERC-20 vault: channel balances, in-flight HTLCs, force closes, reserves, and node-key compromise affect solvency. Public LPs can also be selected against when par is stale.

TreeSwap v1 does not accept public LP funds or create a pooled claim. `V1_CAPABILITIES` keeps public deposits, LP shares, promised yield, rewards, partial fills, public permissionless execution, and web funding disabled. The Earn tab is retained as a calculator for solver operators and now states that it performs no deposit and offers no LP share, APY, or yield. Each vault deposit remains owned and withdrawable only by that solver, while accepted liabilities reduce its own available balance. Lightning stays on that solver's node under a separate budget. Isolated reconciliation, force-close, restart, and directional-exhaustion campaigns pass; deployed multi-operator reconciliation and insolvency drills remain required. Any third-party deposit or yield product requires a new contract, custody/economic/legal review, and explicit capability change; it is not an extension of this v1 vault.

### TS-H07 — Lightning adapter or macaroon compromise

**Status:** Exact RPC manifests, expiring credential/TLS isolation, overlap credential rotation and live old-root revocation, real TLS certificate/pin rotation with rollback, fixed private-HTTPS coordinator transport, hold-invoice terminal faults, accepted-state restart, live fee/amount/in-flight caps, channel-offline recovery, signed intent-bound authorization, dedicated signed adapter responses, durable replay and chain-progress state, stateless fail-closed initialization, production-duration timing, payer- and invoice-side coordinator lost-response recovery, and secret-free audits pass isolated regtest; deployed TLS identity/trust root, isolation, key custody, monitoring, and independent review remain deployment gates

The Ed25519 authorization envelope now snapshots exact plain own-enumerable call, envelope, payload, and flat method-specific operation data before canonicalization, signing, or verification. The upstream coordinator action runner also snapshots one exact method-specific operation with canonical bounded scalars before the authenticated private packet can become a durable commitment, then uses that frozen operation for commitment comparison and authorization signing. Accessors, erased hidden or symbol authority, inherited or prototype-named fields, custom prototypes, object coercion, unsupported methods or values, and post-review or post-sign mutation fail before the side-effect claim, adapter policy, or LND contact. This closes an in-process representation gap but does not protect a compromised packet provider, coordinator signing key, or adapter host.

An adapter that can create, settle, cancel, and inspect every invoice is a hot-wallet control plane. A broad LND admin macaroon can also affect the node beyond TreeSwap.

The funded active and retained-liability recovery runtimes now accept only an
original same-process Lightning action configuration whose adapter origin is a
credential-free private HTTPS origin on port 443. The factory supplies the
module-owned Node HTTPS request path internally and rejects any caller transport
field, so plaintext, public, credential-bearing, path/query-bearing,
nonstandard-port, and callback-substituted transports cannot enter either
runtime. The fixed path uses certificate and hostname verification, a fresh
connection without global `fetch` or a shared dispatcher, no redirects, one
deadline over the complete response body, and strict non-cacheable bounded JSON.
Lower-level HTTP remains explicit regtest-only injection. This removes the
repository transport-substitution gap, but the deployed private DNS, TLS
terminator, trust root, certificate rotation, and network policy still require
independent evidence before funding.

`lib/lightning-adapter-policy.mjs` defines separate exact-URI invoice, payer, and observer roles. It rejects broad or cross-role RPCs, browser-exposed/default/stale/revoked credentials, TLS or private-network failures, node-health and capacity-epoch changes, replayed request IDs or exposure hashes, mutated intent/hash/invoice/amount fields, insufficient directional liquidity, and per-payment/daily/in-flight cap breaches. Hold-invoice settlement also proves `sha256(preimage) == paymentHash`. Macaroons are injected only inside isolated read-only processes and are never accepted from an application request; audits contain no macaroon, preimage, or invoice text. The coordinator requires that audit to be an exact empty-reason allowed record for the same role, method, credential hash, request ID, intent, payment hash, invoice digest, amount, capacity epoch, and signed time window before accepting either a success or reconciliation result. Generic/cross-action audits, unsupported states, malformed or insufficient invoice/HTLC proofs, and loose `NOT_FOUND` errors leave the action `UNKNOWN`. Every parsed `/v1/action` outcome is additionally authenticated by a bounded domain-separated `treeswap.lightning-adapter-response.v1` Ed25519 envelope under one exact reviewed active-runtime-pinned response public key and key ID. Rejections bind the exact request ID and complete authorization-envelope digest; eligible proof commitments bind those fields with the response digest, public-key digest, and key ID. Missing, changed, wrong-key, retired-key, unsigned, request- or authorization-replayed, noncanonical, accessor-bearing, sparse, or oversized envelopes cannot create reconciliation evidence or change state. Even an authenticated rejection or exact signed `NOT_FOUND` cannot prove a prior value-moving call had no side effect, so the action remains `UNKNOWN` and automatic retry stays forbidden. The adapter refuses response/capacity key reuse, and active composition refuses response/request key reuse. This prevents a private-network impostor from forging either success or operational rejection evidence, but a compromised adapter signer remains authoritative and signatures do not provide confidentiality, so deployed key custody, encrypted service identity, monitoring, rotation, retained recovery, and compromise drills remain external gates. Dynamic REST path data is redacted, stream error envelopes cannot masquerade as payment results, read-only failures remain safe to retry and non-ambiguous, and only uncertain value-moving outcomes remain ambiguous. A coordinator Ed25519 signature prevents the application from forging both the request and its matching intent. A separate exact-schema, mode-`0600` chain-progress record persists the last higher block height, best-block hash, header timestamp, and local advance time with file-and-directory sync. Fresh, deleted, or legacy hashless state rejects new exposure; restart cannot reset the clock; and a backward clock observation, height regression, same-height hash/header change, or impossible higher height with the old hash latches closed until a later height with a new hash. Regtest proves fresh and restarted adapters reject without dispatch, then allows exactly one payment only after a real higher block. The current production-duration campaign proves 3,603 monotonic seconds and 119 continuous observations without a block, with adapter recreation after 1,813 seconds preserving the same clock; the final exact payment rejects specifically for no progress with zero native dispatch. It also proves each baked credential has the exact declared root key, URI set, and expiry caveat; forbidden capability categories fail specifically for authorization; timeout and root-key revocation are enforced live; and the admin node stays healthy. Separate exact-URI `SignMessage` and `VerifyMessage` credentials prove four fresh domain-bound challenges recover the declared solver node, mutation fails, cross-role calls are denied, and both disposable roots are revoked. The old and replacement payer credentials work concurrently, old-root revocation deterministically disables only the old adapter, and the replacement remains available through baseline recovery. Separately, rotating the actual LND certificate/key changes the fingerprint without changing node identity or channel point; the old pin fails without payment dispatch, explicit peer reconnect restores channel liveness, the new pin restores adapter service, and a failure path can restore the previous pair. Wrong and late preimages cannot settle, cancellation is terminal, an accepted hold survives the invoice node restarting, exact payment and signed-action replay isolation hold, deliberately lost payer and invoice-settlement responses recover through fresh signed read-only requests with dispatch count one, both exposure directions stop before dispatch when the relevant live channel side is exhausted and recover only after rebalancing, and payer/invoice daily caps survive process restart. Exact UTC rollover drops only prior-day value; permanent request and payment-hash replay protection remains. The invoice recovery lookup contains no preimage, and SQLite, WAL, and shared-memory scans find none. [`LIGHTNING_ADAPTER.md`](LIGHTNING_ADAPTER.md) and [`COORDINATOR.md`](COORDINATOR.md) specify the remaining gates.

For BIT → Lightning, a successful signed tracking lookup now returns the exact hash-bound preimage only to the in-memory coordinator caller, including after a later coordinator restart. Missing or mismatched preimages fail closed, while non-success states expose none. The live lost-response campaign proves the invoice and both raw/textual preimage forms are absent from SQLite, WAL, and shared memory. A signed one-use private-packet protocol and bounded daemon executor now enforce exact action/deadline rehydration, module-private object-identity verification provenance, restart/reconciliation priority, direction-specific payment/claim order, independent finality authorization, one-shot dispatch, and both-assets terminal verification without persisting the invoice or preimage. Before a provider signature can be produced, the response builder requires an atomic replay consumer to accept the exact requester key, request ID, request digest, service time, and expiry; missing, failed, or duplicate consumption rejects. A concrete local provider claims before its private read, consumes before signing, persists replay plus clock high-water state, bounds total request work, and withholds late responses. Plain, copied, or serialized verification, store, reader, claim, and route objects reject. The official operator runtimes accept only a private-packet client carrying the module-owned fixed Node HTTPS provenance; caller-injected transport is test-only. The fixed path uses a fresh port-443 connection, explicit certificate verification, and no global `fetch`, shared agent, or process-wide Undici dispatcher. Plaintext, public, credential-bearing, path-bearing, query-bearing, nonstandard-port, globally certificate-unverified, cacheable, ambiguous-content-type, oversized, and body-stall paths fail closed before a packet can reach execution. Full unattended execution remains disabled until the handler and its separate persistent replay volume are deployed and drilled, its reader/secret scope and private certificate or service-mesh identity, trust root, network policy, and live encrypted path are reviewed, the finality authorizers and asset verifier are independently deployed, and the complete deployed path passes multi-operator fault evidence. See [Durable private-packet provider](./DURABLE_PRIVATE_PACKET_PROVIDER.md).

### TS-H08 — Partial-fill hash reuse

**Status:** Full-fill-only invoice policy, sealed cross-direction onchain payment-hash registry, exact live standard/hold LND regtest decoding, and invoice failure campaigns implemented; deployed isolation and independent review remain deployment gates

A Lightning invoice is not a divisible onchain order. TreeSwap v1 now rejects partial and child intents rather than attempting to divide one invoice. `lib/bolt11.mjs` independently decodes the raw invoice, and `validateFullFillInvoice` accepts one exact, amount-bearing mainnet result while rejecting AMP, keysend, amountless invoices, BOLT 12, unsupported required features, duplicate singleton tags, unsafe timing, unverifiable hashed descriptions, and mismatched signed fields. Basic MPP is allowed only as internal delivery of the single exact invoice total, never as separate TreeSwap fills. BOLT 11 contains no remotely verifiable hold/standard flag, so the protocol does not accept one; the reference solver's local `AddHoldInvoice` provenance is operational evidence, while cross-party safety uses conservative timing and finalized EVM-before-Lightning ordering. `TreeSwapPaymentHashRegistry` is configured with exactly the two reviewed direction contracts and irreversibly sealed; opening either direction consumes the hash globally, so the opposite escrow cannot reuse it. Live standard and hold invoices pass the isolated LND regtest adapter and coordinator paths. [`INVOICE_POLICY.md`](INVOICE_POLICY.md) defines the boundary. Deployed adapter isolation and independent review remain required.

### TS-H09 — Reorg and payment authorization

**Status:** Canonical/finalized escrow gate, one-shot Lightning revalidation, live rapid-block 24-block cutoff, integrated local two-direction EVM/LND deadline proof, exact EVM claim outbox, two-provider read-only agreement, local plus pinned live-BIT fork reorg/replacement campaigns, and credentialed combined pinned-live-BIT execution implemented; public-testnet campaigns through independent providers remain deployment gates

Paying Lightning before the BIT escrow is sufficiently final can leave the payer with a preimage but no durable escrow. `authorizeLightningAction` distinguishes an observed escrow from a matching canonical and finalized escrow, requires confirmation/finality thresholds, healthy finality lag, the exact intent digest, open risk gate, reconciled balances, and healthy synced adapter. Authorization then becomes a short-lived one-shot action bound to the escrow block and hash. Immediately before the LND RPC, dispatch re-reads canonicality, finality, intent, and service state; a post-authorization reorg, finality regression, state change, replay, or exact expiry rejects. The live rapid-block campaign advances an accepted HTLC to a 24-block reserve and proves the adapter rejects the correct preimage there; this is six blocks before pinned LND's observed auto-cancel boundary. The EVM claim runner separately binds one signed transaction hash before broadcast, treats every broadcast result as unknown, and accepts only byte-identical rebroadcasts. Its unattended path reads the exact transaction, canonical receipt, code, event, and finalized state from two provider origins before applying one durable observation; outage or disagreement leaves the action unknown. Local Anvil campaigns prove finalization, replacement/nonce contention, disagreement without mutation, relayer rotation, and a disappeared inclusion that produces a reorg halt. All six escrow block-replacement boundaries also pass on an Anvil fork of the pinned live BIT proxy with zero post-reorg dispatch and one canonical recovery payout. The agreeing origins and fork still use local execution infrastructure, so genuine public-testnet finality and the complete campaign through independently operated providers remain required before funding. See [EVM claim-outbox evidence](./EVM_OUTBOX_EVIDENCE.md) and [controlled EVM reorg evidence](./EVM_REORG_EVIDENCE.md).

The bounded daemon no longer treats a structurally matching callback object as finality or reconciliation authority. Reservation, both dispatch types, and both terminal outcomes require an exact dual-signed, short-lived, release/policy/solver/direction/escrow-bound EIP-712 record and the original module-private verification result. Nominal booleans, copied verification objects, signer substitution, mutation, cross-purpose reuse, stale/future records, unsafe deadlines, and exact-expiry use halt before value movement or completed-history promotion. These signatures authenticate the configured operators and commitments; deployed organizational independence and truthful upstream observations remain external evidence gates.

The reviewed operator runtimes also reject evidence controls constructed with
injected transport, clock, or entropy. The production evidence factory owns a
direct `node:https` request path, the system clock, and cryptographic request-ID
source; explicit test factories propagate separate provenance into both active
and recovery-only controls. The fixed evidence, private-packet, and Lightning-
capacity paths use a shared private-address resolver. Each DNS name is resolved
once with every answer returned verbatim; an empty set, any public address, or a
declared/actual address-family mismatch rejects before dispatch. One validated
private address is pinned for a fresh port-443 connection while the reviewed
hostname remains the TLS SNI and HTTP Host. Private IP literals bypass DNS but
retain explicit certificate verification. The paths use no global `fetch`,
shared agent, or process-wide Undici dispatcher. Packet and evidence responses
also cross one shared reader that requires uncompressed non-cacheable JSON,
rejects conflicting content-length/transfer framing, and requires any declared
length to equal the complete bounded byte stream before parsing. This prevents a deployment
dependency or DNS rebinding from silently changing the fixed private HTTPS
origin, path, port, TLS-verification check, request freshness, or replay identity
while retaining a valid control object. It does not prove DNS administration,
certificate issuance, trust-root scope, private routing, egress policy, endpoint
independence, or honest operators; those remain deployment and review gates.

Each evidence provider now has a concrete fail-closed handler and strict durable
replay ledger. The signed request is policy-matched and atomically claimed before
the independent observation path runs; authority fields are derived from the
request; the assigned EIP-712 role signs; and the claim is durably consumed
before a response leaves. A duplicate, concurrent request, read/sign failure,
closed or full database, copied reader/store, or expiry cannot produce a second
response. Normal startup refuses a missing or empty ledger, so storage loss
cannot silently reset replay history. The ledger also durably latches the
highest locally observed clock second before validation and across every
authority boundary. A forward clock jump may prune an expired request, but a
later backward step cannot make it live again; route health fails through a
restart until real time catches up. A suspected volume loss or rollback still
requires the route to remain offline, requester-key and key-ID rotation, expiry
of all old requests, explicit new-ledger initialization, and a witnessed replay
drill.
Different deployments can still share a dishonest observation backend or
administrator; TLS, persistent volumes, operators, data-path independence, and
live drills remain external evidence gates. See [Durable solver-evidence
provider](./DURABLE_EVIDENCE_PROVIDER.md).

Active and recovery daemon-context construction now rejects non-exact outer
records and non-primitive evidence-policy values before registering any
same-process authority. It inspects own property descriptors rather than
reading accessors, requires plain objects, and validates the exact approver
record. Symbols, hidden fields, getters, prototype-named fields, custom
prototypes, and scalar coercion therefore cannot be erased or executed by a
clone before policy hashing. The same normalized policy values are used for the
digest and the release, solver, escrow-runtime, approver, and freshness checks.

The funded wrapper also rejects a copied, serialized, fabricated, released, or replaced coordinator lease. The raw lease token never leaves module-private state. Ownership, internal time, packet/evidence expiry, and live release funding for new exposure are re-read after external evidence and immediately before every pre-request or evidence-derived durable mutation; value-moving paths check both before claiming their outbox and immediately before the network call, and the Lightning adapter envelope is still required to be current at send time. Losing leadership or validity between those checks starts no request and leaves a recoverable interrupted action. A request that has already started may record its result or ambiguity so a new leader cannot replay it blindly. This is a single-host fail-closed fence, not distributed consensus: funded multi-replica execution remains unsupported and requires a separately reviewed shared fencing design.

The active-execution lifecycle no longer accepts a caller-selected settlement queue. An uncopyable one-use preparation binds a bounded solver-policy set to the original live release supervisor, service lease, store, capabilities, evidence policies, and recursively snapshotted runtime adapters; one supervisor cannot run two active lifecycles. Each cycle discovers the original database's complete nonterminal set, checks that every settlement maps uniquely to the exact selected offer and prepared solver policy before executing any of them, and exposes only aggregate counts plus digests. One unmatched, cross-release, or already-halted liability degrades the cycle, authorizes no new exposure, and advances nothing; it cannot be silently suppressed while the service claims readiness. A halt or gate closure reached during execution ends the cycle immediately, and the resulting aggregate cannot advertise funding, Lightning-dispatch, or new-exposure authority. Cycle commitments use schema v2 to bind the complete discovered, unmatched, halted, attempted, and outcome sets without publishing identifiers. A failed provider refresh revokes the activation, copied or forged supervisors and preparation records reject, and post-preparation callback mutation has no effect. One separate uncopyable cancellation fence is required at policy binding and every active side-effect boundary; shutdown invalidates it and the release supervisor before draining a running step, so late recovery-side work cannot rely on the context's normal post-expiry accounting allowance.

The packaged active-service boundary adds two explicit health phases. Preparation is time-bounded, accepts only the original supervisor/store/mode-bound lease plus a cooperative abort signal, and publishes all authority false. Running health derives authority only from one current release and one complete active cycle; a running or degraded cycle, unmatched liability, stale lease, refresh failure, publication failure, or release mismatch is unhealthy. Configuration must declare `single-host` and exactly one expected replica, and shutdown removes the lease after synchronously revoking the execution fence so a stale active health file cannot survive. The deployment-owned launcher and the lower-level active policy-set handoff reject symbols, hidden fields, accessors, sparse or decorated arrays, unsupported nested values, and extra prototype-named policy fields without invoking getters; an own `__proto__` is preserved for exact-schema rejection rather than erased during cloning. The dual-route evidence client and provider-response builder apply the same exact-data rule to authority-bearing configuration, calls, requests, policies, records, and approvals. They snapshot each bounded value once before validation and replay consumption, then sign and return those same frozen snapshots; caller mutation during replay consumption cannot create a different authenticated response. These controls do not prove that an orchestrator actually runs one replica, do not protect separate hosts or separate runtime volumes, and cannot preempt deployment code that ignores its abort signal. The default container refuses active mode. Remaining deployment risks are real operator-owned startup and secret scopes, enforced one-replica orchestration, external alert delivery, crash drills with real adapters, and any future multi-replica fencing.

Crash-loop masking is separately fail-closed. The active service begins a same-process-provenance run in the durable coordinator database before it verifies a release, and records only aggregate status commitments and fixed outcomes. Startup/background failure increments a bounded retained counter; a prior run left `RUNNING` is an abrupt failure observed by the next owner. Changing the threshold or window cannot erase an in-window failure history. At the threshold, the next start persists `BLOCKED` and exits before provider refresh, policy construction, or dispatch. A read-only supervision probe reports unhealthy unless the current run has a durable active status. This prevents rapid restarts from repeatedly advertising a fresh process as healthy, but local tests do not prove volume durability, process-manager configuration, alert delivery, or a real kill/recovery drill. Those remain deployment gates.

The integrated local campaign binds both live invoice directions into actual TreeSwap escrows, waits for twelve simulated EVM confirmations before Lightning action, claims BIT only from the exact paid-invoice proof, reaches the actual accepted-HTLC safety boundary, and proves claim/refund mutual exclusion at the exact EVM refund timestamp. The accepted route used an 83-block delta for an 80-block advertised final CLTV; the verifier uses the stricter observed boundary and rejects a shorter HTLC. Published source `ccae7f05b4dbe8b082cc7880924717b781b20b6f` passed this inside all 34 sealed local campaigns; the independently reconstructed evidence digest is `sha256:7bc2988a39081d511a46abb7e27e2160e5dceabe23354e6faeeab14c0381f9ec`. Mock BIT, simulated local finality, absent independent providers, and absent funding authority remain explicit limitations.

The credentialed combined runner replaces mock BIT with the pinned proxy at exact block `25788856` and re-verifies the canonical block hash, proxy and implementation runtime hashes, EIP-1967 implementation slot, symbol, decimals, and pause state both before use and before evidence finalization. Its separate evidence schema embeds the re-derived deadline result and cannot be emitted in mock mode. Every provenance mutation, unsafe deadline, dirty or unpublished source, unknown field, unsafe mnemonic, and symlinked state fails closed in local adversarial tests. Clean published source `9ba7d89a8b555f749c2a0f9a450effff340d6833` passed both directions through an authorized archive RPC; the independently revalidated outer evidence digest is `0xb9711ca69dcdad37f41de2ca2bd321534f0404bebd8d6facf89ad2f9f8cad70c` and the nested deadline digest is `0xe2c2efc01ae1f8e04f9765e21e1069ff9f071e87b5fea3e313e3d5816bd10f8e`. The record contains no endpoint or cross-network secret and explicitly excludes public testnet, independent providers, production infrastructure, and funding authorization. See [Pinned live-BIT cross-chain deadline evidence](./LIVE_BIT_CROSS_CHAIN_EVIDENCE.md).

## Medium-severity findings

### TS-M01 — One-tick reward sniping (not applicable to v1)

**Status:** Resolved by exclusion from the immutable v1 capability set

`V1_CAPABILITIES` disables the public order book and maker rewards, and the product exposes neither an order-resting nor reward-claim path. The capability test fails if those flags are enabled. If rewards are ever proposed, require a new protocol version and review with a minimum meaningful tick, minimum top duration, time-weighted accrual, executable quantity, and delayed challengeable settlement; it cannot be added as configuration to v1.

### TS-M02 — Wash fills and sybil rewards (not applicable to v1)

**Status:** Resolved by exclusion from the immutable v1 capability set

V1 issues no reward token, points, rebates, maker emissions, or volume incentives, and `V1_CAPABILITIES.makerRewards` is test-locked to false. A wash fill therefore has no TreeSwap reward to extract and still bears fees and execution cost. Any later incentive system requires a new protocol version plus a separate economic/sybil review and must never emit claimable value greater than independently paid fees.

### TS-M03 — Quote flooding and cancellation churn

**Status:** Bounded admission, nonce/cancellation sequencing, rolling quotas, minimum size, and local atomic solver capacity limits implemented; deployed distributed enforcement remains a deployment gate

`assessRfqAdmission` requires an authenticated request identity, a nonce above its cancellation sequence, minimum quantity, short expiry, per-key active-request limit, rolling request quota, and cancellation quota. The flat quote-book builders reject input above their hard limit before signature work. The authenticated multipath builder separately caps the configured path/batch product and locally verified capability set at 128 candidates, validates every bounded candidate before applying the smaller retained-competition limit, keeps one best valid offer per solver, and deterministically retains the best price/time/ID set. Invalid relay offers cannot consume retained slots, while every authenticated batch remains committed in the delivery digest. `buildReceivedQuoteBook` is explicitly indicative and cannot bind a payable invoice. `buildExecutableQuoteBook` additionally requires a verifier-issued capability result and exact offer bindings for its capability, snapshot, endpoint, chain, escrow address and runtime code, per-solver epoch, inventory, and expiry. Firm offers require fresh uncommitted capacity, an active-offer slot, and the direction-scoped reliability floor. Unknown BIT → Lightning exposure is capped per solver and globally across all identities, and only completed BIT → Lightning history can promote that direction beyond its first-fill cap. The coordinator derives per-direction success, failure, and consecutive-failure history from exact terminal offer rows while retaining global resource commitments, so opposite-direction fills cannot launder reputation or double-count capacity. The coordinator stores exact rolling events, request lifecycles, cancellation sequences, capability expiries, capacity snapshots, commitments, and reliability outcomes atomically; restart, local multi-connection races, backward time, conflicting epochs, early expiry, forged fills, relay cap poisoning, cross-direction promotion/reset attempts, Sybil aggregate-cap attempts, fail-closed legacy migration, and raw-identity persistence are adversarially tested. The pinned runtime also passes verified backup/fresh restore, corruption and unknown-schema refusal, SIGKILL/WAL recovery, version migration, actual bounded-filesystem exhaustion without partial state, authenticated solver response faults, and capacity-reader faults. Bonds remain deliberately disabled until objective failure adjudication exists. Production must deploy the capability endpoint and reader protocols with independent operators plus the solver daemon, deploy the reviewed shared persistence service, and test deployed-volume recovery plus real multi-instance races; the local SQLite, transport, and reader proofs are not distributed deployment evidence.

### TS-M04 — Hidden routing and stale capacity

**Status:** Signed routing cap, exact output, three-key short-lived capability binding, authenticated endpoint challenge/response, fresh independently observed capacity boundary, atomic commitment accounting, reliability suspension, and fresh-authorized fallback implemented; deployed endpoint/readers and routing telemetry remain deployment gates

Every solver offer binds the exact Lightning output, `maxRoutingFeeSats`, short expiry, and its own signed capacity epoch. Offers above the user's routing cap or from a stale epoch are rejected. `verifySolverCapability` binds the EVM solver, direction-specific escrow domain and runtime code hash, recovered LND node, HTTPS origin, Ed25519 endpoint key, exact capacities, monotonic epoch, and expiry. It accepts no caller-supplied verification flag and refuses signed self-report unless independently read BIT inventory and Lightning capacity are fresh, belong to the bound identities, and cover the declaration. The official capability client accepts no caller-supplied endpoint transport, clock, entropy source, or Lightning verifier. The official multipath RFQ client likewise freezes its exact roster and policy and owns its pinned public Node HTTPS transport, system clock, cryptographic challenges, and lifecycle cancellation. The fixed one-shot production calls also reject injected dependencies; only explicitly test-named calls and a separately branded test client accept them. A caller abort or permanent RFQ-client shutdown cancels every path and cannot return a partial selectable collection. Copied clients, extracted methods, and closed clients reject. A deployment-facing owner accepts only that production provenance, takes a one-use lease only while no earlier direct collection is active, blocks retained client handles from collecting or closing outside the service, binds both deployment and call cancellation, prevents a client from being shared between services, and emits identifier-free non-authorizing aggregate status. It has no network listener; its test factory and local simulated paths are not deployment evidence. Local recovery proves node-key possession, not LND channel-graph activity or liquidity. Only the successful in-process verifier result can create the exact executable-quote binding; cloned objects and mutated, re-signed capability, snapshot, endpoint, runtime, epoch, or inventory fields fail closed. The release-time funding predicate repeats that provenance check and refuses the exact proof once its capacity is stale, its expiry is reached, or its chain, direction-specific escrow, or runtime hash differs from the live activated release. `queryVerifiedSolverCapability` adds a fresh random request challenge, exact response echo, Ed25519 response authentication, short authority windows, canonical public HTTPS, DNS/IP pinning with TLS hostname preservation, no redirects, hard timeout, and bounded response parsing before admission. The BIT reader compares canonical finalized vault state across two independently labeled/function-distinct providers, pins exact runtime/proxy/implementation hashes and vault accounting, and applies a reserve. The Lightning protocol binds a separate observer signature to the request, capability, direction, node, epoch, reserve, budget, and aggregate active-channel/in-flight state without returning channel identifiers. The latter authenticates a private-state observer but is not a publicly verifiable balance proof. The admission policy persists the capability expiry, subtracts active commitments from fresh capacity before releasing a firm signature, bounds active firm quotes, caps unknown BIT → Lightning exposure, and suspends repeated solver-attributable failures. The UI calls the result a quote—not guaranteed channel capacity—and `fallbackAuthorization` rejects a different solver/offer until the user authorizes the new exact terms. Production must still deploy the endpoint and readers with independent operators, run the branded RFQ service under their real process/orchestrator lifecycle, reconcile channels and in-flight HTLCs continuously, and measure cancellation plus routing failures across independent testnet solvers before funding. [Solver endpoint protocol](./SOLVER_ENDPOINT.md) and [authenticated multipath RFQ delivery](./RFQ_DELIVERY.md) record these trust boundaries.

### TS-M05 — Mempool preimage disclosure

**Status:** Resolved by immutable beneficiary binding in both escrow directions

Anyone, including an unrelated mempool bot, may relay a valid preimage, but both escrow directions transfer only to the beneficiary fixed in the signed quote before Lightning authorization. Tests claim from an attacker address and prove that it receives zero while the bound beneficiary receives the exact payout. The beneficiary cannot be mutated after signing, and the shared registry prevents reuse in the other direction. Private transaction submission may improve inclusion reliability but is neither trusted nor required for theft resistance.

### TS-M06 — Invoice substitution and malformed invoice data

**Status:** Independent bounded raw BOLT 11 decoding, exact invoice-digest binding, and isolated live standard/hold LND integration implemented; deployed composition and independent review remain deployment gates

`lib/bolt11.mjs` never accepts caller-decoded authority. It bounds the raw string and tag count; verifies Bech32, padding, tag lengths, compact secp256k1 signature, explicit or recovered payee, amount multipliers, UTF-8, singleton tags, BOLT 9 required features, route hints, and fallback structure; and retains no route-hint contents. `validateFullFillInvoice` then validates the exact mainnet network, invoice digest, payment hash, whole-satoshi amount, capability-bound payee for solver invoices, nonzero payment secret, expiry, final CLTV, supported required features, route-hint bound, and inline-description policy. A user invoice fails before private solver disclosure; a solver invoice fails before the second user authorization. Amountless, ambiguous, mutated, duplicate-tag, stale, high-S-with-explicit-payee, unknown-required-feature, and oversized invoices fail closed. Hashed descriptions stay disabled until TreeSwap can authenticate their preimage. BOLT 11 does not expose hold/standard creation kind, so TreeSwap does not trust such a field; its reference solver retains local `AddHoldInvoice` evidence, while the cross-party invariant requires conservative deadlines and finalized EVM state before Lightning action. Both EIP-712 quote shapes bind `invoiceDigest` and `paymentHash`, and the isolated Lightning adapter rechecks those fields with LND before an RPC. Exact standard and hold paths, routing and amount rejection, terminal no-route behavior, duplicate isolation, expiry, wrong/late preimage, cancel, accepted-state restart, and both lost-response paths pass regtest. Deployed adapter identities, composed deployment evidence, and independent review remain required.

### TS-M07 — Data and UI injection

**Status:** Resolved with canonical signed identifiers, bounded text-only displays, structured logs, and browser response controls

Invoice descriptions, solver names, token metadata, and memos are untrusted strings. Escape them in every interface and log, cap length, avoid rendering arbitrary markup, and never treat invoice text as instructions.

Signed relay identifiers now fail unless their original bytes are already canonical; they are never rewritten into a possibly colliding identity. Display-only labels are Unicode-normalized, stripped of invisible and bidirectional controls, forced onto one line, and length-capped. React renders dynamic values as text, audit helpers emit one bounded JSON record, and no invoice text is passed to an instruction or authorization path. Audit field data cannot replace the trusted event label, accessor properties are skipped without invocation, normalized key collisions cannot overwrite an earlier field, and prototype-named keys remain own serialized data instead of mutating or disappearing into the record prototype. Only primitive events and values may be rendered; object-shaped values are skipped and an object-shaped event maps to `unknown` without coercion. CSP, anti-framing, no-sniff, referrer, cross-origin, and browser-permission headers are defined for both Next.js/Vercel and the static worker output. Adversarial tests cover markup, Unicode controls, oversized values, canonicalization collisions, log-line forgery, event-label substitution, accessor or coercion side effects, and prototype-named fields. See [Untrusted text boundary](./INPUT_HANDLING.md).

### TS-M08 — Privacy leakage

**Status:** Two-stage minimum-disclosure policy implemented; production storage deletion remains a deployment gate

A public intent can expose payment hashes, amounts, timing, Ethereum addresses, and Lightning route hints. Publish the minimum needed to price an intent; reveal the full invoice only to the reserved taker, minimize retention, and document the cross-network linkage created by settlement.

Quote discovery now has an explicit blind request projection: an unlinkable pricing identifier, direction, exact output and unit, chain, caps, capacity epoch, and short expiry. Its assertion accepts exactly those nine enumerable own data fields with canonical primitive types and direction-matched output units. Unknown, symbol, prototype-named, accessor, nested, non-canonical, and disguised private fields reject without reading property getters; privacy therefore does not depend on a recursive blacklist. The authenticated coordinator quote book is never a browser payload: a separate client projection strips solver and offer identities, delivery paths, signatures, capacity commitments, receipt details, and rejection data, exposing only exact economics and committed received-set/risk metadata under fresh opaque choice IDs. One same-process session can resolve one choice back to the original verified book, then erases its map; it has no fund, settlement, or listener authority. The remaining authenticated HTTP ingress and durable session store must preserve that provenance and enforce replay, rate, and retention controls before deployment. Only the chosen solver may receive the private settlement packet, and only through an authenticated, encrypted channel bound to that solver. Audit projection separately folds Unicode compatibility forms, case, and separators before redacting sensitive key names, while executable retention deadlines cover pricing data, unselected quotes, private packets, pending email, and minimal receipts. The exact amount is necessarily disclosed for executable pricing, and final settlement remains linkable; the product makes no anonymity claim. See [Privacy boundary](./PRIVACY.md). A production database must enforce and evidence deletion before live swaps open.

### TS-M09 — Governance capture

Active and retained-recovery job preparation snapshot each daemon-evidence policy as bounded exact data before any digest or authority comparison. Accessors are never invoked, post-snapshot mutation is inert, and hidden, symbol, inherited, prototype-key, custom-prototype, unsafe, executable, cyclic or oversized, sparse, and decorated inputs reject. This removes the clone-time getter and field-erasure gap, but it does not attest that a deployment-supplied policy is truthful or independently governed.

**Status:** Resolved in contract architecture, strict deployment policy, finalized receipt verification, and a local closed-deployment rehearsal; release binding, live multisig deployment, independent review, and monitoring remain launch gates

Restart recovery no longer depends on an in-memory funding context surviving an incident. A separate recovery-only activation reopens the exact raw release and approval evidence, rereads current canonical code, proxy implementation, wallet ownership, registry membership, token identity, and reconciled escrow balances through the candidate-bound provider quorum, and accepts closed/emergency/paused or below-reserve state only because it can create no new exposure and can never plan or dispatch Lightning. Schema v9 permanently binds each accepted settlement—before reservation or action—to one unique firm offer, that offer's historical solver capability, the reviewed release and market-risk policy, and the exact daemon-evidence policy. A fresh rotated capability for the same solver may recover the old record, but cannot rewrite its authority. Copies, cross-release or cross-policy replay, method substitution, late binding, duplicate selected offers, unsafe legacy migration, and recovery attempts at either Lightning action stage fail closed. Per-action EVM or terminal work still requires live leadership and fresh dual-signed evidence. The retained-release gate now derives a complete liability commitment from the database, rejects unbound settlements or firm offers, rebuilds every raw old-release input from private immutable storage, requires fresh provider/recovery-key proof on a distinct restored host and process, and blocks nonzero-liability rotation unless separately witnessed old/new actions cover the unchanged live snapshot under a closed gate. See [Retained-release custody and rotation](./RETAINED_RELEASE_CUSTODY.md). The packaged coordinator now has a revoking recovery verifier, an embeddable bounded action loop, a fail-closed execution lifecycle supervisor, and a reusable same-process outer bootstrap. The loop cannot accept raw or network-delivered settlement jobs: a one-use same-process proof derives and commits every field of the complete fixed startup set from the exact restored database and retained readiness, recursively freezes policy/runtime inputs, and fails on omission, injection, copying, reuse, concurrent activation, store or method substitution, settlement drift, stale authority, or more than 64 liabilities. An uncopyable lease ensures only one active loop controls a restored release and remains held until an in-flight cycle exits after stop. The loop reacquires all same-process authority each cycle, executes one step per settlement, and uses a stop-revocable fence at side-effect boundaries. The lifecycle independently refreshes activation and health, reports bounded existing-liability EVM recovery without mislabeling it as globally dispatch-free, fails closed on background or lease loss, and drains the loop before outer resource release. The outer bootstrap acquires the original lease/store/verifier, publishes an unhealthy authority-free preparation status, and rechecks verification plus ownership before handoff. The reviewed recovery operator launcher supplies its only accepted preparation callback: one immutable one-use configuration fixes the private custody path, release, restored host/process, authenticated capability clients, recovery-only evidence controls, packet provider, Lightning reconciliation adapter, EVM signer, and reconciliation providers. It repeatedly proves lease ownership, inspects custody, refreshes every solver possession proof, matches solver/direction/chain/escrow/code/policy, derives readiness from the current same-process activation, and constructs the exact database job set. Its evidence object has no Lightning-authorization method; active controls, copies, accessors, extra or prototype-named fields, nested runtime prototype keys, substitution, duplicate policies, cancellation, lost lease, and reuse fail closed without prototype mutation. Timeout, signal, or failure revokes verification before drain; only then may the store close and lease release. Action health and successful cycle digests also bind the verified release ID, release-record digest, and one-use job-set digest; a valid status from another recovery set cannot be replayed as current health. Status contains only aggregate outcomes. The default entrypoint rejects recovery execution because configuration cannot supply same-process custody and runtime provenance. Local fixtures do not prove real custody or independence, so deployed retained-custody restoration, runtime wiring, operators, providers, persistence, restart/retention/rotation actions, and incident drills remain mandatory. See [Recovery operator entrypoint](./RECOVERY_OPERATOR_ENTRYPOINT.md).

Paused BIT is observation-only in this mode: claim preparation and broadcast remain closed until a later fresh provider quorum observes the reviewed token unpaused. Gate closure, emergency halt, reserve shortfall, or cap excess do not block claims by themselves because the immutable escrows leave exits open, but every runtime/code/accounting check and action-specific approval remains mandatory.

An admin that can instantly change fees, upgrade escrow, redirect a treasury, or pause exits can steal or trap value. Prefer an immutable escrow; otherwise use a multisig, timelock, public change events, hard fee caps, and a pause that blocks only new opens/reservations.

Both escrows are immutable, non-upgradeable, have constructor-fixed fee collectors and limits, and expose no administrator. The shared registry becomes irreversibly sealed to exactly two escrows. The open gate rejects EOAs, a shared controller/guardian, reopen delays under 24 hours, and open windows over seven days. Its public events and automatic expiry cover every state transition, while halt affects new exposure only. A deployment manifest fails closed unless policy-matched source/review/code digests, exact unique owner lists, quorum separation, 2-of-3-or-stronger contract wallets, registry seal, immutable escrow topology, BIT configuration, reference/price limits, fee caps, and finalized zero-balance accounting all match. The v2 RPC observer reconstructs vault available/locked/accounted inventory, user-escrow liabilities, and both raw BIT balances at the same canonical block and rejects promotion unless all reconcile to zero. The closed-Sepolia workflow forces a fresh offline build from the exact clean published commit and derives four deterministic unsigned deployments plus three hash-linked registry actions; its local rehearsal executes that exact generated plan, deliberately fails production policy, and carries no funds. A short-lived preflight anchors nonce, address vacancy, wallets, and BIT before execution. A separate signed postflight reconstructs every exact creation and Safe receipt inside that window, checks successful Safe and registry events, proves the isolated final nonce, and requires two providers to agree on the finalized empty state; the retained provider/operations identities and a new contract reviewer all attest it. Promotion v2 re-verifies that exact signed package. A separate five-role review verifier requires distinct reviewer identities, signers, and organization commitments to sign the same source, protocol, deployment, reports, complete finding dispositions, and validity interval; open findings and accepted critical/high risks fail closed. A separate five-role operations verifier requires complete drills, the exact public adoption policy, the exact safety-monitor policy and isolated confirmer assignments, plus alert, backup, monitoring, reconciliation, solver, and qualification artifacts. Release candidate v6 derives every review and operational hash from those live provenance boundaries, additionally requires the original qualification artifact and its separate review provenance, retains the exact adoption-, safety-monitor-, and confirmer-binding digests, and release authorization plus runtime funding bind the resulting evidence. None of these boundaries establishes real-world organizational independence, reviewer competence, drill truth, or artifact truth, and none can sign, broadcast, open, or fund by itself. See [Governance and deployment boundary](./GOVERNANCE.md), [Independent review evidence](./INDEPENDENT_REVIEW_EVIDENCE.md), [Qualification review evidence](./QUALIFICATION_REVIEW_EVIDENCE.md), [Adoption policy boundary](./ADOPTION_POLICY.md), [Operational readiness evidence](./OPERATIONAL_READINESS_EVIDENCE.md), [Closed public-testnet deployment plan](./CLOSED_TESTNET_DEPLOYMENT.md), and [Signed closed-deployment postflight](./CLOSED_TESTNET_DEPLOYMENT_POSTFLIGHT.md). Live wallets, hardware signers, independently operated providers, monitoring, and actual review evidence do not yet exist.

### TS-M10 — SIWE replay, phishing, or session theft

**Status:** Resolved for EOA account metadata and fail-closed deployment capability; owner-only D1 lifecycle and one real expired-nonce maintenance pass live, while scheduled all-record operations and independent identity/privacy review remain launch gates

A generic wallet signature, reusable nonce, mismatched domain, or readable session token can let an attacker impersonate a wallet account. The current account surface cannot move funds, but notification data and any later private swap history still require protection.

TreeSwap uses the EIP-4361 plaintext format with an exact no-transaction statement. The server generates a random 128-bit nonce, binds it to an allowlisted HTTPS origin, exact domain and URI, Ethereum mainnet, issue time, and exact ten-minute expiry, and atomically consumes it once. Optional resources and mutable scope fields fail closed. The client rechecks address and chain after signing. A random 256-bit session is stored only by hash, replaces prior wallet sessions through one transactional D1 batch, and expires in 24 hours. Its cookie is `HttpOnly`, `Secure`, `SameSite=Strict`, host-only via the `__Host-` prefix, and scoped to `/`; account mutations require the exact origin and all auth responses disable caching. A deployment enables the UI and nonce path only when its database binding proves the exact nonce, session, and notification schema is present; missing, partial, malformed, or failed storage returns a fixed disabled capability before any signing prompt. Routine requests no longer sweep global tables. The owner maintenance action requires the exact private origin, complete storage, and an active SIWE session, then uses one transactional batch to delete at most 100 already-expired records per table and returns aggregate counts only. Credentialed owner-only exercises retain none of the wallet, nonce, message, signature, cookie, credential, email, or raw row data. Contract-wallet SIWE remains disabled until chain-aware EIP-1271 verification and session invalidation are available. Deployed database access controls, monitoring, backup/restore, scheduled purge evidence, and independent identity/privacy review remain required. See [Authentication boundary](./AUTHENTICATION.md), [Live account evidence](./LIVE_ACCOUNT_EVIDENCE.md), and [Account maintenance evidence](./ACCOUNT_MAINTENANCE_EVIDENCE.md).

### TS-M11 — Email correlation, spoofing, and unwanted delivery

**Status:** Risk contained by hard-disabled delivery, immediate detach, and 24-hour pending retention; verified delivery is excluded from this release

Attaching email creates a durable correlation between an Ethereum address and an offchain identity. Accepting an unverified address can also send unwanted messages to a third party.

Email is optional, omitted from SIWE and swap intents, stored separately, and immediately detachable. Invoice and receipt consent are independent. Every new or changed address returns to `pending` and receives an exact 24-hour deletion deadline. An authenticated read deletes only that wallet's expired record; bounded owner maintenance can delete at most 100 expired notification records per invocation. There is no continuous scheduler, mail provider, or send path, and the policy hard-denies delivery even if a caller presents nominal future controls. A reviewed code change must add and jointly enforce ownership verification, unsubscribe, per-wallet and per-address rate limits, minimal retention and scheduled deletion, access auditing, and authenticated sending before delivery can exist. Email is not a dependency for swaps or direct sends. See [Optional email boundary](./EMAIL.md).

### TS-M12 — Direct-send substitution or confused authorization

**Status:** Resolved as a wallet-delegated boundary with one-shot frozen dispatch and unknown-outcome protection

A clipboard attacker, injected provider, stale tab, or last-second account/network change can substitute a BIT recipient or Lightning invoice. A user may also mistake a harmless SIWE login signature for payment authorization, or assume a direct payment has bridge escrow and refund protection.

The direct-send client keeps authentication and payment authorization separate, freezes canonical destination and amount data in a dedicated real-funds review, and requires a second wallet action. BIT sends bind chain, expected contract, runtime-code hash, sender, recipient, and integer amount; recheck code, account, chain, token settings, pause, and balance; simulate and estimate the exact `transfer`; and reject a returned transaction unless its hash, sender, target, calldata, and zero-ETH value match. Only a successful receipt for the same hash becomes confirmed. Lightning sends accept only amount-bearing mainnet invoices with whole-satoshi amounts and pass the exact frozen invoice to the wallet; opening a wallet is never payment, and a WebLN result is explicitly only the wallet's report. Both paths have a one-shot dispatch guard. Any ambiguous error after dispatch becomes “status unknown” with a check-before-retry warning, preventing an automatic duplicate. The UI states that direct sends are irreversible and have no solver, bridge fee, escrow, or refund protection. Users must still verify the complete destination and amount in a trusted wallet because a web client cannot make a compromised provider safe. See [Direct-send boundary](./DIRECT_SEND.md).

## Contract invariants

A production escrow test suite should prove at least:

1. BIT out never exceeds BIT deposited for an escrow.
2. `CLAIMED` and `REFUNDED` cannot both occur.
3. Only a pre-bound beneficiary receives a claim.
4. A claim requires `sha256(preimage) == paymentHash`.
5. A payment hash and maker nonce are consumed at most once.
6. Applied fees never exceed the signed caps.
7. No fee is collected from an unmatched or expired intent.
8. Claims and refunds remain available when new intent creation is paused.
9. Reservation expiry cannot shorten the absolute refund safety window.
10. Every amount conversion is deterministic and conserves value modulo documented dust.
11. A BIT transfer must produce the exact expected escrow balance delta.
12. An implementation-slot change in BIT prevents new reservations until reviewed.

## RFQ and quote-selection properties

1. The client compares only valid signed quotes for the same exact request.
2. Net output includes every mandatory fee in the same order and unit.
3. A relay cannot change a signed amount, beneficiary, hash, or expiry.
4. Cancelling or consuming a nonce prevents every later attempt to use it.
5. A quote cannot be accepted after expiry or beyond its executable quantity.
6. A quote is unavailable while BIT collateral or Lightning capacity is stale.
7. Selecting a fallback quote after a failure requires fresh user authorization when output changes.

## Required adversarial tests

- Stateful fuzzing of every escrow transition and signature field. **Complete for both local escrows: six invariants, 256 × 64-call campaigns per property, plus all-field digest tests.**
- Claim/refund transactions in the same block and around every timeout boundary. **Deterministic and contract boundary harness complete.**
- Preimage copied from the mempool by an unrelated account. **Complete: unrelated relayers receive no BIT in either direction.**
- Ethereum reorg after escrow creation and after claim. **Controlled local and pinned-live-BIT fork fault injection is complete; genuine public-testnet finality through independent providers remains.**
- Bitcoin block delay, LND restart, held HTLC timeout, and force-close. **Full-duration and rapid-block regtest fault injection is complete; deployed multi-operator repetition remains.**
- Replayed intent on another chain, escrow address, protocol version, and nonce. **Complete in local contract tests.**
- Replayed or mutated SIWE domain, URI, nonce, chain, issued time, and expiry. **Complete in the EOA policy harness.**
- Cross-origin session mutation, expired session use, notification access from a different wallet, and an absent or partial durable schema. **Complete in policy and worker-boundary tests; a credentialed owner-only lifecycle now covers live atomicity, while deployed access review, monitoring, backup/restore, and purge evidence remain launch gates.**
- Unverified-email delivery, preference bypass, unsubscribe failure, and wallet/email deletion. **Delivery is hard-disabled; authorization and 24-hour deletion policy tests complete.**
- BIT recipient or amount mutation between review and submission, account/network changes, paused or upgraded token behavior, failed transfer simulation, malformed invoices, WebLN rejection, and `lightning:` fallback falsely reported as paid. **Client policy and rendered-boundary tests complete; a compromised wallet remains outside the web client's trust boundary.**
- BIT pause and implementation upgrade while escrows are open. **Local hostile-token recovery plus pinned mainnet-fork pause, implementation-slot, and exact-delta tests complete; finality fault injection and independent review remain external gates.**
- Fee rounding across dust, maximum values, and thousands of small fills. **Complete.**
- Solver quote spam, cancellation, capacity exhaustion, and deliberate last-look failure. **Repository policy complete; persistent distributed enforcement remains a deployment gate.**
- Relay suppression, quote substitution, and stale fallback selection. **Substitution and fallback tests complete; suppression is disclosed and global-best is not claimed.**
- LP withdrawal while liabilities are reserved or in flight. **Solver-vault stateful invariant complete; live Lightning reconciliation remains a deployment gate.**

The full local campaign report is in [`contracts/test/fuzz/report.md`](../contracts/test/fuzz/report.md).

## Launch gates

### Prototype publication

- Clearly mark bridge flows as simulations and direct sends as real, irreversible wallet payments without bridge protection.
- SIWE may sign a login message, but it cannot authorize a payment; BIT sends request no approval and require their own transaction confirmation.
- Threat model and par-value caveat public.
- MIT license and reproducible build.

### Testnet

- Mainnet-fork escrow campaign, including BIT proxy changes and pauses.
- Regtest hold-invoice adapter and forced-timeout tests.
- Complementary BIT → Lightning escrow and direction-replay tests. **Local cross-chain execution and the credentialed pinned-live-BIT combined fork are complete; public-testnet integration remains.**
- EIP-1271 SIWE support if contract wallets are accepted.
- Verified email delivery, unsubscribe enforcement, retention limits, and abuse controls before sending mail.
- Monitoring for BIT proxy upgrades and pauses.
- No public LP deposits, order book, or rewards.

### Capped mainnet beta

- Independent contract and Lightning design review.
- Multisig/timelock, circuit breakers, caps, reconciliation, and incident runbook.
- Solver bonds and measurable fill reliability.
- Public risk disclosures and explicit loss allocation.
- Very small per-swap and per-epoch limits.

### Public liquidity

- Separate review of custody, LP share accounting, insolvency, withdrawal queues, adverse selection, and applicable legal obligations.

## Primary technical references

- [BOLT 11 payment encoding](https://github.com/lightning/bolts/blob/master/11-payment-encoding.md)
- [BOLT 2 HTLC peer protocol](https://github.com/lightning/bolts/blob/master/02-peer-protocol.md)
- [BOLT 4 final-hop validation](https://github.com/lightning/bolts/blob/master/04-onion-routing.md)
- [LND AddHoldInvoice API](https://lightning.engineering/api-docs/api/lnd/invoices/add-hold-invoice/)
- [LND macaroon permissions and revocation](https://docs.lightning.engineering/lightning-network-tools/lnd/macaroons)
- [EIP-712 typed structured data](https://eips.ethereum.org/EIPS/eip-712)
- [ERC-1967 proxy storage](https://eips.ethereum.org/EIPS/eip-1967)
- [0x firm RFQ lifecycle](https://docs.0x.org/docs/introduction/quickstart/swap-tokens-with-0x-swap-api)
- [BIT verified proxy and implementation](https://etherscan.io/token/0x57A447E4d5e18A9423408C365963A73F08B9d18C#code)
