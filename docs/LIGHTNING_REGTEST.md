# Lightning regtest lab

Status: reproducible two-node lab, bounded credential lifecycle, hold-invoice lifecycle faults, isolated signed-adapter smoke, live solver-node possession proof, and durable lost-response coordinator recovery pass locally. Combined EVM/Lightning and public-testnet evidence remain.

The lab uses immutable multi-architecture image digests for:

- Bitcoin Core `v31.1`;
- LND `v0.21.2-beta`; and
- two isolated LND nodes connected by a private 1,000,000-sat channel with balanced directional liquidity.

The Docker network is internal and publishes no host ports. Runtime RPC and wallet passwords are generated locally with restrictive permissions under ignored `.state` storage. They are regtest-only and must never be reused.

Manual lab commands keep their Docker volumes until explicitly destroyed and use a deliberately high 100,000,000-sat test-only daily ceiling so repeated fault campaigns retain replay journals without exhausting the day. The sealed qualifier is different: it destroys the named disposable regtest volumes before its first campaign and again on success or failure, so interrupted-run state cannot contaminate later evidence. This is not a production recommendation. Separate disposable adapters enforce a 10,000-sat live test ceiling, and deterministic journal coverage proves the exact UTC rollover. Production must publish a much smaller reviewed limit.

## Commands

```sh
npm run regtest:up
npm run regtest:smoke
npm run regtest:adapter-smoke
npm run regtest:credential-smoke
npm run regtest:credential-rotation-smoke
npm run regtest:tls-rotation-smoke
npm run regtest:invoice-fault-smoke
npm run regtest:policy-fault-smoke
npm run regtest:directional-capacity-smoke
npm run regtest:daily-cap-smoke
npm run regtest:stateless-init-smoke
npm run regtest:production-duration-smoke
npm run regtest:stale-chain-smoke
npm run regtest:unsynced-chain-smoke
npm run regtest:force-close-smoke
npm run regtest:route-fault-smoke
npm run regtest:htlc-cutoff-smoke
npm run regtest:solver-node-proof-smoke
npm run regtest:coordinator-smoke
npm run regtest:coordinator-invoice-smoke
npm run regtest:selected-solver-invoice-smoke
npm run qualify:local
npm run regtest:status
npm run regtest:down
npm run regtest:destroy
```

`regtest:destroy` removes only the `treeswap-regtest` containers, internal network, and Docker volumes. It preserves the ignored local runtime credential file. Use it when resetting the disposable lab; `qualify:local` invokes it automatically before and after the sealed campaign matrix.

`regtest:up` initializes both wallets without printing their test seeds, mines spendable regtest funds, opens and confirms the private channel, and bakes separate 24-hour credentials under distinct root-key IDs:

- observer: exact read-only node, channel, pending-channel, and balance RPCs;
- invoice: minimum node/channel health reads plus exact hold-invoice create, v2 lookup, settle, and cancel RPCs; and
- payer: minimum node/channel health reads plus exact decode, send-payment, and track-payment RPCs.

For all six node/role pairs, the bootstrap reads the baked macaroon back, requires the exact root-key ID, exact sorted URI set, and one bounded `time-before` caveat, and checks every granted URI against the pinned node's `ListPermissions` registry. It then requires representative wallet, invoice, payment-history, macaroon-administration, channel-balance, and network-graph commands to fail specifically with `permission denied`; a syntax or node failure cannot satisfy the test.

`regtest:smoke` creates a fresh preimage and 10,000-sat hold invoice on Bob, decodes and pays it from Alice, waits for `ACCEPTED`, settles only with the matching preimage, and requires `SUCCEEDED`. Its temporary payment result is mode-restricted and deleted after validation. The harness uses Bob's local regtest admin credential only to observe invoice state because `lncli lookupinvoice` targets the legacy lookup RPC; the isolated adapter campaign below uses only `LookupInvoiceV2` through its exact role credential.

`regtest:adapter-smoke` performs that lifecycle exclusively through the internal invoice and payer adapter processes. A local coordinator key signs exact 30-second authorizations; only its public key enters the adapter credential volumes. Each adapter verifies its pinned LND certificate, private-network hostname, role, signature, invoice, amount, hash, capacity epoch, live sync, active-channel liquidity, caps, and replay journal. After success, the campaign restarts the payer adapter and proves the exact request remains rejected, then proves the invoice adapter cannot execute a payer authorization.

`regtest:credential-smoke` first performs the exact six-role manifest and negative-authority checks. It then proves a disposable, two-second `GetInfo` credential works before its caveat and fails specifically as expired afterward. A second disposable credential works, its dedicated root-key ID is deleted, and the unchanged credential then fails while the admin node remains healthy. Test root keys and files are removed; no credential material is exported as evidence.

`regtest:solver-node-proof-smoke` bakes two disposable, time-bounded, exact-URI credentials under separate root-key IDs: Bob may call only `SignMessage`, while Alice may call only `VerifyMessage`. Bob signs four fresh exact `TreeSwap solver capability v1` challenges with LND's default double-hash mode. TreeSwap's local zbase32 compact-signature verifier and Alice's independent LND RPC must both recover Bob's exact compressed node public key. A mutated challenge must recover a different key locally and be invalid under Alice's graph-aware verification. Cross-role RPCs must fail specifically for authorization. Both credential files and root-key IDs are removed after the campaign. This proves local node-key possession and exact-message binding; it does not prove public endpoint deployment, current production channel activity, or liquidity, which remain separate reader and deployment gates.

`regtest:solver-capacity-smoke` asks the payer and invoice adapters for separately signed, fresh, direction-bound capacity observations. Each adapter verifies the coordinator request and exact solver, node, capability digest, epoch, direction, and expiry, then derives a capped aggregate from live active-channel directional balance, all pending HTLCs, configured reserve, and operator budget. Both directions return exactly the 100,000-sat test budget without exposing channel identifiers; unsigned, cross-role, mutated, replayed, stale, inconsistent, and key-reused observations fail closed in the live and adversarial suites.

`regtest:credential-rotation-smoke` bakes a replacement payer credential under a new root-key ID, verifies its exact permission and expiry manifest, and runs old and replacement adapters concurrently. Both decode the same exact invoice during the overlap window. The campaign then deletes only the old root key, requires both native LND and the old adapter to reject it, proves the replacement adapter remains available, and restores a fresh standard credential. Read-only authorization and transport failures are explicitly non-ambiguous because they cannot move value; value-moving unknown outcomes remain ambiguous. The initial run plus three consecutive warm-state repetitions pass, and cleanup leaves neither the temporary root key nor credential behind.

`regtest:tls-rotation-smoke` moves Alice's real LND certificate and key into recoverable node-local backups, restarts the pinned node so LND generates a new certificate pair, and explicitly reconnects the existing private peer. The new certificate fingerprint must differ while the node identity and exact channel-point set remain unchanged. The still-running old pinned adapter must reject an exact read-only decode non-ambiguously and native payment history must remain empty for that hash. A deliberate rollback then restores the previous pair and old adapter pin before the campaign rotates again. The final rollout copies the new public certificate, binds its observed fingerprint, recreates the adapters, and requires the same invoice decode to succeed. The backup pair is deleted only after recovery; an exit trap also restores it on unexpected failure. Four consecutive successful campaigns pass without dispatch or secret output.

`regtest:invoice-fault-smoke` proves an unaccepted hold invoice expires to `CANCELED` and rejects a late preimage. It accepts a second hold payment, rejects a wrong preimage without changing `ACCEPTED`, cancels it, confirms `CANCELED`, rejects an exact signed cancellation replay and a correct late preimage, and requires the payer to report failure. Finally, it restarts and unlocks Bob's LND while a third HTLC is `ACCEPTED`, requires the same invoice and channel to recover, settles with the bound preimage, and requires the original one-shot payer request—not a replacement payment—to finish `SUCCEEDED`. Payment-result files are mode-restricted and removed.

`regtest:policy-fault-smoke` submits valid invoices with an excessive fee limit and excessive amount, then requires adapter rejection and `NOT_FOUND` tracking proof that neither payment was dispatched. It also proves the tracking error contains neither the raw nor REST-encoded payment hash. It holds two real 80,000-sat HTLCs, reads at least 160,000 sats in flight from LND, rejects another exposure above the 150,000-sat cap, and cancels both probes. It then stops Bob, waits until Alice reports zero active channels, rejects a fresh payment, recovers and unlocks Bob, requires both channel views to become active, and again proves the rejected hash is unknown to LND. Finally, a disposable payer-adapter process must refuse a deliberately mismatched TLS pin while the unchanged pinned adapter still decodes the exact invoice. The campaign does not emit invoices, macaroons, or preimages.

`regtest:directional-capacity-smoke` rebalances the live private channel until Alice has less than a 100,000-sat payment in local capacity and Bob has less than the same amount in remote capacity. The payer adapter must reject outgoing payment exposure and prove `NOT_FOUND`; the invoice adapter must reject incoming hold-invoice exposure and native lookup must prove no invoice exists. The campaign then rebalances the channel, requires that exact previously rejected payment to succeed once, creates and cancels the previously rejected hold invoice, and restores the transferred balance. Three consecutive warm-state runs pass.

`regtest:daily-cap-smoke` gives disposable payer and invoice adapters separate 10,000-sat daily limits and durable journals. Each adapter opens exactly 10,000 sats of real exposure, restarts against the same journal, and rejects a fresh one-sat exposure specifically because the daily cap is exhausted. Read-only tracking proves the rejected payment is absent, native lookup proves the rejected hold invoice is absent, the successful payment is rebalanced, and the successful hold invoice is canceled. Deterministic journal coverage separately proves that value usage resets at the exact UTC-day boundary after restart while request-ID and payment-hash replay protection remain permanent. Four consecutive live runs pass.

`regtest:stateless-init-smoke` gives a disposable payer adapter a fresh, mode-`0600` chain-progress record. Startup's first real LND height/hash/header observation establishes only an untrusted baseline, so an exact signed 10,000-sat payment must fail closed and read-only tracking must return `NOT_FOUND`. Restarting against the same record cannot make that baseline trusted. Only after Bitcoin regtest mines a genuinely higher block with a new hash may the same exact exposure proceed, and native payment history must contain exactly one matching payment. The campaign rebalances the channel, removes its temporary state, and passes the initial run plus three consecutive warm-state repetitions.

`regtest:production-duration-smoke` uses the full 3,600-second local no-progress ceiling, never a compressed substitute. It first requires a higher real block and one successful, rebalanced payment. It then holds the exact LND and Bitcoin Core height/hash plus LND header timestamp unchanged for more than 3,600 wall-clock seconds while checking chain sync, wallet sync, active channel, adapter availability, observation cadence, and no block progress every 30 seconds. Halfway through, it recreates the adapter against the same durable state. The final exact payment must reject specifically for no observed chain progress, native history must remain empty for that hash, and a later block permits a read-only `NOT_FOUND` proof. The campaign separately raises only the reported header-age ceiling to 7,200 seconds so the test isolates the unchanged 3,600-second local-progress control. During a sealed qualification, the campaign now writes a generated, non-overwriting mode-`0600` companion record containing only the published source, whole-second timestamps, wall and monotonic duration, observation count, maximum observation gap, restart point, and fixed zero-block/zero-dispatch fail-closed results. The qualifier verifies that record, removes the companion, and embeds it under the digest of qualification schema v2; compressed timing, clock divergence, unsafe restart timing, interrupted cadence, too few observations, source drift, mutation, or a missing record aborts evidence. Ordinary standalone runs write no artifact. Clean published source `ccae7f05b4dbe8b082cc7880924717b781b20b6f` passed after 3,604 monotonic seconds, 119 continuous observations, and adapter recreation after 1,806 seconds; rejection was deterministic and native target-payment dispatch remained zero.

Clean published schema-v2 checkpoint [`69ef5991de0bc37cd8fbabd4dcdd2cb41fc9c498`](https://github.com/bobofbuilding/treeswap/commit/69ef5991de0bc37cd8fbabd4dcdd2cb41fc9c498), merged through [PR #47](https://github.com/bobofbuilding/treeswap/pull/47), passed 472 application/security tests with no skips, both production web builds, 68 contract tests, all 41 sealed local campaigns from `2026-08-23T01:32:29.378Z` through `2026-08-23T02:43:17.386Z`, and [main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32610593250), including all 100 pinned-runtime coordinator checks. GitHub deployment record `6043231932` binds exact READY production deployment [`dpl_2NvLKu4JcbTjj1EJ3B5D1FQvG8Ko`](https://treeswap-mpvyk82gr-bittrees-tech.vercel.app), whose generated URL is authentication-protected, to that source; [treeswap.vercel.app](https://treeswap.vercel.app) and [treeswap.bittrees.org](https://treeswap.bittrees.org) serve it publicly with HTTP 200. The ignored mode-`0600` schema-v2 artifact independently reconstructs to `sha256:3deb006f01bb8e0afed88c682202719a3baec1847470279646b06035b3c36ae7`; all 148 configuration hashes, 41 unique passed campaigns, three immutable image pins, exact clean published-`main` binding, privacy exclusions, and empty lab teardown reproduce. Its nested companion digest `sha256:715c20d849b2ae6159fbc5c3ffab3f1b9f3552ac435351bec9c50460651fdec7` binds 3,604 wall seconds, 3,603 monotonic seconds, 119 observations, a 32-second maximum gap, restart persistence at 1,815 seconds, deterministic rejection, zero guard-interval block advances, and zero target-payment dispatch; the temporary companion was removed. This is local-only evidence with simulated EVM reservation and no deployed Lightning monitoring, public testnet, independent providers or solvers, production coordinator, multisig, inventory, independent review, or funding authority.

`regtest:stale-chain-smoke` reads the pinned LND node's real `wallet_synced`, height, and `best_header_timestamp`, then gives a disposable payer adapter a read-only baseline observation. After a deliberate no-block interval crosses its compressed one-second no-progress limit, the adapter must reject an exact signed payment specifically because the same header made no local progress; read-only tracking through the normally configured adapter must prove the payment was never dispatched. Reported past age, locally observed no-progress time, and future skew are independent limits, so the baseline remains valid even when the newest honest block is already several seconds old. The normal adapter uses 3,600-second age and no-progress ceilings, regtest separately allows its synthetic timestamp up to 7,200 seconds ahead, and the same invoice remains decodable there. Repeated stale → force-close → stale sequences pass without emitting invoices, hashes, macaroons, or preimages.

`regtest:unsynced-chain-smoke` pauses the real Alice LND process, advances Bitcoin regtest by 500 blocks, and resumes Alice into a genuine catch-up state. The campaign observes `synced_to_chain=false` or `wallet_synced=false`, requires the adapter to reject an exact signed payment non-ambiguously, waits for both flags and the channel to recover, and uses read-only tracking to prove zero dispatch. The recovered adapter then decodes the same invoice. Three consecutive warm-state runs pass.

`regtest:force-close-smoke` unilaterally closes Alice's only active channel and requires both the exact signed payment and the aggregate close monitor to reject before dispatch while the channel is in LND's waiting-close state. The exact observer macaroon reads both `PendingChannels` and WalletKit `PendingSweeps`. Each v2 collector signs a time-independent commitment to its aggregate block/state, and the verifier requires both commitments to match inside the configured observation window; unit campaigns reject inconsistent healthy snapshots. The live campaign confirms the commitment transaction, reads the pinned node's actual CSV maturity, advances through that maturity, confirms the resulting sweep, and requires all pending-close, HTLC, and non-anchor exposure to clear. Only then does it open and confirm a fresh balanced private channel, prove the rejected payment is `NOT_FOUND`, and decode the same invoice through the recovered adapter. Pinned LND may retain confirmed 330-sat commitment anchors only within the published count, aggregate-value, maturity-age, retry, budget, and deadline bounds; exceeding any bound is unsafe. See [Lightning close and sweep monitor](./LIGHTNING_CLOSE_MONITOR.md). Independent deployed collectors and actual operator-node recovery evidence remain open.

`regtest:route-fault-smoke` starts a third synced LND node with no channels and obtains a standard 10,000-sat invoice from it. Alice's healthy adapter dispatches exactly once and must receive terminal `FAILED` with `NO_ROUTE`. Pinned LND may subsequently track that attempt as the exact bound `FAILED` payment or return non-ambiguous, hash-redacted `NOT_FOUND`; neither observation permits another send. The exact authorization replay and a fresh authorization that reuses the payment hash must both fail at the adapter. A lab-only administrative count confirms LND still has exactly one matching payment record. The zero-state campaign and five consecutive warm-state campaigns pass.

`regtest:htlc-cutoff-smoke` accepts a real hold payment with an 80-block final CLTV, derives its actual HTLC expiry height, and mines enough blocks rapidly for both LND nodes to reach exactly the configured 24-block reserve. The invoice must still be `ACCEPTED`; the adapter must reject even the correct preimage specifically because the HTLC is inside its settlement margin; cancellation must release the original payer as failed. An earlier trial showed pinned LND auto-canceling at 18 blocks, which is why the TreeSwap reserve was raised to 24 rather than relying on the node's terminal boundary.

`test:cross-chain-deadlines` binds live standard and hold invoices to the actual TreeSwap user escrow and solver vault on an isolated loopback Anvil chain. Both escrow transactions reach twelve simulated confirmations before the corresponding Lightning action. BIT → Lightning pays the live invoice and uses its exact transient proof to claim before refund. Lightning → BIT reaches the accepted HTLC's exact 24-block boundary, rejects settlement, releases the payer, proves the EVM refund is still unavailable, and then proves claim/refund mutual exclusion at the exact EVM timestamp. Pinned LND added three blocks to the advertised 80-block final CLTV on the observed route; the campaign records the actual 83-block delta, never extends the signed deadline, and rejects a shorter-than-policy HTLC. Its final evidence is privacy-safe and scoped `local-dual-chain-no-funding-authorization`. Mock BIT and simulated local finality mean this does not replace the pinned live-BIT-fork or public-testnet campaign.

The clean published qualification bound to `ccae7f05b4dbe8b082cc7880924717b781b20b6f` passed this and all 34 local campaigns from `2026-08-20T19:45:37.092Z` through `2026-08-20T20:54:09.927Z`. Its independently reconstructed local-only digest is `sha256:7bc2988a39081d511a46abb7e27e2160e5dceabe23354e6faeeab14c0381f9ec`; the mode-`0600` ignored artifact grants no funding authority.

`regtest:coordinator-smoke` rebuilds and uses a separate coordinator container and credential volume so stale local images cannot satisfy the campaign. It has the Ed25519 private key and its own SQLite volume but no LND macaroon. The payer adapter has only the public key and its payer macaroon. The campaign obtains a real standard 10,000-sat invoice through the authenticated private-packet client, lets the bounded daemon plan and independently authorize the exact payment, discards the successful response, reopens the durable store in `UNKNOWN`, and uses bounded fresh signed read-only tracking to recover `SUCCEEDED` plus the exact bound preimage. It restarts again and re-reads the same confirmed proof so claim construction remains restart-safe. A transient non-ambiguous `NOT_FOUND` remains unresolved and can only trigger another read; it never permits another send. The campaign requires one dispatch and proves SQLite, WAL, and shared memory contain neither the raw invoice nor raw/textual preimage. The reservation input and finality authorization are simulated, so this is not EVM finality or independently deployed provider evidence. Its deterministic evidence digest is `0x5cdd5ea84c923e4b45a67246010801db9d4429b7f309c1d15ef2520359b3310c`.

`regtest:coordinator-invoice-smoke` gives the same credential-free coordinator a transient matching preimage for a real accepted 10,000-sat hold invoice. It settles through the invoice adapter, discards the successful response, reopens in `UNKNOWN`, and recovers `SETTLED` with one preimage-free signed lookup. It requires one dispatch and scans the closed SQLite database, WAL, and shared-memory files for both the raw and textual preimage. The original payer must succeed, and no replacement action is issued. Its reservation input is also simulated.

`regtest:selected-solver-invoice-smoke` gives a separate hardened process one owner-controlled 32-byte payment-secret file and a new LND root-key ID whose macaroon contains only `AddHoldInvoice` and `LookupInvoiceV2`. It derives one request-bound payment hash, creates a real 10,000-sat hold invoice with a 3,600-second expiry and 80-block final CLTV, recreates the core from the retained key, and requires the exact hash, invoice, digest, and add index. The independent TreeSwap decoder then verifies the transient raw invoice's Bech32 checksum, compact signature, regtest network, exact amount/hash/digest, nonzero secret, expiry, CLTV, and required features before emitting only the cleanup hash. The same credential cannot cancel, settle, list invoices/payments, inspect balances, or administer the node; cleanup uses separate lab authority. Pinned LND returns two different missing-invoice REST status/code pairs depending on database history, so the shared transport reduces recognized remote failures to a non-sensitive reason enum and exposes no remote message.

`regtest:selected-solver-invoice-private-service-smoke` exercises the actual private service and production client. Separate volumes ensure the service has only the narrow LND/payment-secret bundle, provider private key, requester public key, and TLS private key, while the client has only the requester private key, provider public key, and pinned public certificate. One signed request creates and durably commits a real 10,000-sat invoice; the campaign hard-kills the service process, restarts against the same SQLite and credential volumes, and requires a fresh authenticated request to receive the byte-identical signed response while LND still contains exactly one matching invoice. Cleanup again needs separate authority. Independent BOLT 11 validation, key-rotation/backup/full-volume drills, public testnet, deployment, and review remain open.

## Local qualification evidence

`npm run qualify:local` requires a clean `main` whose commit exactly matches a fresh `ls-remote` read of the canonical HTTPS origin's `refs/heads/main`. It destroys the exact disposable `treeswap-regtest` containers, network, and volumes before the matrix, then reruns lint, both web builds, all application/security tests, contract formatting and tests, the EVM coordinator fault campaign, and every Lightning campaign above, including both selected-solver invoice-material campaigns. A `finally` boundary destroys the lab again on success or failure. Any initial or final destruction failure aborts evidence, so interrupted state cannot seed a later checkpoint.

On success it writes one new mode-`0600` JSON record under ignored `outputs/`. The record contains only the published commit, UTC start/finish times, runtime versions, immutable external image identifiers, SHA-256 configuration hashes, campaign names/pass states, explicit limitations, and its own deterministic digest. It contains no command output, environment value, path, invoice, payment hash, preimage, macaroon, RPC URL, private key, wallet seed, or email. The command refuses a dirty/unpublished tree, mutable external image, failed campaign, unsafe filename, symlinked output directory, or overwrite. This local record is evidence—not funding authorization, independent review, public-testnet evidence, or a deployment manifest.

Hermetic-regtest checkpoint [`f74b86c802cd0f1b510f9d47637c130f25dbb546`](https://github.com/bobofbuilding/treeswap/commit/f74b86c802cd0f1b510f9d47637c130f25dbb546), merged through [PR #37](https://github.com/bobofbuilding/treeswap/pull/37), completed all 41 sealed campaigns from `2026-08-22T17:17:17.951Z` through `2026-08-22T18:26:28.077Z`. The production-duration stage passed after 3,603 monotonic seconds, 119 observations, midpoint restart persistence, deterministic rejection, and zero dispatch. It then passed stale-header rejection, a genuine 500-block catch-up, force-close/CSV sweep/fresh-channel recovery, route and replay faults, the 24-block HTLC cutoff, both escrow directions, solver proof/capacity/admission, and both lost-response paths. Its ignored regular mode-`0600` evidence independently reconstructs to `sha256:5d86008863e611b3f58a4a97ff4ecabbc193fb2413ca1923d5a716ad9179d724`; all 138 configuration hashes, 41 unique pass states, three immutable image pins, exact published source, privacy exclusions, and initial/final empty-lab destruction reproduce. [Main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32587271070) also passed. The artifact remains local-only with simulated EVM reservation and no operator-node monitoring, public testnet, production infrastructure, external review, inventory, or funding authorization.

Published checkpoint `794b9f3bedb21dd6fe39dae0d7a10a5e94289899` completed all 25 local campaigns from `2026-08-19T23:16:42.445Z` through `2026-08-20T00:23:31.248Z`. Its independently rebuilt evidence digest is `sha256:985eb9ff7eb610982edfa27088a731b6eabd277add22bfe0a7dabf6d61aba825`; the matching [hosted security-and-build run](https://github.com/bobofbuilding/treeswap/actions/runs/32312491429) also passed. The bundle includes the new live solver-node proof and schema-v4 capability-expiry boundary. The hosted coordinator job directly passed all 27 admission, recovery, and solver-capability tests inside the pinned Node `22.22.0` image. This is a reproducible local checkpoint, not the final release artifact, deployment evidence, or funding authorization.

Authenticated-endpoint checkpoint `f474e9c577f9c4e70183275f693ce89216e24032` completed all 25 local campaigns from `2026-08-20T00:43:19.448Z` through `2026-08-20T01:49:47.891Z`. Its independently rebuilt evidence digest is `sha256:55170d90bb3509ca5c0f133327290863d158f2cfa6e0401178a7b8ce3d523b3a`; the matching [hosted security-and-build run](https://github.com/bobofbuilding/treeswap/actions/runs/32318279120) also passed. The hosted coordinator job directly passed all 36 admission, recovery, capability, and endpoint-transport tests inside the pinned Node `22.22.0` image. The endpoint transport configuration hash is part of the evidence. This remains local evidence, not a deployed endpoint, capacity-reader proof, or funding authorization.

Capacity-reader checkpoint `67655f859ec70c191501d073e75cba808ce06def` completed all 26 local campaigns from `2026-08-20T02:15:55.025Z` through `2026-08-20T03:23:02.372Z`. Its independently rebuilt evidence digest is `sha256:0c20cb3ea69ca7eb56ed5f79b215ad317523908ee09436ac4203966c90ac3d58`; the matching [hosted security-and-build run](https://github.com/bobofbuilding/treeswap/actions/runs/32323948108) also passed. The hosted coordinator job directly passed all 37 admission, recovery, capability, endpoint-transport, and capacity-reader tests inside the pinned Node `22.22.0` image. This remains local evidence, not an independently operated deployment or funding authorization.

Daemon-recovery checkpoint `e860eabbfb188d3597df25ee8dfa14001126026e` completed all 26 local campaigns from `2026-08-20T03:38:59.074Z` through `2026-08-20T04:45:56.472Z`. Its independently rebuilt evidence digest is `sha256:65a86a92f7f93407df0cc8254e61cf3740fda2aaf9f76d663c3d9d49e8895419`; the matching [hosted security-and-build run](https://github.com/bobofbuilding/treeswap/actions/runs/32328893965) also passed. The record covers the uncompressed production-duration delay and live payer recovery across two coordinator restarts with one dispatch and no persisted invoice or preimage. It is mode `0600`, ignored, secret-free local evidence—not a deployed Lightning service, public-testnet proof, independent review, or funding authorization.

Authenticated-runtime checkpoint `ac08946d962d062845d585a716c829c0ad73a4a0` completed all 26 local campaigns from `2026-08-20T05:05:56.878Z` through `2026-08-20T06:12:48.897Z`. Its independently rebuilt evidence digest is `sha256:dc6f60bacfa32332eb90bba0b75a879e35022f1cb473cba17241f225feb5450d`; the matching [hosted security-and-build run](https://github.com/bobofbuilding/treeswap/actions/runs/32334233367) also passed. The payer recovery now traverses the authenticated private-packet client and bounded daemon before deliberately losing the adapter result; after restart it reconciles read-only with dispatch count one and no invoice or preimage in SQLite, WAL, or shared memory. The record remains local-only evidence with a simulated EVM reservation—not an independently deployed packet provider or finality observer, public-testnet proof, independent review, or funding authorization.

EVM-outbox quorum checkpoint `cd2a81c0f5ecc7ab1902a1fa576d71f4c7520509` completed all 27 local campaigns from `2026-08-20T06:32:18.096Z` through `2026-08-20T07:39:19.663Z`. Its independently rebuilt evidence digest is `sha256:f0e362260e813c3880b4328800a074c8f57df1459ec6b37a045369a718d17e5a`; the matching [hosted security-and-build run](https://github.com/bobofbuilding/treeswap/actions/runs/32339918956) also passed. The added campaign proves genuine local EVM finalization, disagreement without durable mutation, same-nonce replacement handling, byte-identical rebroadcast, relayer rotation, and preimage non-persistence. Its agreeing origins share one local backend; the record remains local-only and grants no funding authorization.

Safety-monitor checkpoint `4b40a3ca682b63f8d1fec11fa1900448d33676f5` completed all 28 local campaigns from `2026-08-20T07:52:32.228Z` through `2026-08-20T08:59:56.235Z`. Its independently rebuilt evidence digest is `sha256:2644372eebed253ae9649c625a770953ed8c90b9d8da7876cb69ec514e265450`; the matching [hosted security-and-build run](https://github.com/bobofbuilding/treeswap/actions/runs/32345936040) also passed. The added campaign opens and halts the actual local gate through distinct test-only role contracts, proves alert ordering and no healthy-cycle reopen authority, and emits no secret material. The record remains local-only with a simulated EVM reservation, no public testnet, production infrastructure, independent review, public alert delivery, or funding authorization.

Deployment-observer checkpoint `44d929e708768d8bbe53087b415eda0f4ac75f43` completed all 29 local campaigns from `2026-08-20T10:44:40.005Z` through `2026-08-20T11:52:49.730Z`. Its independently rebuilt evidence digest is `sha256:9a0bb29bc90d603327b56606603489247f2b3cab5f3be5ecad18d2cd8417d5e9`; the matching [hosted security-and-build run](https://github.com/bobofbuilding/treeswap/actions/runs/32360223386) also passed. The first sealed attempt correctly wrote no evidence after exposing shared disposable chain-progress state in the stale-header harness. The corrected campaign gives that adapter an isolated ephemeral progress record, has a fast regression assertion for the isolation, and then passed both the full 3,604-second production-duration guard and the following stale-header rejection with zero dispatch. The added deployment campaign reconstructs the closed local gate, registry, escrows, test role wallets, and test token through two local identities and rejects production promotion. The record remains local-only with a simulated EVM reservation, no independent provider, production multisig, public testnet, production infrastructure, independent review, or funding authorization.

Live-BIT-reorg qualification checkpoint `c67d385cc00a6506ce7b8766d208c7c1ceb2b11b` completed all 29 local campaigns from `2026-08-20T12:08:42.039Z` through `2026-08-20T13:16:50.096Z`. The run again held the uncompressed chain-delay guard closed for 3,604 monotonic seconds across 119 observations and a midpoint restart, then passed stale-header, unsynced catch-up, force-close, route/duplicate, cutoff, solver, and both lost-response recovery campaigns. Its mode-`0600` evidence independently reconstructs to `sha256:20c43996d638a6d95b4d51a3c388701d5e245096dd9dc3eb9b1c5b77f986afef`; the matching [hosted run](https://github.com/bobofbuilding/treeswap/actions/runs/32367298446) passed. This is still local evidence with a simulated EVM reservation—not an independently deployed Lightning service, public-testnet proof, independent review, or funding authorization.

## Remaining campaigns

- Standard-invoice success, genuine no-route failure, exact-request replay, same-hash duplicate exposure rejection, excessive-fee and amount rejection, no-dispatch tracking, and both payer/invoice lost-response reconciliations now pass.
- Hold-invoice cancel, expiry, wrong preimage, late settle, signed-action replay, restart while accepted, and the 24-block HTLC cutoff under rapid block advancement now pass.
- Deployed stale-capacity reconciliation remains. Local stale-epoch rejection, the full production-duration delay, rapid blocks, genuine 500-block unsynced-node catch-up, full force-close/CSV-sweep/channel-replacement recovery, compressed-threshold stale-header rejection with zero dispatch, durable stateless initialization, accepted-state LND restart, channel-offline rejection/recovery, live directional exhaustion/rebalancing, and live in-flight-cap saturation now pass.
- Live payer/invoice daily-cap saturation and restart persistence pass, and deterministic journal coverage proves exact UTC rollover without weakening permanent replay protection.
- Stateless initialization, real certificate replacement and pin rollout, overlap credential rotation, deterministic old-root revocation, uninterrupted replacement service, TLS-pin mismatch, exact grant manifests, timeout enforcement, and representative forbidden-RPC categories now pass.
- Four fresh domain-bound solver challenges recover the exact declared LND node, mutation fails, cross-role calls are denied, and disposable signer/verifier credentials are revoked after use.
- Promote a clean published-checkpoint qualification record into the reviewed release manifest. The secret-free generator and schema are implemented; a record from the final release commit remains required.

This lab is local evidence, not permission to fund testnet or mainnet.
