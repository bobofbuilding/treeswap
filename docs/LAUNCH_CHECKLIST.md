# Launch checklist

Status: the public prototype gate passes. Testnet and funded-mainnet gates do not.

No repository checkbox can substitute for deployed evidence. A gate becomes complete only when its artifact, exact deployment identifiers, test result, reviewer, and date are attached to a release record.

## Public prototype — complete

- [x] Swap actions are simulations; direct sends are labeled real, wallet-authorized, irreversible, and outside bridge protection.
- [x] `1 BIT = 100 sats` is disclosed as a reference rather than a token-enforced redemption promise.
- [x] No public pool, LP share, APY, yield, reward, partial-fill, or permissionless funded-execution path exists.
- [x] Threat model, policy documents, MIT license, locked dependencies, CI, and reproducible local build exist.
- [x] Both immutable escrow directions, the sealed shared registry, open gate, and adversarial harness are in the repository.

## Funded testnet — blocked

- [ ] Record the exact BIT proxy, implementation, proxy bytecode hash, implementation bytecode hash, pause state, decimals, and finalized block in a reviewed deployment manifest.
  - [x] Credential-safe finalized-state observer and negative tests implemented.
  - [x] Finalized observation v2 reads vault available/locked/accounted totals, user-escrow liabilities, and both raw BIT balances at the same canonical block. Signed promotion rejects missing, malformed, divergent, pre-funded, or internally unreconciled accounting. The live independently observed zero-balance record remains external.
  - [x] A separate local rehearsal reconstructs the actual closed TreeSwap gate/registry/escrow topology, exact role owner sets and thresholds, runtime hashes, escrow limits, and a test EIP-1967 token at one canonical finalized block. A second local RPC identity agrees, captured quorum fails, and production promotion is explicitly rejected.
  - [x] A fail-closed signed-promotion boundary now requires the exact deployment policy, matching fresh canonical observations, review-bundle digest, finalized block, distinct provider attestations, and separate contract and operations reviewers before deriving candidate manifest evidence. Copied results and arbitrary review/provider labels cannot promote it, and the output has no funding authority.
  - [x] Published deployment-observer checkpoint `44d929e708768d8bbe53087b415eda0f4ac75f43` passed 239 application/security tests, 89 direct pinned-runtime tests plus the bounded-filesystem rollback campaign, 68 contract tests, both builds, all 29 sealed local campaigns from `2026-08-20T10:44:40.005Z` through `2026-08-20T11:52:49.730Z`, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32360223386). Its independently reconstructed digest is `sha256:9a0bb29bc90d603327b56606603489247f2b3cab5f3be5ecad18d2cd8417d5e9`; this is local-only evidence and grants no funding authorization.
  - [x] Clean published deployment-promotion checkpoint `bcbf2b03e7064be136cb54a8c567f905abec8516` passed 271 application/security tests, 91 direct pinned-runtime tests plus disk-full rollback, 68 contract tests, both builds, and all 33 sealed local campaigns from `2026-08-20T18:12:50.167Z` through `2026-08-20T19:20:55.699Z`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32401732721) passed the same source. The ignored mode-`0600` evidence independently reconstructs to `sha256:cbb4f5b62033429e8db734a8fd98f29db6b4c444ccdf5ec18949f91059f90152` and explicitly grants no funding authority.
  - [x] Current published commit `37fa0044554451b392447ac79c83021c5f41ea02` captured and independently revalidated one authenticated Alchemy observation at canonical finalized block `25807484`: exact proxy and implementation hashes, implementation slot/address, `BIT` / `18` / `false` state, source provenance, private-file safety, and privacy exclusions all match. The ignored mode-`0600` file digest is `sha256:4948f7b8dcf8e05120fc6d506783081f78cdc0d6a01ebec872c2939de34bfeda`. The revalidation uses the same provider and grants no review, promotion, or funding authority.
  - [x] Mainnet observation v3 closes the relabel/copy gap before provider handoff: it requires the canonical TreeSwap origin and clean remote-published `main`, distinct nonzero provider-identity commitments, credential-free labels, canonical exact-shape inputs, bounded freshness/skew, and digest-bound comparison v2 output. Bounded non-symlink reads, mode-`0600` non-overwriting writes, explicit external-independence status, and `fundingAuthorization: false` remain mandatory. Provider identity commitments still require real organizational evidence.
  - [x] Signed provider handoff v1 makes both operators accountable for the exact observations, eligible comparison, finalized block, and published source through a one-hour EIP-712 record. It rejects shared signers and duplicated organization, identity-evidence, or service-evidence commitments, but correctly leaves real organizational independence and backend control for external review and grants no funding authority.
  - [x] BIT independent-review v1 re-verifies that complete provider package rather than trusting its summary and requires separate contract-security and provider-independence reviewers to bind the exact source/compiler, roles/storage, upgrade, organizational-control, findings, block, and provider evidence. Reviewer/provider control overlap and critical, high, or open findings fail closed; real reviewers, retained reports, and verified organizational facts remain required.
  - [x] Reviewed BIT manifest v1 derives the exact token proxy, implementation, state anchor, token state, provider heads, signed-evidence digests, artifacts, findings, and reviewer set only from both live module-private verification objects. It rejects cloned or mixed evidence, expiry, unsafe timing, source drift, endpoint/signature material, and overwrite; its output is non-authorizing and cannot substitute for the separate TreeSwap deployment manifest.
  - [x] Clean published signed-provider checkpoint `65bbd9603771d089ba70050e5121be564895eba5` passed 408 application/security tests with one intentional deployment-only skip, both production builds, 68 contract tests, all 38 sealed local campaigns from `2026-08-22T05:57:03.649Z` through `2026-08-22T07:06:31.717Z`, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32555547734). Its mode-`0600` evidence independently reconstructs to `sha256:89d8613a766508b9049601a78a5cece6544d0456f105845d19895fd48045eee5`; all 126 configuration hashes, three pinned images, source equality, privacy exclusions, and lab teardown reproduce. The exact production deployment is [ready and promoted](https://treeswap-ln6sm26py-bittrees-tech.vercel.app). This grants no funding authority and supplies no second provider, independence finding, external review, public testnet, hardware-backed signer, or production infrastructure.
  - [x] Exact published v3 source `fa331ff41bb0a49eab55d968f250959518649ffc` captured and separately revalidated one authenticated Alchemy observation at canonical finalized block `25807580`, including exact source/identity provenance, finality, both runtime hashes, implementation, and `BIT` / `18` / `false` state. Its private 1,446-byte mode-`0600` digest is `sha256:448bbd98d68f9b7cb16430fe6a17ce99cffc73fc4ab08872d632eaf7f59d6913`. This remains one provider account and operator, with no independence finding, review, promotion, or funding authority.
  - [ ] Capture matching observations from two independent authenticated RPC providers, verify source/bytecode, and promote them through review.
- [ ] Run the full escrow suite against a controlled Ethereum mainnet fork: proxy implementation change, pause/unpause while locked, reorg before and after open/claim, nonstandard transfer behavior, and finality rollback.
  - [x] Both actual TreeSwap escrows pass local Anvil block replacement before authorization, after authorization, and after claim, including canonical recovery with one beneficiary payout.
  - [x] Clean published commit `1908b539e2bcb6fa48ce9c2883a0770979b82b01` repeats all six reorg boundaries against the pinned live BIT proxy at exact block `25788856`; evidence digest `0x1475c60668bf57ded78659302e1e03382f17a26c1d8479835f8a8a2436176507` and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32366965287) pass.
  - [x] Clean published checkpoint `c67d385cc00a6506ce7b8766d208c7c1ceb2b11b` then passed 240 application/security tests, 89 direct pinned-runtime tests plus disk-full rollback, 68 contract tests, both builds, and all 29 sealed local campaigns; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32367298446) passed. The mode-`0600` evidence independently reconstructs to `sha256:20c43996d638a6d95b4d51a3c388701d5e245096dd9dc3eb9b1c5b77f986afef` and explicitly excludes public testnet, independent providers, production infrastructure, review, and funding authorization.
  - [ ] Repeat with genuine public-testnet finalized/unfinalized transitions, two independently operated authenticated providers, deployed test escrows, and retained operator evidence.
- [x] Run an isolated Bitcoin/LND regtest adapter: standard and hold invoices, exact BOLT 11 decode, delayed/fast blocks, accepted-HTLC cutoff, timeout, LND restart, force close, credential rotation/revocation, and negative macaroon permissions.
  - [x] Reproducible two-node regtest, balanced private channel, isolated real adapter processes, distinct expiring exact-URI credentials, authorization-specific negative checks, root-key revocation, hold-invoice terminal faults, accepted-state LND restart, rapid-block 24-block cutoff, genuine 500-block unsynced catch-up, full force-close/CSV-sweep/channel-replacement recovery, compressed-threshold stale-header rejection, fee/amount/in-flight caps, channel-offline recovery, and TLS-pin mismatch implemented.
  - [x] Live directional exhaustion rejects both outbound payment and inbound hold-invoice exposure before dispatch, then the exact operations recover after channel rebalancing.
  - [x] Live payer and invoice daily caps survive adapter restart and reject before dispatch; deterministic journal coverage proves exact UTC rollover while replay protection remains permanent.
  - [x] A replacement exact-role Lightning credential overlaps with the old credential, survives old-root revocation without service loss, and returns to a clean baseline across four consecutive runs.
  - [x] A real LND certificate/key rotation invalidates the old adapter pin without dispatch, preserves the node identity and channel, explicitly reconnects the peer, and restores service on the new pin with rollback protection.
  - [x] Fresh durable chain-progress state and restart both reject new exposure until a genuinely higher LND block is observed; the live campaign then permits exactly one payment.
  - [x] An integrated local EVM/LND campaign binds both live invoice directions to actual TreeSwap escrows, requires twelve simulated EVM confirmations before Lightning action, reaches the exact accepted-HTLC safety height, proves refund is unavailable there, and proves claim/refund exclusivity at the EVM boundary. Its secret-free output grants no funding authority; the credentialed pinned-live-BIT-fork repeat now passes, while public-testnet repetition remains open.
  - [x] Clean published cross-chain-deadline checkpoint `ccae7f05b4dbe8b082cc7880924717b781b20b6f` passed 279 application/security tests, 91 direct pinned-runtime tests plus bounded-filesystem rollback, 68 contract tests, both builds, and all 34 sealed local campaigns from `2026-08-20T19:45:37.092Z` through `2026-08-20T20:54:09.927Z`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32410170977) passed the same source. The ignored mode-`0600` artifact independently reconstructs to `sha256:7bc2988a39081d511a46abb7e27e2160e5dceabe23354e6faeeab14c0381f9ec` and explicitly grants no funding authority.
  - [x] The credentialed pinned-live-BIT combined runner and distinct evidence schema are implemented and adversarially reject source, fork, proxy, implementation, code-hash, slot, symbol, decimals, pause, and deadline drift.
  - [x] Clean published checkpoint `9ba7d89a8b555f749c2a0f9a450effff340d6833` passed the credentialed combined runner through an authorized archive RPC against the exact pinned BIT proxy and implementation plus live LND in both directions. Its independently revalidated outer evidence digest is `0xb9711ca69dcdad37f41de2ca2bd321534f0404bebd8d6facf89ad2f9f8cad70c`; the nested deadline digest is `0xe2c2efc01ae1f8e04f9765e21e1069ff9f071e87b5fea3e313e3d5816bd10f8e`. The mode-`0600` record contains no endpoint or cross-network secret and explicitly excludes public testnet, independent providers, production infrastructure, and funding authorization.
  - [x] Fail-closed secret-free local qualification generator binds a clean published commit, immutable images, configuration hashes, timestamps, and complete pass states without capturing command output.
  - [x] Published checkpoint `794b9f3bedb21dd6fe39dae0d7a10a5e94289899` passed all 25 local campaigns and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32312491429); independently rebuilt local-only evidence digest `sha256:985eb9ff7eb610982edfa27088a731b6eabd277add22bfe0a7dabf6d61aba825`.
  - [x] Authenticated-endpoint checkpoint `f474e9c577f9c4e70183275f693ce89216e24032` passed all 25 local campaigns from `2026-08-20T00:43:19.448Z` through `2026-08-20T01:49:47.891Z` and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32318279120); its independently rebuilt local-only digest is `sha256:55170d90bb3509ca5c0f133327290863d158f2cfa6e0401178a7b8ce3d523b3a`.
  - [x] Capacity-reader checkpoint `67655f859ec70c191501d073e75cba808ce06def` passed all 26 local campaigns from `2026-08-20T02:15:55.025Z` through `2026-08-20T03:23:02.372Z` and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32323948108); independently rebuilt local-only evidence digest `sha256:0c20cb3ea69ca7eb56ed5f79b215ad317523908ee09436ac4203966c90ac3d58`.
  - [x] Daemon-recovery checkpoint `e860eabbfb188d3597df25ee8dfa14001126026e` passed all 26 local campaigns from `2026-08-20T03:38:59.074Z` through `2026-08-20T04:45:56.472Z` and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32328893965); independently rebuilt local-only evidence digest `sha256:65a86a92f7f93407df0cc8254e61cf3740fda2aaf9f76d663c3d9d49e8895419`. The record is mode `0600`, ignored, secret-free, and not funding authorization.
  - [x] Prior clean published checkpoint `9ba7d89a8b555f749c2a0f9a450effff340d6833` passed 381 application/security tests, both production builds, 68 contract tests, all 37 sealed local campaigns from `2026-08-21T21:15:10.042Z` through `2026-08-21T22:24:14.633Z`, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32520096089). The run included 3,603 monotonic seconds, 119 continuous no-progress observations, midpoint adapter replacement with persisted state, deterministic rejection, zero target-payment dispatch, genuine 500-block catch-up, force-close/CSV sweep/channel replacement, rapid-block cutoff, both cross-chain directions, solver proof/capacity/admission, and both lost-response paths. Its mode-`0600` evidence digest is `sha256:164dfdf522a9dec83c391d62b1f283aafee41fdd4012d5be50f6cb8b460051dd`; all 113 configuration hashes and the privacy scan independently reproduce. The artifact grants no funding authority.
  - [x] Prior clean published service-isolation checkpoint `0bd76995350011fdaf4cdd2f1e22c3db47a2e4f8` passed 388 application/security tests, the production Vercel build, 68 contract tests, and all 37 sealed local campaigns from `2026-08-21T22:58:49.911Z` through `2026-08-22T00:07:59.522Z`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32534947589) passed the same source. Its mode-`0600` evidence independently reconstructs to `sha256:49fe2e48498ac10d251f975663bcdca17c1fd8a68ecb3c12e3502a67b75cc3a7`; all 116 configuration hashes, exact campaign order, file safety, privacy exclusions, and lab teardown independently reproduce. This local artifact includes no public testnet, independent provider, deployed infrastructure, external review, or funding authority.
  - [x] Current clean published adoption-policy checkpoint `3bf4b17d94236c3ab3bf1ee3114c39ef6a01ee77` passed 393 application/security tests, the production Vercel build, 68 contract tests, and all 37 sealed local campaigns from `2026-08-22T00:41:36.305Z` through `2026-08-22T01:50:27.616Z`; [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32540977722) passed the same source. Its mode-`0600` evidence independently reconstructs to `sha256:281f8210ca426239225f7bd344b699de840e51eb205b4ec02e2d88ee90d65e31`; all 117 configuration hashes, the exact 37-campaign order, three pinned images, source provenance, file and directory safety, privacy exclusions, and lab teardown independently reproduce. This local-only artifact uses a simulated EVM reservation and includes no public testnet, independent provider, deployed infrastructure, external review, or funding authority.
  - [x] Prior BIT independent-review-handoff checkpoint `e893b65038cea566ecbd7a2c63795d6ab99abe7f` passed 414 application/security tests with one intentional deployment-only skip, both production web builds, 68 contract tests, all 39 sealed local campaigns from `2026-08-22T07:31:35.891Z` through `2026-08-22T08:40:43.586Z`, and [hosted main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32559681865); the exact [production deployment](https://treeswap-o24m03oo6-bittrees-tech.vercel.app) is ready. Its ignored regular mode-`0600` evidence independently reconstructs to `sha256:ffb53940aa999e2cb4556d4cf5037a157697b31fb9a3c46744395f8ce9ca4b00`; all 130 configuration hashes, 39 unique passed campaigns, three pinned images, clean canonical remote-`main` binding, privacy exclusions, and empty lab teardown reproduce. The 3,603-second Lightning delay included 119 observations and a midpoint restart. This local-only artifact uses a simulated EVM reservation and includes no real independent provider, external reviewer or reports, organizational-independence finding, public testnet, deployed coordinator infrastructure, or funding authority.
  - [x] Current reviewed-BIT-manifest checkpoint `a0e8291f6c20816a8ee66387a814f794300efe57` passed 418 application/security tests, both production web builds, 68 contract tests, all 40 sealed local campaigns from `2026-08-22T09:05:57.146Z` through `2026-08-22T10:15:06.747Z`, and [hosted main-branch CI](https://github.com/bobofbuilding/treeswap/actions/runs/32563882756); the exact [production deployment](https://treeswap-pru2plff2-bittrees-tech.vercel.app) and [canonical alias](https://treeswap.vercel.app) are ready. Its ignored mode-`0600` evidence independently reconstructs to `sha256:6274811e83196f98c2bd3543bab930fdc2e325e97bd3a81585ef2456b2fc0da9`; all 132 configuration hashes, 40 unique passed campaigns, three pinned images, clean canonical remote-`main` binding, privacy exclusions, and empty lab teardown reproduce. The one-hour Lightning campaign passed its full minimum, midpoint adapter replacement, stale-chain rejection, and zero target-payment dispatch. This local-only artifact uses a simulated EVM reservation and includes no real independent provider, external reviewer or reports, organizational-independence finding, public testnet, deployed coordinator infrastructure, separate TreeSwap deployment, or funding authority.
  - [x] The completed clean-published run is bound to `c67d385cc00a6506ce7b8766d208c7c1ceb2b11b`, ran from `2026-08-20T12:08:42.039Z` through `2026-08-20T13:16:50.096Z`, passed all 29 campaigns, and has independently reconstructed digest `sha256:20c43996d638a6d95b4d51a3c388701d5e245096dd9dc3eb9b1c5b77f986afef`.
- [ ] Deploy the gate, registry, and both escrows from the reviewed source commit. Seal the registry to exactly those escrows and prove every constructor immutable and runtime code hash.
  - [x] An offline operator workflow now forces a clean-published-source rebuild and deterministically prepares four unsigned Sepolia CREATE transactions plus three hash-linked controller calls. A second clean checkout can reproduce the complete plan exactly; mutation, symlink, overwrite, source, artifact, nonce, role, token, risk, order, and authorization drift fail closed. The workflow does not sign, broadcast, open, fund, or prove live state. See [Closed public-testnet deployment plan](./CLOSED_TESTNET_DEPLOYMENT.md).
  - [x] A short-lived preflight now anchors all reads to one canonical Sepolia block, proves the deployer has no runtime code and every predicted address is empty, checks the deployer's anchored and before/after pending nonce, reconstructs all three role wallets and BIT, requires matching observations from at least two separately signing provider identities plus a distinct operations reviewer, and binds every approval to the exact plan/input/source/review/block/nonce. Mutation or expiry fails, and the verified result grants no operational authority.
  - [x] A signed postflight now binds that exact preflight to four canonical creation receipts and three standard Safe execution receipts, checks exact inner calls and success/registry events, enforces the signed time and nonce sequence, and requires two matching finalized zero-balance manifests signed by the retained providers and operations reviewer plus a distinct contract reviewer. Local adversarial evidence only; it grants no operational authority. See [Signed closed-deployment postflight](./CLOSED_TESTNET_DEPLOYMENT_POSTFLIGHT.md).
  - [x] The local timed-Anvil rehearsal signs a two-identity preflight, executes the generated four-transaction/three-standard-Safe-call plan, reconstructs all finalized receipts, signs the two-identity/two-reviewer postflight, derives and signs a postflight-bound promotion, and observes a closed, sealed, empty deployment. Both identities share one local backend and all wallets/signers are test-only.
  - [x] Promotion v2 re-verifies the signed postflight, and release authorization v2 plus runtime funding bind the exact postflight and promotion digests.
  - [x] Separate evidence-derived release preparation now breaks the testnet sequencing safely: a pre-campaign `operator-testnet-bootstrap` record has hard tiny limits and cannot become qualified, while `operator-testnet` requires the complete signed seven-day campaign. Both re-verify the postflight-bound promotion and have no signing or funding authority. See [Public-testnet release candidates](./PUBLIC_TESTNET_RELEASE_CANDIDATES.md).
  - [x] The guarded five-role ceremony derives each exact policy signer and EIP-712 payload, verifies the complete bundle through the candidate-bound finalized ERC-1271 provider quorum, and writes only a private non-activating receipt. Deserialized candidate consistency and that receipt are explicitly not upstream provenance. See [Public-testnet release approvals](./PUBLIC_TESTNET_RELEASE_APPROVALS.md).
  - [ ] Supply independently reviewed Sepolia BIT and Safe addresses, hardware-backed owners, two independently operated authenticated providers, signed preflight and postflight observations, exact live nonce, signed execution receipts, and matching finalized zero-balance state.
- [ ] Deploy distinct 2-of-3-or-stronger controller, guardian, and fee-recipient contract wallets; record owners, thresholds, hardware-key custody, and recovery contacts.
  - [x] A local fail-closed release verifier requires one exact short-lived record and policy digest approved by five distinct identities. Controller and guardian approvals use ERC-1271 at the record's exact canonical finalized block, agree through the exact signed provider-set digest, and match pinned wallet runtime hashes; the Lightning operator, security reviewer, and incident commander sign the same EIP-712 digest.
  - [x] Every funded public-testnet record now requires at least two independent monitors plus all provider/observer/relay/solver/alert minima, loss allocation, support policy, and five report digests derived from a fresh five-reviewer signed evidence package. The qualified mode additionally requires campaign evidence; only the separately named bootstrap mode may omit it, under hard code-level ceilings.
  - [x] A release-derived capability and its runtime snapshot are an exact module-private pair. The funding boundary rejects copied or manufactured snapshots, copied capabilities, arbitrary feature toggles, nominal audit booleans, stale or expired reconciliation, a different deployment digest, a closed gate, or unreconciled balances.
  - [x] Same-process activation now requires a provenance-bound candidate and provider set, re-verifies all five release approvals, verifies a short-lived Lightning-operator/security-reviewer EIP-712 reconciliation, and obtains exact live provider agreement on the canonical head, release-bound gate digest, sealed registry, escrow and BIT code, EIP-1967 implementation, pause state, accounting, reserves, and in-flight caps. Provider disagreement, implementation or code change, accounting mismatch, reserve shortfall, and copied inputs fail closed. See [Public-testnet release activation](./PUBLIC_TESTNET_RELEASE_ACTIVATION.md).
  - [x] The standalone approval verifier rejects candidate or domain mutation, signer-kind downgrade, missing/duplicate/substituted/replayed signatures, provider-set or canonical-block disagreement, stale releases, symlinks, oversized input, and output overwrite. It emits no raw signature, provider URL, gate authority, funding authority, or reusable capability provenance.
  - [x] Signed-release checkpoint `2e2917389b6191a4c62bdbe56f6bab9904141406` passed 252 application/security tests, 91 direct pinned-runtime tests plus disk-full rollback, 68 contract tests, both builds, all 31 sealed local campaigns from `2026-08-20T15:15:57.828Z` through `2026-08-20T16:34:26.514Z`, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32384877772). The ignored mode-`0600` evidence independently reconstructs to `sha256:22011a8ab9c608dbbac34cc88b09074d9f4a562626666d33e208ae22922a6fa3` and explicitly grants no funding authorization.
  - [ ] Supply real Safe addresses, hardware-owner custody evidence, independent RPC providers, independently operated runtime reconciliation signers, operator/reviewer identities, evidence artifacts, and retained signatures. Each role must independently reproduce and compare the candidate before signing; deploy the persistent coordinator so the candidate, approvals, reconciliation, and live state are verified in one uninterrupted activation process. Current repository tests and a standalone receipt are not those external facts.
- [ ] Operate atomic persistent RFQ quotas, cancellation sequence, capacity epochs, solver reliability, idempotent Lightning requests, and one-shot payment authorization.
  - [x] Atomic settlement/action store, one-dispatch Lightning outbox, restart recovery, and live payer- and invoice-side lost-response reconciliation implemented.
  - [x] Connect the EVM claim outbox to exact signed bytes, canonical receipt/finality checks, and fail-closed reorg detection; local execution-client evidence passes.
  - [x] EVM reconciliation is read-only across exactly two provider origins and mutates durable state once only after exact agreement. A controlled timed-slot Anvil campaign passes finalized claim, disagreement-without-mutation, higher-fee same-nonce replacement, byte-identical rebroadcast, relayer rotation, and preimage non-persistence. The agreeing origins share one local backend, so this is not independent-provider evidence.
  - [x] Atomic local RFQ/admission persistence covers opaque rolling identity quotas, permanent cancellation sequences, monotonic capacity epochs, non-oversubscribed firm commitments, fill/failure reliability, and fail-closed suspension across restart and competing connections.
  - [x] Repository solver admission is open and cryptographic: executable quotes bind the verifier-issued capability and capacity snapshot, exact inventory, endpoint key, individual solver epoch, expiry, and escrow runtime code. Indicative quote books cannot authorize settlement.
  - [x] Unknown BIT → Lightning solvers are limited by the configured first-fill cap, and one atomic global in-flight ceiling spans all solver identities. They can reach the established cap only through completed settlements carrying an exact selected-offer and independently verified both-assets terminal proof. Lightning → BIT remains limited to verified solver-owned prefunded BIT.
  - [x] The exact pinned coordinator image passes schema-v4 parity, verified non-overwriting backup/fresh-path restore, corruption and unknown-schema refusal, SIGKILL/WAL recovery, v2 migration, fail-closed v3 capability migration, solver capability, endpoint, capacity, action-recovery, and daemon-ordering verification, plus a real bounded-filesystem `SQLITE_FULL` rollback. The expanded pinned-runtime matrix passes 52 tests locally; published daemon-recovery commit `e860eabbfb188d3597df25ee8dfa14001126026e` also passed all 210 application/security tests and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32328893965).
  - [x] The local capability verifier binds EVM, LND-node, and endpoint-key possession to exact direction, inventory, epoch, and expiry; only fresh independently observed capacity reaches durable admission, and no firm quote may outlive the capability.
  - [x] The local endpoint protocol issues a fresh random challenge, authenticates the exact short-lived response with the bound Ed25519 key, pins the expected solver/direction/origin, refuses redirects and private or mixed DNS, preserves TLS hostname verification, and fails closed on timeout, malformed response, replay, or overstatement.
  - [x] Concrete local readers compare finalized canonical vault state across two RPC providers and obtain separately signed, direction-bound, reserve/budget-capped Lightning aggregates without channel identifiers. Mutation, replay, provider disagreement, insolvency, wrong code, cross-role use, and timeout fail closed.
  - [x] The durable-state daemon planner enforces restart/reconciliation priority and direction-specific payment/claim ordering. A lost or later-restarted successful payer action can recover its exact bound preimage through signed tracking while SQLite, WAL, and shared memory retain no invoice or preimage.
  - [x] The local one-use private-packet protocol and bounded daemon runtime bind an exact fresh packet plus observed reservation block and deadlines to an independent pre-dispatch authorization, execute at most one value-moving step, reconcile ambiguity without redispatch, repair an unbound EVM action after restart, and require a both-assets proof before terminal state. The current local matrix passes 246 application/security tests and 91 direct pinned-runtime tests plus the bounded-filesystem rollback campaign.
  - [x] Permissionless-admission checkpoint `430a829bed99e88596d27326aecb6e3aca21a35a` passed that matrix, 68 contract tests, both builds, the dedicated 40-test permissionless-admission campaign, all 30 sealed local campaigns, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32375971751). The ignored mode-`0600` evidence independently reconstructs to `sha256:acd75e9bd650883329ffac589e802c6c429d6afecab96e63d80bd220ec9e64c3` and explicitly excludes public testnet, independent providers, production infrastructure or multisigs, independent review, and funding authorization.
  - [x] Published authenticated-runtime checkpoint `ac08946d962d062845d585a716c829c0ad73a4a0` passed all 26 sealed local campaigns and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32334233367). Its independently reconstructed evidence digest is `sha256:dc6f60bacfa32332eb90bba0b75a879e35022f1cb473cba17241f225feb5450d`; its scope remains local-only with no funding authorization.
  - [x] Published EVM-outbox quorum checkpoint `cd2a81c0f5ecc7ab1902a1fa576d71f4c7520509` passed 226 application/security tests, 74 direct pinned-runtime tests plus the bounded-filesystem rollback campaign, 68 contract tests, both builds, all 27 sealed local campaigns, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32339918956). Its independently reconstructed digest is `sha256:f0e362260e813c3880b4328800a074c8f57df1459ec6b37a045369a718d17e5a`; agreeing local EVM origins share one backend, and the record grants no funding authorization.
  - [ ] Deploy the authenticated capability endpoint, private-packet provider, readers, finality authorizers, asset verifier, and bounded daemons with independent operators; add reviewed encrypted private transport, persistence, retained backups, alerts, and deployed-volume recovery drills; reproduce the EVM evidence on the pinned BIT fork and public testnet with two independently operated providers.
- [ ] Operate continuous BIT proxy/pause/finality/price monitoring and BIT/Lightning/in-flight reconciliation. Prove alerts close the gate and never block exits.
  - [x] A bounded local secret-free monitor requires eight exact health domains, closes quotes, halts the actual gate through a distinct guardian contract, alerts after closure, and has no reopen authority. Contract tests separately keep existing claims, refunds, and solver withdrawals outside the gate.
  - [x] Published safety-monitor checkpoint `4b40a3ca682b63f8d1fec11fa1900448d33676f5` passed 233 application/security tests, 81 direct pinned-runtime tests plus the bounded-filesystem rollback campaign, 68 contract tests, both builds, all 28 sealed local campaigns, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32345936040). Its independently reconstructed digest is `sha256:2644372eebed253ae9649c625a770953ed8c90b9d8da7876cb69ec514e265450`; the record is local-only and grants no funding authorization.
  - [x] Candidate preparation now re-verifies a three-role service-isolation package and a five-role operational-readiness package. It requires exact twelve-service trust-domain, network, credential-scope, deployment, alert, backup, monitoring, reconciliation, privacy-retention, incident, support, loss, solver, provider, and qualification evidence. It cannot turn either package into a funding capability and does not prove the committed infrastructure or external artifacts are truthful.
  - [ ] Deploy independent continuous collectors, redundant guardian broadcasters, public alert routing, retention, and operator escalation; repeat against deployed escrows with outstanding testnet liabilities.
- [ ] Run at least two independent solvers with tiny testnet limits and no public deposits or rewards.
  - [x] The candidate public-testnet evidence boundary requires two signed solver operators plus two providers, observers, monitors, relays, and alert channels; at least seven days; at least twenty selected swaps per solver with ten in each direction; exact fault scenarios; closed start/end gates; and final zero-liability reconciliation. This verifier is not evidence that those external operators exist.
  - [x] The operator campaign can now be collected without hand-editing the final record: exact policy/participants initialize revision zero; each scenario or solver metric creates a non-overwriting, mode-`0600`, hash-linked snapshot; checkpoints expose omissions; transition verification detects removal/substitution; and finalization derives the safe record through the production verifier. These controls do not run or attest the external campaign.
  - [x] Candidate preparation re-verifies both the fresh deployment promotion and completed campaign, derives every shared release field, commits to both record and policy digests, and combines provider/findings inputs without overwrite ambiguity. A copied verifier result, stale ordering, mismatched manifest/source/chain/gate/provider count, Safe substitution, missing review, symlink, or overwrite fails closed.
  - [x] Bootstrap preparation requires a fresh signed operator roster rather than claimed counts. Every listed provider, Lightning observer, monitor, relay, and solver attests the same deployment and evidence boundary; identities and signers are globally distinct, EVM providers exactly match the deployment promotion, and copied verification cannot enter candidate preparation. The signatures do not establish organizational independence or make retained evidence truthful.
  - [x] Published evidence-boundary checkpoint `e662885346791b80153835c3123f0f77b63ad9f5` passed 263 application/security tests, 91 direct pinned-runtime tests plus bounded-filesystem rollback, 68 contract tests, both builds, all 32 sealed local campaigns from `2026-08-20T16:49:58.056Z` through `2026-08-20T17:57:51.589Z`, and [hosted CI](https://github.com/bobofbuilding/treeswap/actions/runs/32394191747). The ignored mode-`0600` evidence independently reconstructs to `sha256:de647c4a2b35e411bed48a2062e2964c008702f6f17715072b5d9c9388575e6d`; it is local-only, uses a simulated EVM reservation, and grants no funding authority.
- [ ] Deploy durable SIWE/session storage only if accounts are enabled; otherwise disable the account feature. Keep email delivery disabled.
  - [x] The repository now probes the bound database for the exact nonce, session, and notification schema before enabling accounts. Missing, partial, malformed, or unavailable storage disables the UI and rejects nonce creation before a wallet signature; email delivery remains hard-disabled.
  - [x] Owner-only Sites version 11 at exact published checkpoint `e23f5a6d635f1cc33930b24f1f1748f8c46eab9f` passed all 13 secret-free [live account evidence](./LIVE_ACCOUNT_EVIDENCE.md) checks against D1: capability/schema, nonce single-use, cookie policy, persistence, transactional concurrent rotation, origin rejection, sign-out deletion, and no email record. The access policy exposed one owner, no groups, and no external visitors. The ignored mode-`0600` record independently reconstructs to `0xb21f0a51522d966d6b58c916373ccc06c1bb059b6b1252aecaf996588c51ac00`; it excludes independent access attestation, monitoring, backup/restore, purge evidence, email delivery, and funding authority. The Vercel presentation was separately observed returning the exact disabled capability.
  - [x] [Bounded account maintenance](./ACCOUNT_MAINTENANCE_EVIDENCE.md) is implemented without global sweeps on routine account requests: one transactional D1 batch deletes at most 100 already-expired nonces, sessions, and notification records per table, returns aggregate counts only, and requires the exact private origin, complete storage capability, and an active SIWE session. The live observer is fixed to clean published `main`, waits through a real nonce expiry, verifies active-session survival and post-signout denial, and retains no account secrets. This implementation does not create a scheduler or funding authority.
  - [x] Owner-only Sites version 12 at exact published checkpoint `b63ca78870628fa2a1bd5ef0c33fb979863ae95d` passed all nine [account maintenance evidence](./ACCOUNT_MAINTENANCE_EVIDENCE.md) checks across a real ten-minute nonce expiry: exact-origin enforcement, active-SIWE authorization, bounded aggregate response, expired-nonce deletion, active-session survival, and post-signout denial. The ignored mode-`0600` record independently reconstructs to `0x1b5a81b5a28755ea559f3d5f76b28270209b3425af2cc8fb9cafe0be2c45800a`; independently aged session and notification deletion, scheduling, monitoring, access attestation, backup/restore, email, and funding remain excluded. The Vercel presentation remained account-disabled and rejected the private-origin maintenance request.
  - [ ] Independently review and attest the D1 access and least-privilege operator policy.
  - [ ] Deploy continuous account storage failure, latency, unauthorized-access, purge-backlog, and escalation monitoring.
  - [ ] Create a retained backup and complete a witnessed restore into an isolated database.
  - [ ] Schedule bounded expired-record maintenance and retain successful deletion evidence for an independently aged nonce, session, and notification record.

## Capped mainnet beta — blocked

- [ ] Obtain independent review of the Solidity contracts, Lightning timing/adapter, solver/coordinator, authentication/privacy, and operational controls; pin the report digest in the deployment manifest.
  - [x] A local five-reviewer evidence boundary requires distinct reviewer identities, signers, and organization commitments; exact source/protocol/deployment binding; one report and disposition per role; reconciled zero-open findings; no accepted critical/high risk; and one EIP-712 attestation per reviewer. Release candidates derive review digests from that provenance and reject the former unsigned review-template path. Real reviewers and reports remain required.
- [ ] Complete an incident drill for BIT upgrade, BIT pause, oracle disagreement, Ethereum reorg, LND outage, credential compromise, inventory mismatch, and suspected preimage leak.
  - [x] The release boundary now structurally requires those incidents plus alert delivery, backup/restore, provider outage/disagreement, gate-exit preservation, and monitor outage, each with bounded times, distinct evidence, a primary operator, two retained observers, and five operational-role signatures. Actual deployed drills remain required.
- [ ] Publish exact per-swap, per-epoch, daily Lightning, in-flight, routing, price-band, and inventory-reserve caps. Start substantially below tested limits.
  - [x] The exact public adoption-policy schema and verifier now impose mode-specific ceilings and require a candidate's eight cap/reserve fields to match byte-for-byte. Publication of a real signed policy for a deployed release remains open.
- [ ] Publish loss allocation, trust assumptions, solver liveness limits, privacy linkage, upgrade response, and support/escalation contacts.
  - [x] Adoption-policy v1 makes each item explicit, requires three distinct public HTTPS support/security/status paths and named owners, and binds the complete digest into all five operational signatures and candidate v4. Real staffed contacts, endpoint availability, policy acceptance, and deployed enforcement remain open.
- [ ] Collect testnet reliability data and adopt either an objectively adjudicable solver bond or an explicit no-bond exclusion/limit policy reviewed for the beta.
- [ ] Require a final release sign-off from engineering, security reviewer, Lightning operator, contract-wallet signers, and incident commander.
  - [x] The repository verifies the corresponding controller, guardian, Lightning-operator, security-reviewer, and incident-commander approvals over one exact release record and refuses missing, duplicate, wrong-role, replayed, expired, or wrong-domain signatures. No production approvals exist.

## Public liquidity — intentionally out of scope

Public deposits, transferable shares, yield, rewards, and withdrawal queues require a new protocol and separate custody, accounting, insolvency, adverse-selection, economic, and legal review. They cannot be enabled by changing a v1 flag.

The ordered work packages and evidence required to progress these gates are in [Production readiness](./PRODUCTION_READINESS.md).
