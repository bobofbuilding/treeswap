name: treeswap-regtest

services:
  bitcoind:
    image: bitcoin/bitcoin:31.1@sha256:da25cedc66b1daefff9f412ee196c901a899c3fa68a33b20849c3e08b5c40d63
    restart: unless-stopped
    environment:
      BITCOIND_RPC_USER: ${BITCOIND_RPC_USER}
      BITCOIND_RPC_PASSWORD: ${BITCOIND_RPC_PASSWORD}
    command:
      - -regtest=1
      - -server=1
      - -txindex=1
      - -listen=1
      - -fallbackfee=0.0002
      - -rpcbind=0.0.0.0
      - -rpcallowip=0.0.0.0/0
      - -rpcuser=${BITCOIND_RPC_USER}
      - -rpcpassword=${BITCOIND_RPC_PASSWORD}
      - -zmqpubrawblock=tcp://0.0.0.0:28332
      - -zmqpubrawtx=tcp://0.0.0.0:28333
    healthcheck:
      test: ["CMD-SHELL", "bitcoin-cli -regtest -rpcuser=$$BITCOIND_RPC_USER -rpcpassword=$$BITCOIND_RPC_PASSWORD getblockchaininfo >/dev/null"]
      interval: 2s
      timeout: 2s
      retries: 60
    volumes:
      - bitcoind-data:/home/bitcoin/.bitcoin
    networks: [regtest]

  alice:
    image: lightninglabs/lnd:v0.21.2-beta@sha256:17e7e5adf823717a8e1ba7e02ac5d90eb58a029b5d30073914f64ab23e7cebf4
    restart: unless-stopped
    depends_on:
      bitcoind:
        condition: service_healthy
    command:
      - --noseedbackup
      - --bitcoin.active
      - --bitcoin.regtest
      - --bitcoin.node=bitcoind
      - --bitcoind.rpchost=bitcoind:18443
      - --bitcoind.rpcuser=${BITCOIND_RPC_USER}
      - --bitcoind.rpcpass=${BITCOIND_RPC_PASSWORD}
      - --bitcoind.zmqpubrawblock=tcp://bitcoind:28332
      - --bitcoind.zmqpubrawtx=tcp://bitcoind:28333
      - --rpclisten=0.0.0.0:10009
      - --restlisten=0.0.0.0:8080
      - --listen=0.0.0.0:9735
      - --tlsextradomain=alice
      - --alias=treeswap-alice
      - --color=#3ea34b
    volumes:
      - alice-data:/root/.lnd
    networks: [regtest]

  bob:
    image: lightninglabs/lnd:v0.21.2-beta@sha256:17e7e5adf823717a8e1ba7e02ac5d90eb58a029b5d30073914f64ab23e7cebf4
    restart: unless-stopped
    depends_on:
      bitcoind:
        condition: service_healthy
    command:
      - --noseedbackup
      - --bitcoin.active
      - --bitcoin.regtest
      - --bitcoin.node=bitcoind
      - --bitcoind.rpchost=bitcoind:18443
      - --bitcoind.rpcuser=${BITCOIND_RPC_USER}
      - --bitcoind.rpcpass=${BITCOIND_RPC_PASSWORD}
      - --bitcoind.zmqpubrawblock=tcp://bitcoind:28332
      - --bitcoind.zmqpubrawtx=tcp://bitcoind:28333
      - --rpclisten=0.0.0.0:10009
      - --restlisten=0.0.0.0:8080
      - --listen=0.0.0.0:9735
      - --tlsextradomain=bob
      - --alias=treeswap-bob
      - --color=#f4a340
    volumes:
      - bob-data:/root/.lnd
    networks: [regtest]

networks:
  regtest:
    internal: true

volumes:
  bitcoind-data:
  alice-data:
  bob-data:
